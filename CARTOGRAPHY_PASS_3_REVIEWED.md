# Global counterexample cartography — reviewed pass 3 replacement

**Cutoff:** 2026-07-22 20:28:06 UTC  
**Previous published cutoff:** 2026-07-22 20:10:33 UTC  
**Exact provenance:** [`ANALYSIS_SNAPSHOT_PASS_3R.md`](ANALYSIS_SNAPSHOT_PASS_3R.md)  
**Quality audit of the replaced pass:** [`PASS_3_QUALITY_AUDIT.md`](PASS_3_QUALITY_AUDIT.md)

## Executive correction

The first publication of pass 3 was mathematically useful but operationally incomplete. This replacement integrates its valid findings into the primary map and adds four material corrections:

1. PR #33's frozen corrected-stage exclusion is independently verified at its frozen source, with native-ledger integration still pending.
2. A permanent phase-1 continuation of issue #39 is exactly ordinary Collatz in a shifted coordinate and is not a separate amplifier.
3. The quotient-refund atom is canonically `ACL-P036`; the duplicate `ACL-P038` label is retired.
4. PR #19 has advanced to a proposed H-cycle exclusion through `2,479,700,524` blocks plus entropy and prefix-return barriers.

No unconditional counterexample was found.

## 1. Frozen corrected phase-34 class: independently verified negative result

PR #44 independently reconstructed the exact PR #33 chain at

```text
PR #33: c9d62bce3e93f5785f72e4520bc576863d9379eb
PR #3 interface: f274dfeee3c9c391c48e58d8b57cb9f1759236f8
```

and returned `PASSED` for:

```text
L-9704 -> L-9705 -> L-9706 -> T-9705.
```

The current global standing is:

> Every physically overlapping directive in the frozen corrected 256-transition phase-\(-34\) class has a nonordinary signed completion.

This is green at the frozen-source confidence level. The PR #33 native ledger still needs integration. The result says nothing about:

- linear-height quotient-refund schedules;
- cross-cycle handoffs;
- adaptive stage architectures;
- growing essential rank;
- persistent degeneracy;
- or changing internal prime alphabets.

The old room/seam search inside the frozen class is closed as a counterexample route. Its formulas remain useful for constructing architectures that explicitly leave the class.

## 2. Direct cycle route: still the shortest certificate

A positive cycle remains logically closest to a disproof because the final object is finite.

### Exact/proposed negative packets

PR #42's proposed theorem and exact MITM packet cover all admissible cycle windows through 27 accelerated odd terms:

```text
24 admissible (m,K) windows,
802,459,998,516 positive compositions covered,
0 modular matches.
```

Its separate `(41,65)` experiment covers:

```text
all {1,2} words of total 65,
all words with exactly one exponent >=3,
35,548,596,168,868 words,
0 modular matches.
```

These are exact bounded exclusions, not evidence against unrestricted critical-scale words.

PR #13 wave 7 supplies a native proposed theorem excluding odd-state length `184`, conditional only on the cited Hercher local-minimum theorem and the committed exact decoder/stability proof. It is a local-minimum frontier theorem, not the current full verified-height frontier.

### Critical scale

Issue #41 corrects the search scale. The cycle product identity forces serious candidates onto huge continued-fraction shapes. PR #45 therefore works with

```text
k=3,149,971,404,836,
A=4,992,586,555,009.
```

Its first compressed construction forces the numerator to vanish modulo a certified 61-bit factor product of the denominator, but a directed interval proves that the rational fixed point lies more than `0.00247` from the next integer. This is a rigorously rejected near-candidate.

The live finite target is still:

```text
C(w)=n(2^A-3^k)
```

for the **entire** denominator, followed by exact valuation replay.

## 3. Negative-cycle perturbations: valid algebra, critical-scale warning

PR #47 proves an exact one-pulse formula for repeated known negative cycles. Its frozen scan of 720,000 reduced candidates finds only the trivial `n=1` cycle.

The distributed-pulse generalization from the original pass is correct. For a repeated base word of length `N`, base prefix exponents `A_j`, and pulse increments `delta_i`, put

```text
Delta_j=sum_(i<j)delta_i,
Delta=sum_i delta_i.
```

Then

```text
C_delta
 =sum_(j=0)^(N-1)3^(N-1-j)2^(A_j+Delta_j),

D_delta
 =2^(A_N+Delta)-3^N.
```

A positive cycle requires

```text
C_delta=nD_delta
```

and exact replay.

If valuation `a_i` is increased by `delta`, the current affine constant changes by

```text
(2^delta-1)
 * sum_(j=i+1)^(N-1)3^(N-1-j)2^(A_j),
```

with the suffix evaluated after earlier pulses. Thus fixed-total-pulse families can be compiled with the upper-triangular affine monoid and joined modulo their full denominator.

The strategic correction is essential:

> Small repetition counts are regression controls. A breakthrough distributed-pulse search must be compiled at a source-admissible critical continued-fraction/verified-height scale and must coordinate cancellations across all prime-power factors of the denominator.

## 4. Linear-height quotient refund: strongest new divergent architecture

PR #13 `LIT-KTHM-0050` proves for the linear phase-\(-34\) grid

```text
t_j=B+16j
```

that one stage has

```text
A(B)=1792B+3657472,
E(B)=2816B+5792512,
```

and, for every divisible `B>=477424`,

```text
3^[A(B)]>2^[E(B+4096)].
```

For every finite current/next word pair, the quotient congruence has one residue class

```text
Y=y_0+k*2^[E(B+4096)]
```

and the next quotient is

```text
Y_next=c_(w,v)+3^[A(B)]k.
```

Large lifts are positive and expanding.

This is outside the frozen PR #33 theorem because it reverses the quotient-extinction inequality. But pairwise transitions still do not give one infinite ordinary orbit.

The canonical atom is now:

```text
ACL-P036 — linear-height quotient-refund invariant.
```

A valid invariant must choose the word and lift causally from current finite control plus one unbounded quotient/carry, and certify the next most-significant boundary without referring to the next-next cylinder.

Two independent issue-#43 attempts are active.

## 5. Centered forced tail: exact one-counter problem

PR #44 starts from the independently verified centered recurrence and proves that once appended blocks are zero,

```text
64B'=81B+e-e'.
```

The legal branches are exact, `B+4e mod17` is invariant, and every positive legal step strictly increases `B`. Therefore any explicit positive all-time legal seed is automatically a divergent induced-chart orbit.

However, at precision `64^d`, the greatest recurrent residue kernel is exactly the finite cylinder/de Bruijn graph. Every fixed-modulus lasso is a periodic completion ghost.

Thus deeper residue PDR cannot settle the ordinary section. The required certificate needs:

```text
finite control,
previous digit/sign,
carry <=17,
one unbounded top/length coordinate,
canonical most-significant closure or carry flush.
```

This is a sharper positive target than generic all-itinerary search, but no seed is known.

## 6. Cross-cycle handoff: phase-1 is a secret reduction

Issue #39's scale-22 cell and physical handoff remain exact finite evidence outside the frozen PR #33 class.

A later issue comment supplies the key reduction. In phase `v=1`, with `q=n+1`,

```text
(q,1) -> (T(q-1)+1,1).
```

A permanent phase-1 tail is therefore exactly the original shortcut Collatz dynamics in a shifted coordinate. The huge phase-1 quotient and its 10,000-renewal prefix do not provide a new amplifier or induction.

A genuine cross-cycle certificate must instead prove one of:

1. infinitely many exits from phase 1 into nontrivial negative-cycle phases with net regenerative gain;
2. a finite positive return before the phase-1 reduction;
3. a multi-phase invariant whose proof does not reduce to the original conjecture.

This moves issue #39 below quotient refund in architectural leverage unless such a return mechanism is found.

## 7. H: strong finite-cycle and structured-language barriers

PR #19 advanced after the first pass-3 cutoff.

### Proposed cycle exclusion

Using an exact minimum-state lemma, the exhaustive ordinary sweep below `3*2^65`, and a continued-fraction certificate, proposed `T-9515` excludes every positive exact H block cycle with

```text
L<=2,479,700,524.
```

This is a finite-period theorem, not termination of H.

### Proposed structured nonperiodic barriers

PR #19 also proves at proposed level:

- a factor-entropy lower bound for finite-alphabet survivors with logarithmic capital;
- zero-entropy finite-alphabet codes cannot survive with logarithmic capital;
- an exact prefix-return versus capital inequality;
- for alphabet `{2,3}`, every certified prefix-return constant `C<=84` is excluded.

A viable structured H counterexample must now combine:

```text
delayed symbolic novelty or positive entropy,
sufficient capital growth,
infinitely many fresh bridge primes,
eventual ordinary carry stabilization,
and a positive all-time initialization.
```

The alternative is still an unbounded reset-renewal ray with critical/supercritical endpoint product.

## 8. Sanctuary and equivalent witnesses

PR #42 proposes that no nonempty forward-invariant union of complete residue classes can avoid the trivial cycle. Thus a sanctuary cannot be a bare modulus set.

A regular sanctuary remains a direct finite certificate only if its DFA stores genuine canonical-word boundary information.

Coverage deficit, solution-cone third ray, and spectral routes remain exact equivalent witnesses, but no current computation extracts the required third component.

## 9. Corrected priorities

### By logical distance to a disproof

1. **Full-denominator positive cycle certificate** — finite equality and replay.
2. **Regular sanctuary DFA** — finite closure certificate.
3. **Centered height-augmented forced-tail seed** — one seed plus one-counter induction.
4. **Linear-height quotient-refund invariant** — one ordinary expanding invariant.
5. **Multi-phase cross-cycle return/invariant** — must escape the phase-1 secret reduction.
6. **Positive infinite H ray** — strong cycle and low-complexity barriers now apply.
7. **Equivalent third-component witness** — extraction theorem still missing.

### By present architectural research leverage

1. **Linear-height quotient refund** — clearest exact escape from frozen-class extinction.
2. **Critical-scale mechanical cycle compiler** — already operates at the correct global scale.
3. **Critical-scale distributed-pulse/cross-prime circuit** — valid finite grammar, not yet implemented at scale.
4. **Centered one-counter top-boundary invariant** — exact positive monotone dynamics, existence open.
5. **H delayed-novelty/reset-renewal frontier**.
6. **Cross-cycle repeated-return architecture**.

## Bottom line

The corrected pass does not produce an unconditional counterexample.

It does identify the full-objective frontier more accurately:

- one important fixed architecture is independently excluded;
- residue-only PDR and permanent phase-1 handoffs are secret reductions;
- small cycle families and low-complexity grammars are increasingly closed;
- serious cycle work must control the full denominator at critical scale;
- serious divergent-orbit work must causally control one unbounded ordinary resource and its top boundary.

The two highest-value positive programs are therefore:

```text
a full-denominator critical-scale cycle circuit,
or
a causal linear-height quotient-refund invariant.
```
