# UNVERIFIED register — wave 8

These items are deliberately not promoted to sourced theorems.

## U8-01 — normality of every rational-base minimal word

**Status:** `CONJECTURAL`.

Andrieu–Eliahou–Vivion conjecture that every nonzero minimal word in base `P/Q` is normal over `{0,...,Q-1}`. At `(P,Q)=(3^12,2^19)` this would immediately exclude the six-branch chart's digit confinement. No proof is known in the located source.

## U8-02 — occurrence of a digit outside every proper subset

**Status:** `UNVERIFIED / weaker-than-normality target`.

It would suffice to prove that the minimal word of every positive root in base `3^12/2^19` eventually uses a digit outside the six-element physical set. I located no theorem establishing even this restricted digit-escape statement.

## U8-03 — direct automaton-state lower bound from Dubickas complexity

**Status:** `UNVERIFIED`.

Dubickas gives the factor-complexity lower coefficient. Turning it into a sharp minimum number of states for a particular automatic/transducer controller requires a separately proved controller-specific complexity upper bound.

## U8-04 — Ostrowski compiler closes PR #50

**Status:** `METHOD PROPOSAL ONLY`.

Bugeaud–Reutenauer parametrize Christoffel conjugates by integer Ostrowski numeration. A definition-level mapping to every PR #50 sibling-swap or repair choice has not been proved.

## U8-05 — PR #53 repetition equation falls under de Weger

**Status:** `UNVERIFIED`.

The pulse-size coordinates are capped, but the remaining repetition parameter has not yet been reduced to one fixed S-unit equation or inequality satisfying de Weger's exact hypotheses.

## U8-06 — BCZ gcd theorem bounds repetition

**Status:** `UNVERIFIED`.

A native relation must first force a large gcd of two multiplicatively independent exponential sequences in the precise form covered by Bugeaud–Corvaja–Zannier.

## U8-07 — variable-coefficient S-unit theorem applies to PR #53

**Status:** `UNVERIFIED`.

Corvaja–Zannier's surface results include variable-coefficient S-unit equations, but the native parameter space, nondegeneracy, and exceptional curves have not been mapped.

## U8-08 — full Dubickas `(81,64)` constant and equality language

**Status:** `UNVERIFIED / HIGH PRIORITY`.

The complete 2006/2008 source formulas have not yet been acquired in this pass. No numerical normalization or Thue–Morse equality classification is imported without the full source.

## U8-09 — path-complete certificates settle H arithmetic

**Status:** `METHOD PROPOSAL ONLY`.

The switched-system literature handles automaton-constrained growth/stability. H has affine intercepts, integer divisibility, and changing arithmetic states; these must be included in any native certificate.

## U8-10 — Väänänen–Wallisser applies beyond frozen periodic vectors

**Status:** `UNVERIFIED`.

The supplied paper proves a finite-dimensional linear-independence measure under explicit orbit and numerical hypotheses. No application to adaptive rational-base minimal words or unbounded transported stacks is claimed.
