# Wave 8 topic note — centered-power source specialization and H dual certificates

## Part A — centered powers at `81/64`

The native equivalence is exact:

\[
 \exists\xi>0:\left\|\xi(81/64)^n\right\|\le1/81
 \quad\forall n
\]

if and only if the centered binary subsystem has a nontrivial ordinary survivor.

### Literature task

Acquire the complete Dubickas sources and freeze:

1. the exact large-limit constant for `(p,q)=(81,64)`;
2. endpoint conventions;
3. the extremal or near-extremal symbolic word;
4. any two-interval exclusion that matches the centered arcs;
5. the translation from the source sign word to native nearest-integer carries and appended blocks.

### Decision tree

```text
source constant > 1/81:
  ordinary section empty;

source constant = 1/81:
  classify equality words and apply native recurrence/morphic filters;

source constant < 1/81:
  quantify the exact deficit and seek a native arithmetic gain.
```

The branch's Thue–Morse, morphic, and small-transducer exclusions make the equality language a realistic target rather than an afterthought.

## Part B — H as automaton-constrained affine switching

For one frozen finite H macro grammar, attach to each edge

```text
positive multiplier a_e,
positive affine toll b_e,
exact residue/valuation legality.
```

### Primal certificate

Search for state-dependent piecewise-linear or path-complete potentials satisfying an exact decrease outside a finite trap.

### Dual certificate

When the primal fails, extract an exact rational occupation measure or high-growth cycle from the dual. Replay that object in the actual H arithmetic rather than treating it as a numerical instability witness.

### Hierarchy

```text
maximum cycle mean on log multipliers
 -> state-dependent affine potential
 -> path-complete piecewise-linear potential
 -> occupation-measure dual
 -> exact congruence refinement
 -> ordinary reset/renewal replay.
```

The scalar multiplier layer is exact. Affine intercepts and integer legality require the augmented native state; the switched-system literature does not remove that obligation.
