# Claim map — PR #3, wave 2 additions

Namespace: `PR3/...`. Verdicts concern literature matching only.

| Claim | Literature verdict | Located support / boundary |
|---|---|---|
| `PR3/L-0007` offset tensor law | `FOLKLORE / STANDARD` algebra after the native code definitions | Minkowski-sum composition is elementary; the exact inverse-root interpretation is native |
| `PR3/L-0008` arbitrary 3-adic precision | `KNOWN — COROLLARY` | lifting-the-exponent supplies the valuation; the chosen atomic code is native |
| `PR3/T-0006` geometry-preserving amplification | `POSSIBLY NOVEL FORMULATION` | no exact external antecedent located; finite Minkowski preservation is elementary, Collatz-code realization is native |
| `PR3/T-0007` complete dyadic projection | `POSSIBLY NOVEL FORMULATION` | no exact antecedent located; do not infer finite-boundary regeneration from modular projection |
| `PR3/T-0008` negative-shadow rational-base formulation | `PARTIAL OVERLAP` | rational-base numeration is nearby; the exact negative-template return identity follows natively from parity-affine cylinders |
| `PR3/L-0012` inverse signatures are negative targets | `FOLKLORE / STANDARD` once definitions are frozen | direct rearrangement of the affine formula |
| `PR3/T-0009` renewal-code criterion | `PARTIAL OVERLAP` | `LIT-KTHM-0018` supplies the clopen-cover obstruction; the exact Collatz selector criterion is native |
| `PR3/T-0010` fractional-window/aspect-ratio law | `KNOWN — COROLLARY` | `LIT-KTHM-0016` gives the complete affine-radix proof |
| `PR3/T-0013` graph-directed return / positive cycle mean | `PARTIAL OVERLAP` | phase-potential portion is standard (`LIT-KTHM-0017`, Karp/difference constraints); exact negative Collatz edges and survivor realization are native |

## Most useful literature connection

Rational-base and graph-directed literatures provide the correct language for addresses, compact completion objects, cycle potentials, and real windows. They do **not** solve the branch's central requirement: a deterministic dyadic-cylinder selector whose survivor set contains one ordinary positive quotient.

## Review priority

1. Independently reconstruct the exact negative-shadow edge identity.
2. Check the one-target clopen argument with `LIT-KTHM-0018`.
3. Treat `T-0013` as two claims: standard phase-potential infrastructure and native arithmetic coverage.
4. Audit every graph attractor argument against `LIT-KTHM-0027`: compact completion is not ordinary-integer realization.