# Claim map — issues #8 and #9

These issues remain IDEA-level programs. The imported results below sharpen their first falsifiable experiments; they do not promote either direction.

## Issue #8 — conditioned `3`-adic rare-event resonances

| Component | Verdict | Guidance |
|---|---|---|
| affine correction recurrence | `FOLKLORE / STANDARD` | use `LIT-KTHM-0015`; direct small-depth trajectory checks remain mandatory |
| finite-state conditioned transfer operator | `KNOWN — COROLLARY` | use the tilted matrix identity in `LIT-KTHM-0026` |
| persistent Fourier mode under joint depth/modulus growth | `OPEN NATIVE TARGET` | one truncation's eigenvalue or spectral gap does not give a uniform theorem |
| nested residue classes | `STANDARD p-ADIC COMPATIBILITY` | require least-positive-representative stabilization to obtain one ordinary integer |
| connection to Tao | `PARTIAL OVERLAP` | Tao's `3`-adic mixing and Fourier ideas concern an almost-all theorem, not a conditioned explicit exceptional orbit |

**Recommended experiment:** freeze one finite state space and one normalized tilted operator; verify it by exhaustive small-depth enumeration; calculate leading eigenvalues with exact or interval-certified arithmetic; then state precisely how constants depend on modulus before increasing the truncation.

## Issue #9 — compressed positive cycles

| Component | Verdict | Guidance |
|---|---|---|
| affine monoid | `FOLKLORE / STANDARD` | `LIT-KTHM-0015` |
| exact cycle equation | `KNOWN — EXACT` | `LIT-KTHM-0023`; same classical substrate as Eliahou and Simons–de Weger |
| cycle lower-bound frontier | `KNOWN — EXTERNAL`, parameter-sensitive | Hercher's `m` counts local minima; do not replace it with accelerated length |
| reverse cellular-automaton encoding | `PARTIAL OVERLAP` | Sterin–Woods offers a neighboring representation, not a positive-cycle construction |
| proof-producing compressed solver | `OPEN NATIVE TARGET` | every output needs exact expansion/replay and a declared grammar frontier |

**Recommended certificate:** compressed valuation word; expanded local-minimum count; `(k,A,C)` summary; factor/residue evidence for `2^A-3^k`; reconstructed positive integer; exact intermediate valuations; return equality; tiny independent verifier.

## Coordination with PR #11

Issue #9 and PR #11's cycle direction should use one affine summary and one verifier. Distinguish grammar families and parameter ranges instead of maintaining two partially overlapping bounded censuses.