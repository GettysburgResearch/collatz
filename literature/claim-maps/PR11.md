# Claim map — PR #11 affine ping-pong / cycle / fuel packet

Namespace: `PING/...`. Literature matching does not promote native statuses.

| Claim/path | Verdict | Audit |
|---|---|---|
| `PING/T-0101` precision accounting | `FOLKLORE / STANDARD` components | exact native accounting must be checked against its definitions |
| `PING/T-0102` digit-transfer Schottky obstruction | `PARTIAL OVERLAP` | classical ping-pong/Schottky lemmas concern separated domains; dyadic integrality and Collatz cylinders are native |
| `PING/T-0103` classical Schottky meta-obstruction | `PARTIAL OVERLAP` | acceptable only for the explicitly defined certificate class; not an obstruction to all symbolic counterexamples |
| `PING/T-0104` atomic tensor radius freeze | `REFUTED AS STATED` | cancelled by the explicit near-scale seed in `LIT-KTHM-0024` |
| `PING/T-0105` general tensor radius freeze | `REFUTED AS STATED` | same counterexample; arbitrary suffix alphabets do not prevent cancellation with seed differences |
| corrected separation-qualified radius theorem | `FOLKLORE / STANDARD` | complete proof in `LIT-KTHM-0024`; the separation invariant must hold at every stage |
| cycle MITM / multiblock searches | `INTERNAL EXACT COMPUTATION` | coordinate with issue #9; bounded emptiness is only a declared frontier |
| heteroclinic negative-template work | `POSSIBLY NOVEL FORMULATION` | rational-cycle and `p`-adic shadow literature are nearby; exact grow/keep templates are native |
| valuation-fuel tradeoff | `IDEA / EMPIRICAL` | the reusable requirement is a compositional fuel state and a regenerative cycle, not isolated favorable episodes |

## Required correction before merge

The packet and theorem files must no longer say the tensor-radius paths are closed. At minimum:

1. mark `T-0104` and `T-0105` `REFUTED` or replace them with the separation-qualified theorem;
2. add the counterexample as a regression;
3. identify every downstream conclusion that used unconditional radius freeze;
4. prove the separation condition stage-by-stage before reusing a freeze conclusion.

## Literature-guided next move

Unify the cycle direction with issue #9's affine monoid and exact replay format. For fuel, define a finite or compressed state carrying current scale, exact `2`-adic depth, and future residue obligations. A claimed regenerative loop should replay as an exact integer cycle in that augmented state.