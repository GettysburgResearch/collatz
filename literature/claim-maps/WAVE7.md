# Claim map — wave 7

| Wave-7 item | Native targets | Relationship | Boundary |
|---|---|---|---|
| `LIT-KTHM-0048` rational-base ordinary marker | `ADEL/L-9313`, `ADEL/L-9314`, `PR3/T-0002`, `PR35/T-8806`, issue #40 | canonical finite-integer and p-adic representation infrastructure; native alphabets remain restricted or translated | no ordinary survivor existence |
| `LIT-KTHM-0049` rational-base tree state | PR #12, PR #14, PR #35, issue #40, collision charts | all rooted subtrees differ; exact successor uses an infinite-state transducer | finite-state boundary only |
| `LIT-KTHM-0050` linear-height quotient refund | `PR3/L-0031`, `PR33/T-9705`, `SYN/T-9831`, PR #38 `ACL-P014` | schedule change reverses the next-radix inequality and gives infinitely many positive finite quotient transitions | no coherent infinite integer path yet |
| `LIT-KTHM-0051` `(8,13)` prime sieve | issue #9, issue #41, `SYN/L-9904` | prime 233 excludes all 792 positive valuation compositions, or 99 primitive necklaces | exact finite verifier packet below the external cycle frontier |
| `LIT-KTHM-0052` `(184,292)` frontier family | issue #9, issue #41, `SYN/L-9904` | minimal accelerated length compatible with 92 local minima; fully factored 290-bit denominator; exact 92-block recurrence with sixteen excess units | search family only; no divisibility hit |

## Live branch changes

### PR #33

The proposed `T-9705` closes the frozen corrected doubling-scale class by quotient cap/co-cap reduction followed by an almost-S-unit theorem. Wave 7 recommends no further word search inside that exact class. `LIT-KTHM-0050` changes the schedule and breaks the first quotient-extinction hypothesis.

### PR #34

The newest packet proposes:

- a maximal finite-type bounded-width Evertse closure (`T-9831`);
- a bounded-essential-leaf extension even when nominal width grows (`T-9834`);
- primitive-necklace cycle normalization (`L-9904`);
- faithful atomic spectral support with no nuclear implementation (`L-9905`); and
- an adjacent Schur-core gcd target for period ten (`T-9835`).

Constructive designs should now state exactly which Evertse hypothesis they violate.

### PR #37 and issue #40

The centered recurrence and appended-block chain are independently verified at the frozen source. The unrestricted factor-complexity statement was refuted by the constant words and repaired for nonconstant itineraries. The next positive object is an ordinary invariant for the verified forced-tail map retaining its unbounded exact root state.

### Issue #39

The first exact phase-34 room opening crosses into another negative-cycle architecture. The live target is a finite return or an all-time cross-cycle resource invariant.

### Issues #9 and #41

Positive cycle synthesis is active and remains the shortest finite route. Use `LIT-KTHM-0051` as a modular verifier control. `LIT-KTHM-0052` supplies the first serious family at the published local-minimum frontier:

```text
k=184,
A=292,
92 alternating local minima,
2^292-3^184 fully factored into 19 primes of at most 48 bits.
```

## Priority order

```text
finite positive cycle
cross-cycle ordinary spine
linear-height quotient refund
centered forced-tail ordinary invariant
H reset-renewal endpoint-product escape
equivalent nonconstructive witnesses
```

No item in this map claims a positive Collatz witness.