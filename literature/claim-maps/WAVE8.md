# Claim map — wave 8

All native IDs remain branch-qualified. No status promotion is performed here.

| Native object | Literature / imported result | Mapping verdict | Main remaining obligation |
|---|---|---|---|
| `PR45/L-8407` six-branch chart | `LIT-KTHM-0053`; rational-base representation trees; Dubickas 2009 | **EXACT DEFINITION-LEVEL IDENTIFICATION** after `x=h/3` | Find one positive root whose minimal word stays in the six-digit set forever |
| `PR50/T-8305` run-highway conjugacy | `LIT-KTHM-0053` | **EXACT COROLLARY** | Preserve physical conjugacy and ordinary root in any solver |
| `PR50/L-8309` constant-slope gauge | integral ceiling map `x -> ceil(3^12 x/2^19)` | **SAME SLOPE / STRONGER CANONICAL FORM** | Use canonical minimal digit rather than externally prescribing type |
| `PR50/T-8307` eventually periodic tail exclusion | Dubickas nonperiodicity; rational-base minimal words | **KNOWN COROLLARY / native proof retained** | None for the declared periodic class |
| `PR48/L-8210` transported stack | `LIT-KTHM-0054` | **NONSTATIONARY GENERALIZATION** | Prove eventual-zero transported digits for one ordinary physical path |
| `PR49/T-8512` size depth | `LIT-KTHM-0054`; `PR48/R-8203` | **NUMERICAL CAPACITY RETAINED; RAW DIGIT CROSSWALK NARROWED** | Use inverse-affine transported residues, not plain Euclidean digits |
| `PR50/L-8308` Farey grammar | Bugeaud–Reutenauer 2025 | **METHOD NEIGHBOR** | Map the exact grammar/rotations to integer Ostrowski parameters |
| `PR50/Q-8301` mixed-place height closure | product formula / exact finite replay | **NATIVE FINITE CERTIFICATE** | Cross the full dyadic + odd-prime + real height budget |
| `PR53/L-8201` pulse resultants | sparse resultants; de Weger; BCZ; variable S-units | **NATIVE RESULT + STRATEGIC EFFECTIVE BRIDGE** | Reduce repetition length to one source-compatible equation |
| `ADEL/T-9315..T-9317` centered powers | Dubickas rational-power limit points and two-interval theorems | **HIGH-PRIORITY SOURCE SPECIALIZATION** | Acquire exact formula and map extremal word to appended blocks |
| `H/L-9526,T-9516,T-9517` | path-complete Lyapunov functions; dual occupation measures | **METHOD BRIDGE** | Freeze finite affine macro grammar and preserve exact arithmetic legality |
| low-period prescribed controllers | Väänänen–Wallisser 1991 | **KNOWN FIREWALL** | Check exact numerical/orbit hypotheses for each family |

## Cross-program synthesis

The principal live constructive object is now the canonical rational-base ceiling orbit

```text
x_(n+1)=ceil(3^12 x_n/2^19)
```

with its minimal digit restricted to six values. This object simultaneously packages:

- the PR #45/PR #50 physical chart;
- the PR #48 transported-cylinder ordinary-marker criterion;
- the complexity packet's need for high-complexity schedules;
- the rational-base tree's requirement for unbounded exact root state.

A positive solution is already an ordinary integer-first counterexample. A compatible `2`-adic digit word without eventual stabilization is not.
