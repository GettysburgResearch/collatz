# UNVERIFIED — literature audit wave 4

These are useful, explicit gaps. None may be silently converted into a citation or theorem application.

## U4-01 — Väänänen--Wallisser 1991 exact hypotheses

**Known:** the paper proves a linear-independence measure for values of the exact p-adic Tschakaloff function with rational `q` and `0<|q|_p<1`.

**Unknown:** number of allowed points, restrictions on point ratios, inclusion of `1`, global height hypotheses, and the exact measure.

**Consequence if verified:** potentially closes every fixed periodic stack tail in PR #20.

## U4-02 — Dubickas large-limit constant at `(81,64)`

**Known:** the 2006 paper gives explicit large and small limit-point constants for `||xi(p/q)^n||`.

**Unknown:** the exact value of the universal large-limit constant for `p=81,q=64`.

**Consequence if greater than `1/81`:** PR #16 `T-9315` would imply the nonexistence of a nontrivial ordinary survivor.

## U4-03 — Dubickas two-interval specialization

**Known:** the 2008 paper excludes certain unions of two intervals for rational-power fractional parts.

**Unknown:** whether its general inequalities exclude

```text
[0,1/81] union [80/81,1)
```

for ratio `81/64`.

## U4-04 — symbolic maximal-minor saving for period four

**Known:** PR #20's current period-four construction misses exponent one by a small explicit margin. Bombieri--Vaaler and cyclotomic determinant methods can reduce height through common factors of maximal minors.

**Unknown:** whether the exact period-four symbolic coefficient matrix has a common factor of sufficient asymptotic degree.

## U4-05 — uniformity in the period word

Even if every fixed period is irrational, no inspected result yet gives constants uniform enough in the period length and word to pass to the balanced standard-word limit.

## U4-06 — atomlessness of the PR #34 decoder measure

DWW purification is available only after the exact measure in the Hall application is proved atomless, and only for finitely many static integral constraints.

## U4-07 — section finiteness of actual correction maps

The general finite-section criterion is known. No proof currently decides whether the actual PR #3/PR #34 address and correction isometries have finitely many sections.

## U4-08 — all-dyadic q-Lucas endpoint invariant

The four-step q-Pascal operator is exact. The proposed extension to every dyadic block requires an invariant controlling endpoint-state cancellation; no such invariant is yet proved.

## U4-09 — stationary-SFT normalization of PR #3

Krieger/MacDonald cannot be applied until the scale-dependent connector system is converted into a stationary mixing SFT and all periodic-point/visible-output hypotheses are checked.

## U4-10 — status integration for PR #32

The adversarial review independently reconstructed the ADEL chain at a frozen commit. The native claim ledger and project review registry have not yet completed the formal status update.

## U4-11 — path-complete potential for expansion-positive stages

The state-dependent trap theorem supplies a proof format, not a certificate for the current PR #3 normalized stage relation. A failed solver run is not a mathematical obstruction without an exact dual cycle/flow.

## U4-12 — direct link from static purification to ordinary integers

Even a successful deterministic measurable selector would not provide temporal coherence, one finite integer, or compatibility over all moduli.