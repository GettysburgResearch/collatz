# Live repository review — literature and counterexample pass 7

**Agent:** `gpt56-pro-03`  
**Date:** 2026-07-22  
**Status:** strategic/literature synthesis plus two exact native lemmas; no native status promotion and no counterexample claim

## 1. Purpose of this pass

The portfolio has accumulated strong exclusion and completion-height theorems. This pass deliberately asks a different question:

> Which current route can still produce a full unconditional counterexample, and what exact architecture must replace the classes now proposed excluded?

The answer is no longer “search harder inside the frozen corrected stage.” The most developed finite-type architecture now has a proposed uniform ordinary-exclusion theorem, while the cross-direction forge proposes a nearly maximal Evertse closure for bounded essential width.

The constructive portfolio must therefore focus on proof objects that are finite and ordinary from the beginning, or on architectures designed to violate a named load-bearing hypothesis of the new closure theorems.

## 2. Latest high-signal repository changes

### 2.1 The frozen corrected 256-stage class is proposed closed

`PR33/T-9705` claims that every infinite physically overlapping directive in the frozen doubling-scale corrected class selects a nonordinary `2`-adic point. Its proof first traps every signed ordinary quotient in the cap/co-cap values `0` or `-1`, then converts the path into nondegenerate 258-term almost-`{2,3}`-unit zero sums whose primitive endpoint mass has exponent below `1/50`.

If independent review passes, further searches inside that exact schedule should stop. The valid constructive question becomes:

```text
Which explicit schedule or architecture breaks
cap/co-cap quotient extinction or the subunit endpoint gate?                 (1)
```

### 2.2 The Evertse boundary has become abstract

`SYN/T-9831` proposes a reusable exclusion for every finite type catalogue with:

```text
bounded essential coordinate count,
finite internal prime union,
nondegenerate primitive zero sums,
primitive endpoint outside-prime exponent Xi<1,
infinitely many primitive points.                                            (2)
```

`SYN/T-9834` further warns that merely increasing nominal equation width is not enough if the relation splits into uniformly bounded nondegenerate leaves carrying the same subunit endpoint gate.

A successful new construction must intentionally use at least one of:

```text
unbounded essential leaf width;
primitive endpoint exponent Xi>=1;
an essential changing prime alphabet;
persistent degeneracy not reducible to bounded leaves;
quotient refund;
architecture-changing phase transitions.                                    (3)
```

### 2.3 The centered recurrence is now a verified positive platform

PR #37 independently reconstructs the exact centered equivalence, nearest-integer cylinders, appended block, and nonconstant recurrence barriers. Once a block becomes zero, future continuation is a deterministic partial integer map on the unbounded state `(C_K,e_K)`.

This validates issue #40's proof-carrying modular/PDR lane. The central safety warning is exact: a modular lasso is not an ordinary orbit unless its carry and high integer state close.

### 2.4 Three direct constructive lanes are active

- **Issue #39:** the first audited phase-34 zero cell at scale 22 exits the frozen architecture and enters a cross-cycle ordinary-spine handoff. The easiest phase-1 continuation loses bits in the finite audit; the live target is a new cross-cycle resource or finite return.
- **Issue #40:** centered forced-tail PDR can either construct one ordinary survivor or prove a quantified nonstabilization invariant.
- **Issues #9 and #41:** positive cycles remain the shortest logical route. The latter explicitly moves beyond the local-minimum frontier with smooth-denominator and CRT synthesis.

These should receive priority over another broad family of completion-only constructions.

## 3. Exact constructive result of this pass — linear-height quotient refund

`LIT-KTHM-0050` changes the phase-34 padding schedule from doubling scales to the linear grid

```text
t_j=B+16j,
0<=j<=256.                                                                 (4)
```

One stage has exact exponents

```text
A(B)=1792B+3657472,
E(B)=2816B+5792512.                                                         (5)
```

The next stage begins at `B+4096`. For every multiple of 16 with `B>=477424`,

```text
boxed:
3^[A(B)] > 2^[E(B+4096)].                                                    (6)
```

Thus the homogeneous output of one free quotient contains more than one full next-stage radix. For every ordered pair of finite stage words, the next-cylinder congruence has one quotient residue modulo the next radix and infinitely many positive ordinary lifts; along those lifts the next quotient grows asymptotically by the factor in `(6)`.

This is an exact `signed quotient refund` architecture and lies outside `PR33/T-9705`'s frozen doubling schedule. It does not yet provide one coherent infinite integer path.

### Required positive theorem

Construct a family of ordinary quotient states `K_B` and an explicit word-selection/lift rule satisfying

```text
(B,Y) in K_B
 -> (B+4096,Y_next) in K_(B+4096),
Y_next>Y,                                                                  (7)
```

with one finite positive initial state. The rule must choose the ordinary lift parameter at each stage from the current finite state, not from a precomputed inverse-limit word.

A proof of `(7)`, combined with the existing physical tower identity, would give an unconditional unbounded Collatz orbit.

## 4. Rational-base literature changes how to build the invariant

`LIT-KTHM-0048` imports the least-significant rational-base division theorem: ordinary nonnegative integers are exactly finite canonical representations. Frougny–Klouda provide the p-adic representation and eventual-periodicity layer without identifying different completion limits.

`LIT-KTHM-0049` imports a more sobering and useful fact: the canonical integer representation tree is highly nonregular, every rooted subtree is different, and the successor of bottom words requires an infinite transducer whose state graph is essentially the tree itself.

Therefore:

1. finite compatible digits do not give an ordinary marker;
2. residue-only PDR can merge states with inequivalent exact futures;
3. a successful positive machine should retain one unbounded quotient/carry coordinate on purpose;
4. the right target is not generic finite state, but a **finite nucleus plus one proved exact unbounded state**.

This directly supports the linear-height refund lane and issue #40.

## 5. Direct cycle packet completed and correctly scoped

`LIT-KTHM-0051` exhausts the complete accelerated packet

```text
k=8,
A=13,
D=2^13-3^8=7*233.                                                          (8)
```

A dynamic program over all 792 positive compositions proves that the numerator residues modulo 233 are every residue except `0` and `138`. Hence 233 alone excludes the whole packet.

This is a strong verifier regression, not a frontier theorem: Hercher proves that a nontrivial positive cycle requires at least 92 local minima, while an eight-letter valuation word has at most eight. Active cycle work should use this packet as a correctness gate and move immediately to the declared beyond-frontier families.

## 6. Best current path toward an unconditional counterexample

### Priority A — finite positive cycle

A successful exact cycle certificate ends the problem without any ordinary-completion theorem. The current best architecture is:

```text
primitive necklace normalization
 -> choose critical (k,A) beyond the local-minimum frontier
 -> prioritize a smooth or partially smooth denominator 2^A-3^k
 -> solve numerator divisibility prime by prime
 -> replay every exact valuation and return.                                  (9)
```

The new `233` packet demonstrates the desired proof-producing modular style. It also shows that one prime can kill an entire family before expensive replay.

### Priority B — cross-cycle ordinary spine

Issue #39 is the only current construction beginning from a concrete positive ordinary room opening that demonstrably leaves the class proposed closed by PR #33. Its next theorem should be either:

```text
one finite return equation closing a positive cycle;
```

or

```text
a cross-cycle potential proving a sequence of ordinary handoffs has
strictly positive net resource forever.                                      (10)
```

Long finite renewal prefixes without `(10)` should not be accumulated as if they were convergence evidence.

### Priority C — linear-height quotient refund

Theorem `(6)` supplies a second explicit escape from quotient extinction. The next agent should search for an invariant on

```text
(stage prefix,
quotient residue,
ordinary quotient magnitude,
endpoint outside-prime content,
current physical type).                                                       (11)
```

A finite automaton alone is unlikely to suffice; rational-base tree theory predicts a genuine unbounded state.

### Priority D — centered forced-tail construction

Issue #40 has the best verified exact recurrence. A positive result requires one explicit `B_0` and an inductive invariant proving the forced partial map never fails. The abstraction must preserve either the exact quotient or a ranking/refund coordinate strong enough to lift every modular step.

### Priority E — H reset-renewal escape

The H branch now has explicit primitive four-term equations. A survivor must keep its primitive endpoint product critical or supercritical for every `d<1`, or use an essential changing prime alphabet. The positive target should be stated as a reset-renewal ray with a proved lower endpoint-product exponent, not merely infinitely many fresh primes.

## 7. Important negative prioritization

The all-fixed-period q-series program remains mathematically valuable, but it is an exclusion/filter route. Even a theorem proving every fixed period irrational does not construct a counterexample. The remaining period-ten target is now an adjacent Schur-core cubic-gcd problem, not the shortest path to a positive witness.

Similarly:

- weighted EQ is now independently verified at its stated finite-depth level, but does not select an exceptional integer;
- spectral support extraction is exact only for the nonnuclear atomic quotient; a Fredholm route still needs a support-preserving intertwiner;
- rational-base normality is conjectural;
- thinness, Hausdorff dimension, and deep finite minima do not prove integer existence or emptiness.

## 8. Recommended agent restart order

```text
1. issues #9/#41: exact beyond-frontier cycle synthesis;
2. issue #39: cross-cycle finite return or resource invariant;
3. new quotient-refund lane from LIT-KTHM-0050;
4. issue #40: proof-carrying centered forced-tail PDR;
5. H: critical endpoint-product construction or exclusion;
6. fixed-period block determinant only after the direct lanes are staffed.
```

## 9. Status boundary

No theorem in this review supplies a positive Collatz counterexample. The linear-height result proves an exact escape from one load-bearing negative architecture; the cycle result is a finite exclusion; the rational-base sources provide correct state models and warnings. The full positive target remains one explicit ordinary initialization with an all-time proof.