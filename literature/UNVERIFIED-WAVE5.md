# Unverified register — literature audit wave 5

This file records exact gaps after the attached-paper audit. None may be silently upgraded to a theorem application.

## U5-01 — Väänänen–Wallisser beyond period nine

**Verified:** the full theorem applies to the PR #20 periodic phase vector through minimal period nine.

**Not verified / false as a direct claim:** the source theorem's numerical hypothesis does not hold at dimension ten for the current parameter. It therefore does not prove irrationality for every fixed period.

**Required next evidence:** a different theorem, a stronger determinant/height calculation, or a source-independent native proof.

## U5-02 — Uniformity in growing period

A qualitative theorem for each fixed period would still not automatically prove irrationality of the balanced nonperiodic `17/18` directive.

**Required:** constants uniform enough in period length to compare adjacent standard-word periodic approximants with the S-adic limit.

## U5-03 — Fixed-word S-unit reduction for cap stitches

No repository file yet expresses

```text
S_m(w)=R_(m+1)(w')
```

for one fixed ordered word pair as one scale-independent finite-term equation in a fixed finite-rank multiplicative group.

**Required:** exact symbolic expansion, coefficient stability, distinctness of scale solutions, and complete proper-subsum classification.

## U5-04 — H successive-core S-unit reduction

The equation in `SYN/L-9894` has the visual form of a `{2,3}`-unit equation, but the variables `X,Y` may contain uncontrolled fresh primes.

**Required:** persistent/fresh prime decomposition, finite-rank persistent group, and proof that fresh mass is bounded or contradictory.

## U5-05 — Explicit p-adic logarithmic-form application

Chim 2025 and Bugeaud 2007 are relevant in scope, but no current native file freezes all required algebraic numbers, exponents, multiplicative-independence hypotheses, and constants for the H or cap-stitch problems.

**Required:** one exact source-to-native parameter table and numerical inequality.

## U5-06 — Dubickas specialization at `(81,64)`

The full explicit 2006 nearest-integer constant and the 2008 two-interval inequalities have not been evaluated at the repository's critical radius `1/81`.

**Required:** full-source acquisition, exact symbolic specialization, rigorous comparison, and translation of any extremal Thue–Morse word to the native nearest-integer block recurrence.

## U5-07 — Latest PR #34 theorem wave

The claims `L-9868` through `L-9894` have same-session audits but no repository-independent reconstruction as a complete packet.

**Required:** prioritize the load-bearing claims after the period-nine retarget rather than independently reconstructing every exploratory lemma indiscriminately.

## U5-08 — PR #3 / PR #33 stage extinction chain

The new cap bound, quotient extinction, composite-cap equivalence, and cap-height ratio are complete-looking but not independently reconstructed.

**Required:** freeze exact heads and review `PR3/L-0030`, `PR3/T-0031`, `CYL/L-9702`, `CYL/T-9703`, `CYL/L-9703`, and `CYL/T-9704` in dependency order.

## U5-09 — H ordinary-section minimum

The equivalence between H termination and `nu_K -> infinity` is proposed and the finite checkpoint is exact only at bounded precision.

**Required:** independent boundary audit plus an asymptotic theorem; finite monotone growth is not enough.

## U5-10 — Ordinary Collatz conclusion

No result in wave 5 supplies one positive ordinary seed with an infinite certified future, nor proves that all possible Collatz counterexamples are excluded.
