# Unverified and open interfaces — wave 7

## U7-1 — exact conjugacy to canonical rational-base systems

The centered, collision, and phase-34 recurrences are rational-base-like, but their digit alphabets are translated, signed, state-dependent, or nonstationary. A definition-level conjugacy to a specific Akiyama–Frougny–Sakarovitch convention has not been proved uniformly.

**Status:** `UNVERIFIED — application-specific`.

## U7-2 — linear-height physical architecture

`LIT-KTHM-0050` follows algebraically from the arbitrary finite-chain scaled-tail recurrence in `PR3/L-0031`. Independent review must still reconstruct:

- the linear height grid within the permitted stabilized tower domain;
- the full stage exponent sums;
- the next-stage radix comparison;
- the canonical input/output quotient formula; and
- the distinction from the frozen doubling-scale architecture.

**Status:** `PROPOSED native theorem; no infinite path`.

## U7-3 — quotient-state invariant

No finite rule currently chooses the quotient lift parameter in `LIT-KTHM-0050` for all stages from one ordinary initialization. Pairwise positive transitions do not supply a coherent infinite path.

**Status:** `OPEN — highest-priority constructive interface`.

## U7-4 — centered forced-tail abstraction completeness

Issue #40's modular/PDR abstraction must preserve enough carry or completion-height state that every abstract transition lifts to the same ordinary integer orbit. The rational-base subtree theorem suggests residue-only completeness is unlikely.

**Status:** `OPEN`.

## U7-5 — issue #39 cross-cycle continuation

The scale-22 room opening and finite handoff data have not been independently inspected by this literature pass. No all-time cross-cycle potential or finite positive return has been supplied.

**Status:** `UNVERIFIED native finite packet / positive continuation open`.

## U7-6 — cycle frontier parameter mapping

Hercher's theorem uses local-minimum count. Active compressed-word searches must compute this count from the valuation word rather than identify it with accelerated length. The exact mapping for each proposed grammar remains a native obligation.

**Status:** `OPEN per candidate family`.

## U7-7 — rational-base normality

The 2025 normality statement for minimal and maximal rational-base words is conjectural. It cannot be used as an exclusion theorem.

**Status:** `CONJECTURAL — diagnostic only`.

## U7-8 — maximality of the Evertse architecture

`PR33/T-9705`, `SYN/T-9831`, and `SYN/T-9834` remain proposed pending repository-independent reconstruction and source matching. Wave 7 treats them as strategic boundaries, not promoted facts.

**Status:** `PROPOSED / review pending`.

## U7-9 — full unconditional witness

No branch currently supplies one explicit positive integer together with a complete proof that its shortcut-Collatz orbit avoids the trivial cycle forever.

**Status:** `OPEN`.