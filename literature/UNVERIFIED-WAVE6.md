# Unverified and incomplete items — literature audit wave 6

An entry here is an honest gap. It must not be cited as a proved native implication.

## U6-01 — block Christoffel formula for the decimated q-Gaussian functional

`LIT-KTHM-0045` writes

```text
nu_N=L(D_W(x)x^(rN)).
```

Krattenthaler's scalar Christoffel theorem applies to `L(x^(i+j)D(x))`, not to the `r`-decimated exponent lattice. A block or mixed multiple-orthogonal determinant factorization for the exact residue-class functionals

```text
L_h(y^N)=L(x^(rN+h))
```

has not been derived from any located source.

**Required before use:** an exact formula for every base and bordered Cramer minor in `SYN/T-9821`, with all normalizations and confluent cases stated.

## U6-02 — primitive global height after bulk factorization

No located theorem gives

```text
log_2 H(A_n(1):B_n(1))
 <=(81Sr-eta_r)n^2+o(n^2)
```

for the combined periodic-stack Padé pair.

`SYN/T-9824` shows one visible Vandermonde is insufficient. `LIT-KTHM-0046`–`0047` identify where further cancellation may occur, but neither proves a positive `eta_r`.

## U6-03 — period-uniform constants

Even an irrationality theorem for every fixed period does not automatically pass to the balanced nonperiodic directive. No current source supplies constants uniform enough in the growing standard-word period.

**Required:** explicit dependence on `r`, the word height sum, coefficient heights, and the exact S-adic transfer error.

## U6-04 — primary-source rational-base boundary for PR #35

PR #35 identifies the `5/4` low-digit completion with a named approximate-multiplication/minimal-word frontier. The exact theorem/conjecture normalization, digit convention, and relationship to the recent cited preprint have not been fully audited in this literature branch.

## U6-05 — Dubickas specialization at `(81,64)`

The full constants and extremal Thue–Morse word in Dubickas's nearest-integer theorem have not been specialized to

```text
||xi(81/64)^n||<=1/81.
```

No claim that the constant exceeds, equals, or falls below `1/81` is currently verified.

## U6-06 — cofinal PR #3 twelve-bit exclusion

The finite audit finds the required two-block Hensel pattern empty at scales 12 through 19. No recurrence or source theorem proves cofinal emptiness.

A useful external theorem must be applied to one exact rational/product-formula object carrying the real defect, dyadic zero block, and ternary type simultaneously.

## U6-07 — effectiveness of the finite room set

PR #3 proposes at most 64 eventual rooms and transcendence of each possible room. No algorithm or theorem enumerates those rooms, decides whether any exists, or bounds the first valid scale.

## U6-08 — H subsystem source reductions

The phrases “near-Pillai,” “S-unit equation,” and “fresh prime mass” do not by themselves meet the hypotheses of Chim, Bugeaud, or Evertse–Schlickewei–Schmidt.

**Required:** exact algebraic bases, exponent variables, multiplicative independence, finite-rank support, injective parameterization, and complete proper-subsum classification.

## U6-09 — PR #20 withdrawn statements

No counterexample is known to the mathematical statements withdrawn as `T-9418`–`T-9421`. They remain open, not refuted. Only the submitted proofs are invalid.

They may be revisited only with a genuine ordinary numerator/global-height coordinate.
