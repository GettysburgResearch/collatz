# Q-6501 — Close both least-counterexample coefficient lanes

**Claim ID:** `Q-6501`  
**Title:** Eliminate the zero-rate rank-escaping supercritical cusp and every finite first-crossing cylinder  
**Status:** **OPEN / GLOBAL BLOCKER**  
**Authoring agent:** `gpt56-positive-02` (`GPT-5.6 Pro`)  
**Created:** 2026-07-31  
**Last updated:** 2026-08-01  
**Issue:** #78  
**Dependencies:** `D-6501`, `L-6501`--`L-6506`, `T-6501`--`T-6507`, `R-6501`--`R-6502`; branch-qualified PR #76--#77

## Exact target

After independent reconstruction of the branch-qualified dependencies, prove both statements below.

# Lane A — exclude the zero-rate, rank-escaping, supercritical ordinary cusp

There is no positive integer `n` whose actual shortcut-Collatz orbit satisfies

\[
D_k=q_k-{\log2\over\log3}k\ge0
\qquad(k\ge0).
\tag{A}
\]

The newest reviewed PR #77 theorem `T-6710` gives the exact source formulation:

\[
\boxed{
\text{Lane A is empty}
\iff
\tau_c(n)<\infty\text{ for every }n\ge1
\iff
m_N^{\rm sup}\to\infty.}
\tag{1}
\]

For one fixed source `n`, the missing statement is equivalently a source-dependent upper bound on all-supercritical divisibility:

\[
\boxed{
 v_2(3^{q(w)}n+A_w)<|w|
}
\tag{2}
\]

for every sufficiently long all-supercritical word `w`.

The present packet proves that any ordinary Lane-A orbit must satisfy all of the following.

## 1. Divergence and correction control

\[
T^k(n)\to+\infty,
\]

\[
P_k\le e^{7/9}k^{1/9},
\qquad
T^k(n)=n3^{D_k}P_k.
\]

## 2. Logarithmic mean bank and records

\[
{1\over K}\sum_{k=1}^{K}D_k
\ge {8\over9}\log_3K-O_n(1),
\]

\[
\max_{k\le K}3^{D_k}
\ge {K^{8/9}\over ne^{5/3+o(1)}}.
\]

## 3. Sparse fixed-band returns

For every fixed `H`,

\[
\#\{k<K:D_k\le H\}=O_{n,H}(K^{1/9}),
\]

so `D_k -> infinity` in natural density one.

## 4. Critical relative recurrence

The source-qualified López--Stoll theorem forces

\[
\liminf_{k\to\infty}{D_k\over k}=0.
\]

Therefore there are arbitrarily late times with

\[
\log T^k(n)=o(k).
\]

## 5. Simultaneous canonical-boundary collapse

Along a cusp subsequence,

\[
r_k=n,
\qquad
s_k=T^k(n),
\]

with

\[
{\log r_k\over k}\to0,
\qquad
{\log s_k\over q_k}\to0.
\]

## 6. Exact adelic saturation

At one cusp prefix,

\[
A+n3^q=s2^k.
\]

`L-6505` proves a normalized three-place product of order

\[
H(A,3^q,2^k)^{-4+o(1)}.
\]

But every point lies identically in the slowly moving plane

\[
A+n3^q-s2^k=0.
\]

After quotienting this plane, the coordinate product is exactly the projective-line baseline

\[
H(3^q,2^k)^{-2}.
\]

Thus standard Ridout/Subspace smallness gives no contradiction without a second independent form.

## 7. Multiplicative-rank and prime-place escape

Define the positive split

\[
U_j={A_j\over s_j2^{k_j}},
\qquad
V_j={n3^{q_j}\over s_j2^{k_j}},
\qquad
U_j+V_j=1.
\]

For the group generated by the first `J` actual pairs, `T-6507` proves from Beukers--Schlickewei

\[
\boxed{
\operatorname{rank}\langle(U_1,V_1),\ldots,(U_J,V_J)\rangle
\ge {\log_2J\over8}-1.}
\tag{3}
\]

Moreover

\[
\boxed{
\#\operatorname{supp}_{\rm prime}
\left(6n\prod_{j\le J}A_js_j\right)
\ge {\log_2J\over16}-{1\over2}.}
\tag{4}
\]

If `N_J` and `E_J` are respectively the new numerator and endpoint prime sets outside the fixed support of `6n`, then

\[
\boxed{
|N_J|+2|E_J|
\ge {\log_2J\over8}-O_n(1).}
\tag{5}
\]

Every fixed finite-rank, fixed-`S`, and finite multiplicative-alphabet cusp class is therefore eliminated.

Equation `(5)` localizes the escape: if endpoint prime support is sublogarithmic, essentially the whole required multiplicative rank must be generated inside the Collatz affine numerators.

## 8. Bilateral canonical propagation

For every fixed `0<epsilon<alpha` and every large cusp length `K`, a linear interval of cuts

\[
t_0\le t\le(\alpha-\varepsilon)K
\]

is canonical from both directions and satisfies

\[
x_t\le\min\{3^{q_t},2^{K-t}\}.
\]

If

\[
B_t=\min\{3^{q_t},2^{K-t}\},
\]

then distinctness of the same ordinary orbit gives

\[
\#\{t:B_t\le Y\}\le\lfloor Y\rfloor-n+1.
\]

## 9. Infinite-stopping tail-minimum ladder

The same orbit generates tail minima

\[
h_0<h_1<h_2<\cdots,
\qquad
h_{i+1}\le(3h_i+1)/2,
\]

with infinite ordinary stopping time and coefficient-stopping depths tending to infinity.

# The exact Lane-A closure options

Any one of the following proves `(A)` impossible.

## A1. Second independent quotient form

Find slowly moving forms, independent after quotienting the physical affine plane, for which one fixed `epsilon>0` gives

\[
\prod_{v\in S}
{ |M_{1,v}(3^q,2^k)M_{2,v}(3^q,2^k)|_v
 \over \|(3^q,2^k)\|_v^2}
\le
H(3^q,2^k)^{-2-\varepsilon}.
\]

## A2. Numerator-rank-to-endpoint-height transfer

Prove that simultaneous zero canonical source/end rates force

\[
\operatorname{rank}
\langle(U_1,V_1),\ldots,(U_J,V_J)\rangle
=o(\log J).
\tag{6}
\]

This contradicts `(3)`.

A sufficient raw version is

\[
\limsup_j{\log s_j\over q_j}>0.
\tag{7}
\]

A more structural version is to prove that the affine numerators cannot alone satisfy

\[
|N_J|\ge\left({1\over8}-o(1)\right)\log_2J
\]

while the same endpoint sequence has zero exponential rate.

## A3. Bilateral capacity overload

For one cusp and one `Y=exp(o(K))`, prove

\[
\#\{t:\min(3^{q_t},2^{K-t})\le Y\}
>Y-n+1.
\]

This contradicts `L-6506`.

## A4. Pointwise orbit mixing or inverse-tree descent

Force one coefficient-subcritical prefix on the same orbit, or show that the tail-minimum ladder creates a smaller counterexample.

# What does **not** close Lane A

## Scalar firewall — `R-6501`

The four scalar conditions, critical `D_k/k ->0`, finite ordinary compatibility at every depth, and one completed `2`-adic word are mutually consistent.

## Product-formula firewall — `R-6502`

The apparent Subspace power saving is exactly the physical affine plane. Quotienting leaves equality. Reusing the same relation at more precision does not create a second form.

## Endpoint accumulation alone — PR #77 `L-6711`

Low-band odd endpoints force the canonical endpoint upward, but the same bounded canonical source may remain compatible. Endpoint escape is not source escape.

## Finite-rank theorem alone

The unit-equation theorem forces rank escape; it does not prevent the affine numerators from carrying all fresh prime mass while the endpoints remain subexponential.

# Lane B — exclude all finite first crossings

For every first-crossing parity word `w`, use the exact data of `L-6502`:

\[
x=r_w+2^j t,
\]

\[
T^j(x)=y_w+3^q t,
\]

\[
0\le t\le
H_w=\left\lfloor{y_w-r_w\over2^j-3^q}\right\rfloor.
\]

Prove that no member of this finite list can be a least positive counterexample.

PR #81 folds positive cycles into this canonical-displacement lane and closes the upper-mechanical/no-wrap class. PR #83 proves the universal corrected shifted equation and polynomial sparsity of failure words. High-bank, square-root-rough, high-complexity, growing-support denominator cancellation remains.

Another finite set of crossing lengths or continued-fraction cells does not close Lane B.

# Why this is exhaustive

If Collatz is false, choose a least positive counterexample.

- If its coefficient never crosses below one, it belongs to Lane A.
- If the crossing is finite, its first crossing belongs to Lane B.
- If the orbit is eventually periodic, rotate the nontrivial positive cycle to its minimum; one period belongs to Lane B.

There is no third positive ordinary failure mode.

# Current status

The fixed-integer attack has crossed one genuine arithmetic boundary: an infinite Lane-A cusp cannot remain in finite multiplicative rank or finite prime support, and its first `J` cusp levels require rank at least `(log_2 J)/8-1`.

It has **not** crossed the final source/end incompatibility. The strongest exact remaining target is the numerator-rank-to-endpoint-height transfer `A2`, with the fixed-source valuation form `(2)` and alternatives `A1/A3` making the same boundary explicit.

No proof of Collatz and no counterexample is claimed.
