# Linear-refund review and constructive frontier

**Agent:** `gpt56-refund-01` (`GPT-5.6 Pro`)  
**Issues:** #43 and #46  
**Branch:** `agent/gpt56-refund-01/43-linear-refund-invariant`  
**Last updated:** 2026-07-23

This packet contains:

1. adversarial reconstruction of PR #45's compressed critical-cycle packet;
2. adversarial reconstruction of PR #42 `T-8601`, with neutral pending-replay labels for its large finite censuses;
3. adversarial reconstruction of the PR #49 intrinsic phase-34 core decoder;
4. adversarial reconstruction of the PR #51 negative-cycle run highway;
5. common changing-modulus refund theorems, source-qualified schedule exclusions, and a corrected transported-stack interface.

No positive-integer Collatz counterexample or `K-####` object is claimed.

## First refund result

For linear stage bases

```text
B_n=B_0+4096n,
A(B)=1792B+3657472,
E(B)=2816B+5792512,
```

a coherent ordinary path has canonical quotient coordinates

```text
Y_n=y_n+2^E(B_n+4096) k_n.
```

Once `B_n>=955024` and `k_n>=1`, the exact two-stage refund inequality forces

```text
k_(n+1)>=2 k_n.
```

## Common top-boundary machine

The intrinsic PR #49 core and the PR #51 maximal-run core share one exact machine:

```text
q      = rho + 2^H ell
q_next = sigma + P ell
sigma >= 0,
P odd.
```

For PR #49, the next quotient exponent simplifies to

```text
H=11(t+33),
```

independent of both target types. From the first uniform multiple-of-sixteen threshold

```text
t=5632,
```

every coherent positive top lift is permanently refunded and grows multiplicatively.

For PR #51, the corresponding cone is

```text
9^(r_n+1)>2^(5+3*r_(n+3)).
```

Growth and local capacity are therefore no longer the missing resources.

## Affine-run schedule closed negatively

The earlier explicit schedule

```text
r_n=64+n
```

satisfies both growth cones but does not have an ordinary core. `R-8202` proves more generally that every eventually affine positive-slope schedule

```text
r_n=R+d*n,
d>=1,
```

selects an irrational scalar Tschakaloff value. The source theorem is Amou--Matala-aho--Väänänen (2007), Theorem 5.1, applied at the 2-adic place.

The primary small-chart target is now `Q-8203`:

```text
one genuinely nonlinear/adaptive ordinary run-core path
+ every certified block of nine runs has total >=44
+ exact top-boundary closure.
```

## Plain stack refuted; transported stack repaired

PR #49 `T-8512` correctly proves large ordinary radix capacity, but its plain Euclidean digits are not the residues consumed by later connectors. The intervening odd affine map twists every later digit. `R-8203` gives a concrete intrinsic-core mismatch.

`L-8210` supplies the exact repair. For

```text
H_(h+1)*ell_(h+1)=A_h*ell_h+delta_h,
```

the first `s` future legality conditions pull back to one nested residue

```text
ell_0=Theta_s mod K_s,
K_s=product_(h=1)^s H_h.
```

The transported digits of `Theta_s` are generated one at a time through the accumulated odd multiplier and carry. They, not the raw Euclidean digits, form the true future-cylinder stack.

The retained PR #49 size estimates imply

```text
transported depth >= floor(r/288)-1,
transported depth >= floor(r/233)-1 eventually,
liminf transported_depth/r >= 1/233.
```

Thus a hypothetical permanent-refund orbit still dynamically creates an unbounded finite ordinary stack, but only after inverse-affine transport is included.

## Exact remaining boundary

The shared positive obligation is now:

```text
generate every next transported low residue causally
from one finite ordinary initial object,
with eventual-zero most-significant closure
and every physical gate replayed.
```

A compatible modular path, a unique `2`-adic fixed point, or a raw mixed-radix capacity estimate is not an ordinary witness.

## Review order

1. `reports/gpt56-refund-01/2026-07-23-second-pass-transported-stack.md`
2. `research/refund-review/claims/L-8210-transported-future-cylinder-stack.md`
3. `research/refund-review/claims/R-8203-false-future-stack-crosswalk.md`
4. `research/refund-review/claims/R-8202-eventually-affine-run-tschakaloff-exclusion.md`
5. `research/refund-review/claims/Q-8203-nonlinear-block-average-highway.md`
6. `reports/gpt56-refund-01/2026-07-23-critical-path-eureka.md`
7. `reports/gpt56-refund-01/2026-07-23-43-intrinsic-core-adversarial-review.md`
8. `reports/gpt56-refund-01/2026-07-23-46-run-highway-adversarial-review.md`
9. `research/refund-review/claims/L-8203-common-changing-modulus-quotient-law.md`
10. `research/refund-review/claims/T-8201-intrinsic-top-lift-doubling.md`
11. `experiments/X-8203-absorption-affine-run/README.md`
12. earlier PR #45/#42 reports and `L-8201`--`Q-8201`.