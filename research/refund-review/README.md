# Linear-refund review and constructive frontier

**Agent:** `gpt56-refund-01` (`GPT-5.6 Pro`)  
**Issues:** #43 and #46  
**Branch:** `agent/gpt56-refund-01/43-linear-refund-invariant`  
**Last updated:** 2026-07-23

This packet now contains four coordinated tasks:

1. adversarial reconstruction of PR #45's compressed critical-cycle packet;
2. adversarial reconstruction of PR #42 `T-8601`, with neutral pending-replay labels for its large finite censuses;
3. adversarial reconstruction of the PR #49 intrinsic phase-34 core decoder;
4. adversarial reconstruction of the PR #51 negative-cycle run highway, plus a common top-boundary theorem.

No positive-integer Collatz counterexample or `K-####` object is claimed.

## First refund result

For linear stage bases

```text
B_n=B_0+4096n,
A(B)=1792B+3657472,
E(B)=2816B+5792512,
```

a coherent ordinary path has canonical quotient coordinates

```text
Y_n=y_n+2^E(B_n+4096) k_n.
```

Once `B_n>=955024` and `k_n>=1`, the exact two-stage refund inequality forces

```text
k_(n+1)>=2 k_n.
```

## Critical-path eureka

The intrinsic PR #49 core and the PR #51 maximal-run core share one exact machine:

```text
q      = rho + 2^H ell
q_next = sigma + P ell
sigma >= 0.
```

For PR #49, the next quotient exponent simplifies to

```text
H=11(t+33),
```

independent of both target types. From the first uniform multiple-of-sixteen threshold

```text
t=5632,
```

every coherent positive top lift doubles at every connector.

For PR #51, the corresponding cone is

```text
9^(r_n+1)>2^(5+3*r_(n+3)).
```

The explicit aperiodic schedule

```text
r_n=64+n
```

satisfies this cone and the physical run-five growth condition. Its ordinary realization is now the isolated question `Q-8202`.

## Exact remaining boundary

Growth, local branch existence, and finite physical replay are no longer the missing resources in either live architecture. The sole positive obligation is:

```text
generate every next low residue causally
from one finite ordinary initial object,
with canonical most-significant closure.
```

A compatible modular or `2`-adic path is not an ordinary witness.

## Review order

1. `reports/gpt56-refund-01/2026-07-23-critical-path-eureka.md`
2. `reports/gpt56-refund-01/2026-07-23-43-intrinsic-core-adversarial-review.md`
3. `reports/gpt56-refund-01/2026-07-23-46-run-highway-adversarial-review.md`
4. `research/refund-review/claims/L-8203-common-changing-modulus-quotient-law.md`
5. `research/refund-review/claims/T-8201-intrinsic-top-lift-doubling.md`
6. `research/refund-review/claims/L-8204-run-highway-refund-cone.md`
7. `research/refund-review/claims/Q-8202-linear-run-ordinary-completion.md`
8. `experiments/X-8202-critical-core-crosswalk/README.md`
9. earlier PR #45/#42 reports and `L-8201`–`Q-8201`.
