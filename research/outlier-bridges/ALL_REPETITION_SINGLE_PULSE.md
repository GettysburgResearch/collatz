# Outlier bridge audit addendum — the repetition axis collapses for one pulse

**Agent:** `gpt56-outlier-01`  
**Issue:** #52  
**Date:** 2026-07-23  
**Status:** `T-8202` is `PROPOSED / SOURCE-DEPENDENT`; `X-8202` is an exact finite certificate

## Eureka boundary

`L-8201` converted every fixed negative-cycle pulse cone into a finite box but left repetition length infinite. The next bridge combines explicit real logarithmic forms with certified continued fractions.

For a single pulse, the exact divisor condition forces

```text
0 < (delta-r alpha)log(2) < 2c/U^r.
```

Matveev bounds `r` explicitly. Legendre's criterion then says the reduced ratio `delta/r` is an upper convergent of `alpha`. Exact continued fractions leave sixteen primitive convergents, and a one-line denominator-size inequality rejects every positive multiple.

Subject to independent reconstruction of the quoted Matveev theorem, this proves:

> No single upward pulse in any repetition or rotation of either known ordinary negative accelerated cycle produces a nontrivial positive Collatz cycle. The unique hit is the trivial word `(2,2)` with `n=1`.

## Frontier change

Before `T-8202`, the single-pulse family had a finite scan through repetition `20000` and no all-repetition theorem. After `T-8202`, the remaining negative-cycle offense must use at least one of:

1. at least two pulse locations;
2. more than one primitive macro-block type;
3. a baseline other than the two known negative cycles;
4. a nonperiodic or changing-support construction;
5. the divergent run-core chart rather than a finite positive cycle.

## Why this is not the final result

This is an exclusion theorem, not a counterexample. It moves the project forward by deleting a genuinely infinite false-positive direction and exposing the next exact problem:

```text
uniform repetition control for fixed multi-pulse support.
```

The natural next bridge is to combine `L-8201`'s sparse resultants with explicit logarithmic forms, large-gcd estimates, or primitive-divisor arguments that are uniform in the repeated baseline.
