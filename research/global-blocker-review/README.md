# Global blocker review: ordinary extraction and entropic ghosts

**Agent:** `gpt56-outlier-01`  
**Issue:** #55  
**Branch:** `agent/gpt56-outlier-01/55-entropic-extraction-firewall`  
**Namespace:** `74xx`

## Purpose

This packet does not add another amplifier, long finite prefix, inverse-limit encoding, or conditional growth theorem.

It independently reviews draft PRs #56 and #57 and attacks the exact inference between finite compatibility and one ordinary infinite orbit.

## Main conclusions

### `T-7401` — the valid extraction inference

For nested positive survivor sets `S_n`, one ordinary infinite seed exists exactly when the least roots

```text
m_n=min S_n
```

remain bounded, equivalently eventually stabilize.

This is the missing Archimedean hypothesis. Finite-place compactness alone gives only an inverse-limit point.

### `R-7401` — entropy and drift still do not extract an integer

Inside the raw shortcut-Collatz parity system there is a computable compact perfect family of infinite paths with:

```text
positive entropy,
positive 2-adic Hausdorff dimension,
uniform supercritical odd density,
infinitely many positive ordinary roots for every finite prefix,
```

but no path is the parity sequence of any ordinary signed integer.

Thus compactness, branching, entropy, dimension, and conditional growth cannot replace bounded least roots.

### Blunt progress verdict

The repository has genuine exhaustive negative progress and useful verified infrastructure. But no live positive ordinary architecture has crossed its existence boundary.

The current full-objective funnels are exactly:

```text
ordinary orbit:
  bounded least roots versus escape to infinity;

positive cycle:
  exact full-denominator remainder zero versus nonzero remainder.
```

## Claims

| ID | Status | Content |
|---|---|---|
| `T-7401` | `PROPOSED` | Two-place ordinary extraction is exactly Archimedean tightness / bounded least roots. |
| `R-7401` | `PROPOSED` | Positive-entropy uniformly supercritical Collatz ghost forest with no ordinary path. |

## Review verdicts

The mathematical cores of the following source claims reconstruct independently:

```text
PR #56 T-7801: PASSED, one editorial control-character fix required;
PR #56 R-7801: PASSED within proof-schema scope;
PR #57 L-7601/T-7601: PASSED;
PR #57 T-7602: PASSED;
PR #57 T-7603: PASSED as a decision restatement, not a decision.
```

No source claim is promoted by authority; the review records an independent reconstruction and scope audit.

## Read first

1. `claims/R-7401-entropic-supercritical-ghost-forest.md`
2. `claims/T-7401-archimedean-tightness-extraction.md`
3. `GLOBAL_PROGRESS_VERDICT.md`
4. `REVIEW_PR56_PR57.md`
5. `../../reports/gpt56-outlier-01/2026-07-25-55-global-blocker-review.md`

## Exact remaining target

For one strict architecture, prove one of:

```text
sup_n m_n < infinity,
```

or

```text
m_n -> infinity.
```

The first extracts a written ordinary seed and yields a `K-####` candidate after replay. The second eliminates the complete architecture.

For the cycle lane, prove one complete remainder equality or a mixed-place height contradiction. Anything else remains infrastructure or family-specific narrowing.