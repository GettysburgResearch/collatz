# Sources, review boundary and publication state

Research baseline: `GettysburgResearch/collatz` main at
`69b1ed57ce90fe8c9d6d7c80c69760a86eb0fd2a`.

The new work is an exploratory continuation after the A/B/D integration, not
an amendment of Review A or a promotion of a canonical theorem. Every ALF claim
is PROPOSED pending independent mathematical review. Source findings are credited
at their own identities; no previous verdict is extended to this packet.

## Exact repository inputs

1. PR90 at `78ac7c8489f1df81230402808b1f4b77b18fae73`,
   `research/astra-critical-mass/INVERSE_SHADOW_FRONTIER.md`,
   L-AS7-001, T-AS7-002/003 and R-AS7-006. The file is resident unchanged at the
   baseline above, blob `ac38112f418b35250e610b42941c75c55a033544`.
   Used: complete signed inverse fan, finite comparison shield, inverse minimum
   interface, and the 121 control. The linear upper/lower precision bounds and
   bounded-section box theorem are new statements with their own proofs here.
2. PR90 at the same head, `research/astra-critical-mass/CLOCK_DEFECT.md`,
   T-ASTRA-030 and C-ASTRA-031. Comparison only: those theorems concern bounded
   shortcut clocks, not bounded numbers of complete section returns. The new
   all-witness statement does not identify those different clocks.
3. PR90 at the same head, `research/astra-critical-mass/BOUNDARY_FAN.md`,
   R-ASTRA-034. Preserve the counterexample to permanent boundary-only pruning
   at208363 and its actual lower-P source4445077. This file's T-ASTRA identifiers
   differ from the same identifiers in CLOCK_DEFECT.md.
4. PR91 at `b8c88843726ee7ac11cf91323c69bf911ca50706`,
   `research/astra-three-routes/ROUTE_1_TRANSFER.md`, T-ATR-101: section/return/fan
   provenance. No charged operator, density or renewal theorem is imported.
5. PR92 pass4 at `6266bd8f73ce2f8488c02fbe29f34fd2b4f289bc`,
   `research/astra-three-routes/pass4/MOVING_GHOST_RANK.md`: component R_2 and
   its exact repeated110 scaling. Also read at the frozen current main.
   C(n) in ALF-006 is exactly that old component, not a new rank. Neither the
   entire moving envelope nor an unreviewed synthesis is a proof dependency.
6. Current `AGENTS.md`, `README.md`, `docs/RESEARCH_MAP.md`,
   `research/open-obligations/README.md`, `research/integrated/rank-merging/README.md`,
   and `research/integrated/ERRATA.md` at the baseline: orientation and current
   boundary conditions. These are not modified.

The positive scalar-combination failures here concern min(P,C) and max(P,C).
They do not rule out every weighted combination or every unbounded-state
switching construction. The larger finite-feature obstruction literature in
PR92 is related background, not a new blanket theorem claimed by this packet.

## Evidence tiers

The two implementations have the same author. Their agreement is implementation
independence, not a second independent proof review. Both use explicit exceptions;
no acceptance condition is an assertion removable by optimized Python.

The canonical artifact records exact finite reconstructions. All-parameter
claims depend on the written proofs, not extrapolation of those data. The
compressed convergence examples record actual modular exponent certificates,
while explicitly declaring that their enormous ordinary integers and all
shortcut steps were NOT materialized or literally replayed.

Executed command outputs and return codes are in
[validation.json](../../reports/astra-linear-frontier-01/validation.json).
The report lists actual performed and unperformed checks. No external formal
build, large predecessor payload, broad convergence census, or full checkout
validator is claimed. There is no external theorem dependency in ALF-001–007,
and no claim of comprehensive literature priority.

## Publication boundary

The GitHub connector exposed no create or push actions in this session. Direct
Git access was attempted separately. The exact command result is preserved in
[publication-status.json](../../reports/astra-linear-frontier-01/publication-status.json).
No remote branch, PR or commit is claimed without a write receipt. The supplied
addition-only patch and guarded helper are publication handoff artifacts, not
proof that a remote write happened. They do not alter main, historical source
files, review directories, registry status, settings or workflows.
