# Cross-branch interfaces — linear quotient refund

All references remain branch-qualified and retain their source status.

## PR #3

- `L-0016`: exact phase-34 tower anchors and high-tail preservation.
- `L-0017`: canonical mixed-radix connector.
- `L-0031`: scaled-tail recurrence and the constants `p=(5,30,20,56)`, `b=(9,54,36,24)`.
- `T-0030`: ordinary quadratic/formal-group bulk generator; a possible future source of causal top-boundary arithmetic.
- `T-0031`: doubling-scale quotient extinction; used as the contrast, not as a premise for the linear schedule.
- `T-0028`/`T-0029`: finite prefix routing; contextual only.

## PR #33 and PR #44

PR #33 `T-9705` closes the frozen doubling-scale corrected class. PR #44 independently reconstructed that chain within its exact frozen scope. `L-8501` changes the schedule so the first load-bearing extinction inequality is reversed; it does not refute or weaken the frozen theorem.

PR #44 also proves the relevant abstraction firewall: a fixed-modulus PDR lasso can be only a periodic completion ghost unless a canonical most-significant boundary or finite-support obligation is carried. `L-8504` supplies an intrinsic ordinary boundary marker, but all-time legality remains open.

## PR #34

- `T-9831` and `T-9834`: fixed-width and bounded-essential-width almost-`S`-unit boundaries.
- `T-9832`: general oriented completion criticality.
- `L-9915`: finite control with only an additively updated counter observed through fixed residues emits an ultimately periodic word.

The present map escapes `L-9915` because its modulus and exact quotient operation grow with `t`; nevertheless the theorem rules out replacing the exact top coordinate by a fixed-residue additive abstraction.

## PR #48 — independent issue-#43 review

- `L-8201`: independently proves that coherent linear-height lift quotients grow automatically after the second-stage refund threshold.
- `R-8201`: preserves the nested-cylinder/top-boundary firewall.
- `L-8202`: quantifies the extreme mismatch between finite word branching and the next binary modulus.
- `Q-8201`: recommends a causal top-boundary compiler, possibly coupled to PR #3's ordinary quadratic bulk.

These findings agree with `T-8504`, `T-8506`, and `T-8508`: growth is not the bottleneck. The positive target is ordinary coherence from one finite state.

## PR #13

- `LIT-KTHM-0042`: fully inspected Väänänen–Wallisser theorem and applicability protocol.
- `LIT-KTHM-0048`/`0049`: ordinary rational-base marker and infinite-state boundary.
- `LIT-KTHM-0050`: first exact width-256 linear refund theorem.

`L-8501` generalizes refund to every fixed width and identifies width one as the minimal local target. `T-8503` recomputes the source parameter for this linear grid and obtains the exact period cutoff `58`.

## PR #51 — negative-cycle pulse chart

PR #51 now supplies a genuinely parallel small model.

`L-8004` gives the invariant divisible-seven core

\[
2^{4+3s}v^+=9^{r+1}v+1.
\]

`L-8002` then derives the exact changing-modulus quotient transition

```text
k=rho_(r,s,t)+2^(4+3t)*ell
  ->
k_next=sigma_(r,s,t)+9^(r+1)*ell.
```

`T-8002` proves that a forever-defined path whose emitted runs are all at least five is a complete positive unbounded Collatz orbit.

`L-8507` shows that the phase-34 core has exactly the same structural interface:

```text
m=rho_k+2^(11(t+33))*ell
  ->
m_next=sigma_k+3^G*ell.
```

In both systems:

```text
- the modulus changes with the physical scale;
- one ordinary quotient carries the most-significant boundary;
- the odd multiplier refunds the unused lift;
- finite pair compatibility is automatic;
- infinite definedness is the sole existence gap.
```

The phase-34 system has four continuation residues with a type-independent modulus; the pulse chart has an unbounded run label controlling its modulus. A reusable top-boundary theorem should be stated for this shared multiplicative-quotient interface, not for either branch's surface notation alone.

## Issue #40 and PR #37

The proof-carrying modular/PDR recommendation is directly relevant. A sound abstraction must retain the exact ordinary counter or prove that equal abstract states have equivalent futures; a residue-only lasso is insufficient.

## Issue #39

The cross-cycle handoff is a distinct architecture-changing lane. The present packet stays inside the phase-34 linear connector family.

## New native cross-interface

`L-8504` identifies the ordinary physical boundary marker

\[
\nu_2(n+34)=11t+5+i,
\]

and the determinant-one complement basis. This supplies a common certificate language for future PDR, cross-cycle, and one-counter work:

```text
physical integer n,
intrinsic valuation marker,
odd boundary word W,
unimodular top coordinate k,
canonical next-boundary divisibility.
```

`L-8507` adds the exact complete top-boundary language:

```text
four residues rho_k in one modulus H_t,
one nonnegative carry sigma_k,
one odd refund multiplier 3^G.
```

`T-8505` adds the global requirement that any infinite path escape every fixed finite prime support.
