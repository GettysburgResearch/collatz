# Linear-height quotient-refund packet

**Agent:** `gpt56-cylinder-01`  
**Issue:** `#43`  
**Namespace:** `85xx`  
**Status:** theorem-level claims are `PROPOSED`; `X-8501` is an exact finite interface audit

## Headline

The frozen doubling-scale architecture in PR #33 loses its ordinary quotient to the next complete radix. This packet changes the padding schedule to

\[
t_n=t_0+16n.
\]

The resulting architecture is outside that negative theorem. More importantly, it does not require 256 transitions before refund appears.

For one connector,

\[
N_t=3^{7(t+1)},
\qquad
Q_t=2^{11(t+33)}.
\]

At every multiple of `16` with `t>=3744`,

\[
\boxed{N_t>2Q_t.}
\]

A legal ordinary residual transition therefore doubles its residual counter.

## Exact deterministic map

For the current edge `i -> j`, the connector data are generated from one ordinary inverse carry

\[
Nr=1+Mc
\]

and one six-bit cell. The carry advances from `t` to `t+16` by appending one exact base-`3^112` digit. No completed logarithm or inverse tape is initialized.

Given the current finite residual `z`, the next type is not freely chosen. It is uniquely decoded by:

1. one forced low-block divisibility condition of width `11(t+33)-6` bits;
2. one six-bit membership test among four cells.

When those tests pass, the next residual is an exact integer. Above height `3744`, it satisfies

\[
z_{n+1}\ge2z_n.
\]

Thus a single finite state whose decoder is defined forever is already a complete counterexample certificate. Growth is no longer an open obligation.

## Why this is closer to the full objective

The earlier 256-stage pairwise theorem said every finite word pair has infinitely many expanding quotient lifts. That still left an arbitrary future word and an inverse-limit quotient.

`T-8501` removes both freedoms locally:

```text
current ordinary state
  -> forced low block
  -> unique next tower type
  -> exact next ordinary residual.
```

The full positive target is now only infinite definedness of this deterministic partial counter map from one explicit seed.

## Structural filters

`T-8502` proves that the set of all compatible initial residual completions has Haar measure zero and `2`-adic Hausdorff dimension zero. At depth `N`, the directive supplies `2N` choice bits but exact continuation demands

\[
88N^2+(11t_0+275)N
\]

residue bits.

`T-8503` applies the fully inspected Väänänen–Wallisser theorem to the exact linear-grid completion series. The improved parameter closes every eventually periodic local type tail of minimal period at most `58`; the source condition first fails at `59`.

Therefore a witness must be an exceptional ordinary point generated by genuinely unbounded arithmetic state. Neither entropy nor a short finite-state cycle can create it.

## Full counterexample formula

A forever-defined finite tuple `(t_0,i_0,i_1,z_0)` with `t_0>=3744` and `z_0>=1` initializes

\[
n_0
=
A_{i_0}(t_0)
+2^{11(t_0+1)}
\left(
\eta_{t_0;i_0,i_1}
+2^{11(t_0+17)}z_0
\right)
-34.
\]

`T-8501` proves every physical block, positivity, and unboundedness. No `K-85xx` identifier is assigned because no forever-defined tuple has yet been proved.

## Verification

```bash
python3 -B experiments/X-8501-linear-refund/derive.py \
  --output experiments/X-8501-linear-refund/results/canonical.json \
  --summary experiments/X-8501-linear-refund/results/summary.txt

python3 -B experiments/X-8501-linear-refund/verify.py \
  --check-results experiments/X-8501-linear-refund/results/canonical.json
```

The checker does not import the derivation module.

## Review order

1. `D-8501-linear-connector-refund-class.md`
2. `L-8502-inverse-carry-connector-normal-form.md`
3. `T-8501-deterministic-refund-decoder.md`
4. `L-8501-fixed-width-linear-refund.md`
5. `T-8502-quadratic-cylinder-pressure.md`
6. `T-8503-period-58-irrationality.md`
7. `Q-8501-infinite-defined-residual.md`
8. `experiments/X-8501-linear-refund/`

## Scope boundary

This packet is not a Collatz counterexample. It is an integer-first reduction that leaves one exact safety property open. Finite valid prefixes, the zero-dimensional completion set, and the causal inverse-carry generator do not prove one ordinary residual survives forever.
