# Linear-height quotient-refund packet

**Agent:** `gpt56-cylinder-01`  
**Issue:** #43  
**Namespace:** `85xx`  
**Status:** theorem-level claims are `PROPOSED` unless explicitly marked otherwise; experiments are exact finite-interface audits

## Mission

Change the phase-`-34` tower schedule from doubling to

\[
\boxed{t_n=t_0+16n}
\]

and construct one positive ordinary integer whose exact finite state routes forever. A successful routing theorem immediately gives an unbounded shortcut-Collatz orbit.

The packet does not count finite prefixes, completed `2`-adic points, entropy, or future symbols as ordinary initialization.

## 1. Local refund and physical marker

For one connector,

\[
N_t=3^{7(t+1)},
\qquad
M_t=2^{11(t+17)}.
\]

`L-8501` proves that at every multiple of `16` with `t>=3744`,

\[
\boxed{N_t/M_t>2^{177}.}
\]

The connector inverse is generated causally by one ordinary Bezout state

\[
Nr=1+Mc.
\]

Put

\[
d=M-r,
\qquad
e=N-c.
\]

The complement basis is unimodular:

\[
\boxed{
\begin{pmatrix}M&d\\N&e\end{pmatrix}
\in\operatorname{SL}_2(\mathbf Z).}
\]

For consecutive boundary words `(W,U)`,

\[
k=eW-dU,
\qquad
b_i=MU-NW.
\]

The physical boundary integer

\[
n=2^{11(t+1)}W/64-34
\]

satisfies

\[
\boxed{\nu_2(n+34)=11t+5+i.}
\]

Since `16|t`, the one integer recovers its height and type; its remaining factors recover the ternary signature and unbounded core.

## 2. Intrinsic primitive-core decoder

Write

```text
beta=(2,3,2,1),
b_i=2^i 3^(beta_i).
```

After the first boundary, the prime-to-six core `C` obeys

\[
\boxed{2^LC'=3^GC+1.}
\]

The exact runtime state is

```text
(t,gamma,i,C),
gamma in {1,2,3},
i in {0,1,2,3},
gcd(C,6)=1.
```

Put

\[
G=7(t+1)+\gamma-\beta_i,
\qquad
D=11(t+17)-i,
\]

\[
X=3^GC+1.
\]

The step is defined exactly when

\[
2^D\mid X
\]

and, with `Y=X/2^D`,

\[
[3^{\beta_i}Y]_{64}\in\{5,30,20,56\}.
\]

The residue gives the unique next type `j`,

\[
C'=Y/2^j,
\]

and

\[
(t,\gamma,i,C)
\longmapsto
(t+16,\beta_i,j,C').
\]

The corresponding physical integer is

\[
\boxed{n=2^{11t+5+i}3^\gamma C-34.}
\]

Every defined update is exactly `11(t+1)` shortcut-Collatz steps with `7(t+1)` odd steps.

`T-8506` proves the uniform growth

\[
\boxed{C'>2^{170}C,}
\]

and consecutive cores are coprime.

## 3. Local ordinary block compiler

`L-8506` compiles every finite core state into exactly eight ordinary source cylinders modulo

\[
3\cdot2^{D+6}.
\]

Each branch has the exact form

\[
\widehat R_{j,\nu}
+3\cdot2^{D+6}m
\longmapsto
\widehat S_{j,\nu}
+3\cdot2^{6-j}3^Gm.
\]

For one fixed current block, `L-8507` compiles complete compatibility with the following block into four cylinders of one top quotient:

\[
\boxed{
m=\rho_k+H_t\ell
\longmapsto
m'=\sigma_k+3^G\ell,}
\]

where

\[
\boxed{H_t=2^{11(t+33)}}
\]

is independent of both target types and both ternary lifts.

## 4. One long block, six routing bits, one fixed type alphabet

`L-8509` proves that the four complete residues share all but six bits. With

\[
B_t=H_t/64,
\]

there is one common residue `lambda` and four distinct digits `d_k` such that

\[
\rho_k=\lambda+B_td_k.
\]

Thus a complete next test is

```text
one forced block of 11(t+33)-6 bits,
then one of four six-bit cells.
```

`L-8510` gives one branch-independent base-64 update

\[
\boxed{64m'+e_k=3^G(d_k+64\ell)+J,}
\]

with

\[
0\le J<\frac43\,3^G.
\]

`L-8511` proves that the four cell sets are state-dependent affine copies of the one physical alphabet

```text
p=(5,30,20,56).
```

With

\[
u_\gamma=3^{1-\gamma},
\qquad
v_i=3^{8-\beta_i}
\pmod {64},
\]

there are translations `delta,epsilon` for which

\[
d_k=u_\gamma p_k+\delta,
\qquad
e_k=v_ip_k+\varepsilon
\pmod {64},
\]

and the normalized symbol `p_k` is identical on both sides. The six-bit router carries one fixed physical type symbol; the difficult data are the common long residue and full ordinary quotient.

## 5. Intrinsic Hensel quotient and fixed toll alphabet

`L-8512` introduces a second determinant-one coordinate. Put

\[
a=[-3^{-G}]_{2^D},
\qquad
h=(3^Ga+1)/2^D.
\]

Then

\[
\boxed{
\begin{pmatrix}2^D&a\\3^G&h\end{pmatrix}
\in\operatorname{SL}_2(\mathbf Z),}
\]

and every high-divisible core is

\[
C=a+2^Dq,
\qquad
Y=h+3^Gq.
\]

The intrinsic quotient is recovered by

\[
q=hC-aY.
\]

For

\[
H_t=2^{11(t+33)},
\]

the four complete current-target/next-high-divisibility residues are

\[
\boxed{
q\equiv
\Lambda+U b_j
\pmod {H_t},
\qquad
b=(9,54,36,24),}
\]

for one odd unit `U`. The physical type gate is automatic. Exactly one free-lift class modulo three is excluded by source primitivity.

For an allowed lift,

\[
\boxed{
q=\varrho_j+H_t\ell
\longmapsto
q'=\tau_j+3^G\ell.}
\]

`L-8513` proves

\[
0\le\tau_j<3^G.
\]

Thus every connector is a carry-free mixed-radix operation:

```text
pop one exact dyadic block varrho_j mod H_t;
push one canonical G-trit block tau_j;
preserve the same finite ordinary high tail ell.
```

The system is a genuine self-routing ordinary stack transducer, not a completion metaphor.

## 6. Permanent multiplicative refund

A top lift is canonical when `ell=0` and refunded when `ell>=1`.

`T-8509` proves that canonical runs have a uniform finite bound. `T-8510` defines

\[
q(t)=\left\lfloor\frac{63t-353166}{665}\right\rfloor
\]

and proves, for every legal refunded step,

\[
\ell^+\ge2^{q(t)}\ell.
\]

At every multiple of `16` with `t>=5632`,

\[
\ell\ge1
\Longrightarrow
\ell^+\ge4\ell.
\]

Hence the refunded region is absorbing. Since canonical runs are bounded, every hypothetical infinite ordinary path becomes permanently refunded after finitely many connectors.

The full top quotient then satisfies

\[
\boxed{m^+>2^{175+q(t)}m.}
\]

At the absorption threshold this factor already exceeds `2^177` and increases with height.

## 7. Raw capacity and corrected transported cylinders

The original `T-8512` correctly proved numerical growth of raw mixed-radix capacity but incorrectly identified its plain Euclidean digits with later legality digits. It is now marked **SUPERSEDED** and retains only the size theorem.

The corrected theorem is `T-8513`.

For the actual next `s` connectors, every intervening odd affine multiplier pulls the later conditions back to one unique current residue

\[
R_{r,s}\pmod {P_s},
\qquad
P_s=\prod_{h=0}^{s-1}H_{r+h}.
\]

Writing

\[
m_{n+r}=R_{r,s}+P_sU_{r,s}
\]

defines the genuine free ordinary quotient beyond the complete transported cylinder.

On every permanent-refund tail,

\[
U_{r,\lfloor r/288\rfloor}\ge1
\qquad(r\ge288),
\]

and

\[
U_{r,\lfloor r/233\rfloor}\ge1
\qquad(r\ge100909).
\]

Thus the current finite integer continually prepays a complete exact future-cylinder horizon of positive linear depth and retains positive ordinary freedom beyond it.

PR #48 `R-8203` independently found the raw-digit defect. PR #48 `L-8210` gives the equivalent inverse-affine transported-stack cocycle on the free lift; under its convention, which omits the already-paid current connector, the displayed depths lose one level. The two formulations agree.

## 8. Arithmetic renewal

`T-8505` proves that every hypothetical infinite path escapes every fixed finite prime set infinitely often. Consecutive prime-to-six cores are coprime, and infinitely many globally new odd primes divide the physical shifts `n_n+34`.

A successful invariant cannot be a fixed-prime library, short periodic controller, fixed-modulus lasso, additive fixed-residue counter, or preloaded stack.

## 9. Complete counterexample criterion

One written positive integer whose intrinsic decoder is defined forever is an unconditional Collatz counterexample. The packet already supplies:

```text
state reconstruction from the integer;
exact physical replay and positivity;
deterministic future type selection;
>170 primitive-core bits per connector;
eventual permanent refund;
accelerating top-quotient growth;
carry-free mixed-radix stack transport;
linearly growing complete-cylinder prepayment;
adjacent-core coprimality and global prime renewal;
unboundedness.
```

The sole remaining positive theorem is **entry and routing**:

```text
find one finite integer whose ordinary Hensel/top quotient
hits the next affine fixed-alphabet cylinder forever.
```

## 10. Structural filters

- `T-8502`: compatible completions are Haar null and have `2`-adic Hausdorff dimension zero.
- `T-8503`: every eventually periodic local type tail of minimal period at most `58` is nonordinary.
- PR #44: fixed-modulus PDR lassos are completion ghosts without canonical top closure.
- PR #34 `L-9915`: fixed-residue additive one-counter controllers are ultimately periodic.
- `T-8505`: fixed finite prime support is impossible.

## Verification

Every experiment has a separately written checker importing no derivation module.

```text
X-8501 payload:
6328faaa680e54ab0245ba8cca41c81cebbb03ede1299b9d948d1fbf1aee41fd

X-8502 payload:
0080f0a52318034e24b97b7c0dc90c9b0539464d575aac2588dd157cccdbe2a8

X-8503 semantic:
320b667d3c82007624e7bb3ccbb3738aa31503afea3718058f9e23613b7940df

X-8504 semantic:
9610e5be23c04c02a1a1ee3a95c40ce7a96939c103cc67558862c3e3c1de0e09

X-8505 semantic:
195d29ef16bb7038ac9b3da9080344dfcc450f6663a973a757e475503afcf10e

X-8506 semantic:
cf101f17607a6c9f0c42c4723cc72968ee93acdeb8114ce20e3d3770107c827c

X-8507 transition:
66c03b99bb494255cc8d56f1e226c3f850f9548a60a0e3d3a036abac449cc724

X-8508 table:
a9bac5d31f9d5c2664d627c474941c2c7859c830b729c07567fdbba91daa53ef

X-8509 semantic:
b05a53257b8309fcd9da40d389ed87daa8ffdeb208b5c3837b726668ac002b91

X-8510 semantic:
9a200a568909a8686c6f36436dac1bc6ef4be906b043fdba05aa5be70e2c057d

X-8511 semantic:
aae8894b675bc797745ed61899af18d8828471460f7fe094123c1e47c637d957
```

## Review order

1. `T-8507-intrinsic-core-decoder.md`
2. `L-8512-hensel-quotient-toll-alphabet.md`
3. `L-8513-carry-free-ternary-push.md`
4. `L-8507-type-independent-top-boundary.md`
5. `L-8509-common-top-cell-prefix.md`
6. `L-8510-canonical-base64-carry.md`
7. `L-8511-affine-type-alphabet.md`
8. `T-8509-canonical-run-bound.md`
9. `T-8510-noncanonical-absorption.md`
10. `T-8513-complete-cylinder-prepayment.md`
11. `T-8505-fresh-prime-turnover.md`
12. `Q-8501-infinite-defined-residual.md`
13. `experiments/X-8510-*` and `X-8511-*`

## Scope boundary

This packet does not yet contain a Collatz counterexample. The remaining object is one explicit finite ordinary core plus an all-time routing invariant for the changing affine four-symbol cylinder. No `K-85xx` identifier has been assigned.