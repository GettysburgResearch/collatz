# Global counterexample cartography — pass 3

**Cutoff:** 2026-07-22 20:10:33 UTC  
**Previous cutoff:** 2026-07-22 18:27:08 UTC  
**Provenance:** [`ANALYSIS_SNAPSHOT_PASS_3.md`](ANALYSIS_SNAPSHOT_PASS_3.md)

## Executive update

The third pass changes the map in four ways.

1. The frozen corrected 256-stage collision exclusion has received an independent `PASSED` reconstruction.
2. Fixed-modulus centered PDR is now exactly understood and cannot certify an ordinary witness.
3. The direct positive-cycle lane has accumulated much stronger exact negative packets, but its unrestricted critical-scale space remains open.
4. Linear-height quotient refund and distributed negative-cycle pulses emerge as the two clearest positive construction mechanisms outside the newly closed low-memory classes.

No unconditional counterexample was found.

## 1. Corrected phase-34 class: independently closed at frozen scope

PR #44 independently reconstructs the PR #33 chain through `T-9705` and returns `PASSED` within the exact frozen corrected 256-transition architecture.

The load-bearing conclusion is now:

```text
no physically overlapping infinite directive in the frozen corrected
256-transition phase-34 class has a signed ordinary completion.
```

This is a genuine confidence promotion for that exact class. It does not cover:

- linear-height quotient-refund schedules;
- cross-cycle handoffs;
- growing essential rank;
- changing prime alphabets;
- or adaptive architectures not reducible to the fixed 258-coordinate equation.

Consequently the old room/seam search inside the frozen class is not a live counterexample path. Its surviving value is as anatomy and as a source of escape interfaces.

## 2. Centered forced-tail dynamics: residue-only abstraction is exactly the ghost

When appended blocks are zero, PR #44 derives the exact ordinary recurrence

```text
64 B' = 81 B + e - e'.
```

Legal branches are determined by `(B mod 64,e)`, and `B+4e mod17` is invariant. Every positive legal step strictly increases `B`, so any finite positive all-time seed would already be a divergent witness.

The obstruction is existence, not growth.

At fixed precision `64^d`, the greatest recurrent PDR kernel is exactly the `d+1`-bit cylinder graph, isomorphic to a binary de Bruijn graph with `2^(d+1)` states. Every lasso is a periodic 2-adic ghost; the constant lassos select zero.

Therefore:

> Increasing the modulus of a residue-only safety proof never approaches ordinary concretization. A sound certificate must carry a canonical most-significant boundary, carry flush, finite-support condition, or an unbounded height/length counter.

The centered positive atom is now a one-counter transducer problem, not a finite-state modular problem.

## 3. Positive cycles: enormous exact negative packets, unrestricted space still open

PR #42 proposes/exactly checks:

- no positive accelerated cycle in every admissible window through 27 odd terms;
- 802,459,998,516 positive compositions tested, with zero modular matches;
- no bare forward-invariant union of residue classes avoiding the trivial cycle;
- no hit in the `(41,65)` families containing all `{1,2}` words and every word with exactly one exponent at least three, conceptually covering 35,548,596,168,868 words.

PR #13 wave 7 further records:

- the complete `(8,13)` prime-233 sieve packet is empty;
- all positive accelerated cycles with 184 odd terms are excluded through the cited local-minima framework.

PR #45 attacks the true critical scale. It compiles a mechanical word with

```text
k = 3,149,971,404,836,
A = 4,992,586,555,009
```

without expansion, applies 43 exact adjacent swaps, and forces divisibility by a certified 61-bit factor product of the full denominator. A directed interval then proves the resulting fixed point is nonintegral by more than `0.00247`.

This is a useful failed near-candidate: satisfying a large proper divisor of `D` is not close enough. The decisive target is full exponential-circuit equality

```text
C(w) = n(2^A-3^k).
```

## 4. Negative-cycle perturbations: exact one-pulse reduction

PR #47 proves, at proposed level, an exact formula for repeating a negative accelerated cycle and increasing one valuation by `delta`. If the negative fixed point is `z`, the repeated word has parameters `(kr,Ar)`, and

```text
D=2^(Ar+delta)-3^(kr),
```

then the perturbed fixed point is

```text
n = z - (2^delta-1)(3z+1)3^(kr-1)/D.
```

Hence integrality requires

```text
D | (2^delta-1)(3z+1).
```

An exact scan of 720,000 reduced candidates from the known negative 3- and 11-cycles through repetition 20,000 finds only the trivial positive cycle `n=1`.

### New synthesis: distributed-pulse circuit

The exact next object is not another pulse-location scan. For a base repeated word of length `N=kr`, prefix exponents `A_j`, and pulse increments `delta_i` placed before selected suffixes, define

```text
Delta_j = sum_(i<j) delta_i,
Delta   = sum_i delta_i.
```

The perturbed cycle constant and denominator are exactly

```text
C_delta = sum_(j=0)^(N-1) 3^(N-1-j) 2^(A_j+Delta_j),
D_delta = 2^(Ar+Delta)-3^N.
```

Thus a distributed-pulse positive cycle is exactly a finite exponential-circuit solution

```text
C_delta = n D_delta,
```

plus exact valuation replay.

The useful computational structure is sequential pulse insertion. Raising the valuation at boundary `i` by `delta` multiplies every later prefix term by `2^delta`; the change is

```text
(2^delta-1) * suffix_constant(i),
```

where the suffix constant is taken after all earlier inserted pulses. Upper-triangular affine summaries therefore support a meet-in-the-middle search over pulse packets with exact joins modulo the full `D_delta` and a final directed quotient interval.

This is a materially larger offense than the single-pulse family while remaining finite, proof-producing, and integer-first.

## 5. Linear-height quotient refund: strongest active positive architecture

Issue #43, now under two independent attacks, reverses the quotient-extinction inequality. For the linear schedule

```text
t_j=B+16j,
```

one complete 256-transition stage has

```text
A(B)=1792B+3657472,
E(B)=2816B+5792512.
```

For sufficiently large divisible `B`,

```text
3^A(B) > 2^E(B+4096).
```

Every finite pair of stage words then admits infinitely many positive integral quotient lifts, and the next quotient is affine expanding in the lift parameter.

The missing implication is causality:

```text
current finite state and quotient
 -> choose next word and lift
 -> preserve a forward invariant ordinary set
 -> strict physical growth forever.
```

Pairwise solvability is not enough because a choice may encode future inverse-limit digits. The intended proof object is finite control plus one unbounded quotient/carry and a canonical top-boundary condition.

This lane is presently the strongest explicit escape from the verified fixed-width collision no-go.

## 6. H direct search and structural frontier

The H branch now reports exact direct search through all positive A-state candidates `n<=2^65-2` represented by 62,608,681 ghost residue classes:

- no positive H cycle;
- no 300-block survivor;
- maximum tested life 34;
- no periodic block-word cycle through word length 14.

This is a major finite negative packet, not an asymptotic theorem. The live H target remains either:

- a primitive endpoint-product exclusion; or
- an ordinary reset-renewal ray that keeps endpoint product critical or supercritical.

## 7. Revised priority order

### Direct positive offense

1. linear-height quotient-refund invariant;
2. distributed-pulse negative-cycle meet-in-the-middle;
3. full-denominator critical-scale mechanical circuit;
4. DFA sanctuary with genuine word-boundary memory;
5. cross-cycle regeneration potential or exact return.

### Decisive negative theorems

1. independently integrate PR #44's frozen-class verdict;
2. prove a height-augmented centered one-counter finite trap, or construct its seed;
3. classify whether quotient refund inevitably preloads future digits;
4. prove the H endpoint-product gate;
5. bound or exploit distributed-pulse circuit rank.

## Bottom line

The new evidence does not support claiming a counterexample. It does support a sharper strategic conclusion:

> Fixed-state, fixed-modulus, fixed-width, one-pulse, small-cycle-window, and low-complexity sparse-cycle constructions are increasingly closed. The live positive frontier requires one unbounded ordinary resource—quotient, top boundary, pulse packet, or changing phase—controlled causally from finite data.
