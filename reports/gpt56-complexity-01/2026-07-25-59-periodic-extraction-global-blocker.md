# Session report — periodic extraction and the global Collatz blocker

**Date:** 2026-07-25  
**Agent:** `gpt56-complexity-01`  
**Issue:** `#59`  
**Branch:** `agent/gpt56-complexity-01/59-periodic-extraction-dichotomy`  
**Objective:** stop finite-prefix work, identify the exact global inference missing in the repository, and close one exhaustive infinite class

## 1. Sources inspected

The pass read the root operating model and the current high-signal repository state, including:

- pass-5 global cartography in PR `#38`;
- the ordinary-refund-machine crosswalk;
- PR `#33` and its independent PR `#44` review;
- fixed six-branch, changing-height refund, run-core, H, centered, sanctuary, and cycle packets;
- the new ordinary-extraction audits in PRs `#56` and `#57`.

No source theorem is promoted by this report.  The statements below distinguish direct reconstruction, branch-qualified proposed work, and strategic interpretation.

## 2. Independent reconstruction of the missing inference

For nested nonempty positive-integer sets

\[
S_0\supseteq S_1\supseteq\cdots,
\qquad m_n=\min S_n,
\]

one has

\[
\bigcap_nS_n\ne\varnothing
\quad\Longleftrightarrow\quad
(m_n)\text{ is bounded}
\quad\Longleftrightarrow\quad
(m_n)\text{ is eventually constant}.
\]

The proof is elementary: nesting makes `m_n` nondecreasing; a common seed bounds it; a bounded nondecreasing integer sequence stabilizes; and its stable minimum lies in every earlier set by nesting.

This independently confirms the logical core of PRs `#56` and `#57`.

The repeatedly missing inference is therefore not an unspecified compactness theorem. It is the archimedean statement

```text
sup_n m_n < infinity.
```

Finite compatibility, an inverse-limit path, and conditional growth do not imply this bound.

## 3. Why the shared amplifier architecture cannot supply extraction

PR `#56` supplies a universal affine countermodel: arbitrary nested nonordinary dyadic cylinders can be decorated with exact positive affine maps whose legal steps expand by any prescribed factors. Every finite prefix still has infinitely many positive representatives, while no ordinary integer survives all depths.

PR `#57` supplies the Collatz-native instance `(1110)^infinity`, with exact block map

\[
T^4(x)={27x+19\over16}
\]

and unique seed `-19/11`.

I reconstructed the latter directly.  It proves that all of the following may hold simultaneously:

```text
finite positive realizability at every depth;
unique exact 2-adic path;
computable periodic schedule;
strict supercritical multiplier;
conditional divergence if ordinary;
```

while the ordinary positive survivor set is empty.

Thus there is no universal extraction theorem based only on the common cylinder/refund data used by the current positive architectures. A successful proof must exploit a special global identity of one source system.

## 4. New theorem proved in this pass

`T-7401` classifies every eventually periodic shortcut-Collatz parity itinerary.

For a period word `w` of length `L`, with `s` odd branches,

\[
T_w(x)={3^s x+C_w\over2^L}.
\]

Finite parity-cylinder bijectivity makes the infinite parity code injective on `Z_2`. Hence the unique seed of `w^infinity` is fixed by the period map and equals

\[
\boxed{x_w={C_w\over2^L-3^s}.}
\]

This gives the exact dichotomy:

```text
3^s>2^L:
    x_w<0;
    no positive ordinary realization;

3^s<2^L:
    positive ordinary realization
    iff 2^L-3^s divides C_w;
    then the itinerary is a positive integer cycle.
```

A finite preperiod merely asks for one exact positive integer preimage of that cycle point. Therefore every positive ordinary eventually periodic itinerary eventually cycles.

## 5. Why this is real progress rather than another encoding

The theorem closes the complete eventually periodic parity class at all periods.

It is not:

- a period cutoff;
- a finite search;
- a symbolic approximation;
- an `if an orbit exists, it grows` theorem;
- or another inverse-limit representation.

On the negative side it eliminates an exhaustive class strictly narrower than Collatz. On the positive side it gives the complete finite denominator condition and therefore a cycle certificate after replay.

It also shows that ordinary extraction and full-denominator divisibility are not separate issues on a periodic tail: they are the same equation.

## 6. Blunt repository assessment

### Genuine progress

The following are not circular:

1. PR `#33`/`#44` proposes and independently reconstructs a negative ordinary-extraction theorem for one entire corrected-stage architecture.
2. Several periodic, morphic, fixed-state, fixed-support, and fixed-pulse-cone classes are eliminated uniformly.
3. False real/`Q_2` and raw-stack bridges were discovered and corrected rather than accumulated.
4. The cycle program has exact full-denominator and mixed-place closure criteria, even though no candidate reaches them.
5. The present theorem closes every eventually periodic raw parity schedule.

### Treading water relative to the full objective

The positive refund/amplifier lanes have not crossed extraction. More work of the following forms does not change that:

```text
longer finite survivors;
stronger local quotient growth;
more refund capacity;
finite SCCs;
entropy or complexity lower bounds;
fresh-prime consequences conditional on existence.
```

Those results may constrain a future witness, but they do not establish one common initial integer.

The correct summary is:

```text
negative exhaustive-class program:
    genuine and sometimes decisive;

positive ordinary-existence program:
    exact state reductions and strong conditional dynamics,
    but no least-root bound and no extracted seed.
```

## 7. The two remaining global blockers

### Ordinary divergent-orbit side

For each fixed strict architecture, decide

\[
\boxed{m_n\text{ bounded}}
\qquad\text{or}\qquad
\boxed{m_n\to\infty}.
\]

The first gives a fixed ordinary seed; the second eliminates the architecture.

The smallest current target is the six-branch rational-base machine from PRs `#45`, `#50`, and `#57`.

### Positive-cycle side

For one finite word, prove

\[
\boxed{C(w)=n(2^A-3^k)}
\]

for a positive integer `n`, then replay every valuation.  Proper-factor divisibility and a near-integer real quotient are not intermediate versions of this equality.

## 8. What remains open

This pass did not:

- decide the six-branch least-root sequence;
- produce an ordinary divergent seed;
- produce a nontrivial positive cycle;
- eliminate genuinely aperiodic seed-first machines;
- or resolve Collatz.

The next work should attack one least-root sequence or the whole denominator. It should not return to prescribed periodic schedules, because `T-7401` has now closed them completely.
