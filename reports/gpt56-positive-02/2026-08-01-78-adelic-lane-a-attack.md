# Session report — fixed-integer adelic attack on Lane A

**Agent:** `gpt56-positive-02` (`GPT-5.6 Pro`)  
**Issue:** #78  
**Draft PR:** #80  
**Branch:** `agent/gpt56-positive-02/least-counterexample-global`  
**Date:** 2026-08-01

## Objective

Attack the full remaining Lane-A object rather than derive another scalar drift consequence.

The assumed ordinary orbit has

\[
D_k\ge0\quad\forall k,
\]

logarithmically growing mean coefficient bank, sparse fixed-band returns, and a cusp subsequence on which the same ordinary source forces

\[
r_j=n,
\qquad
s_j=T^{k_j}(n),
\]

\[
\log r_j/k_j\to0,
\qquad
\log s_j/q_j\to0.
\]

The requested research interfaces were product formula, S-units/Subspace, Ridout, and source/endpoint incompatibility.

## Frozen repository state inspected

```text
PR #77  1c8ed3c7edbb190d59490d2f92a3c342c2f856eb
PR #80  fbc178758314d5e908f28aedf7b356938935b256
PR #81  5609d8b76f8b0b1e2b9a3b3e9e1122b67732e648
PR #83  674f36cb6f2bb620e6fab21da2838c7c52a5072d
```

The source PR #80 head above is the pre-session head. PR #81 supplies the canonical source/end ray and exact source-escape target. PR #83's newest material is primarily the corrected shifted-denominator and sparse Lane-B family.

`R-6501` was independently re-read before this attack. Its conclusion remains valid: scalar surplus profiles and inverse-limit compatibility are not ordinary extraction.

## Sources inspected

1. Evertse--Schlickewei--Schmidt, *Linear equations in variables which lie in a multiplicative group*, Annals 155 (2002), Theorem 1.1.
2. Ru--Vojta, *Schmidt's subspace theorem with moving targets*, Inventiones 127 (1997), for applicability positioning.
3. López--Stoll, arXiv:2101.12747, for the already imported rational critical-density boundary.
4. Kramer, arXiv:2607.10041, for the start/end exponent-code diagnostic boundary.

The ESS theorem statement and explicit constant were checked in the primary paper. Ru--Vojta is not invoked positively; the native sequence fails the needed independent-moving-target role because it lies in one explicit moving plane.

# New result 1 — exact affine-plane saturation (`L-6505`)

For one actual cusp prefix,

\[
A+n3^q=s2^k,
\qquad
\gcd(A,6)=1.
\]

With

\[
X=(A,3^q,2^k),
\]

three determinant-one form systems at `infinity,2,3` give the exact product

\[
\prod_{v,i}|L_{i,v}(X)|_v
={A\over2^k3^q}|s|_2|n|_3.
\]

All-prefix supercriticality gives

\[
A\le(q/3)3^q.
\]

At the zero-rate cusp,

\[
\prod_{v,i}{|L_{i,v}(X)|_v\over\|X\|_v}
\le H(X)^{-4+o(1)}.
\]

This is genuine Subspace-scale smallness. However every point lies identically in

\[
X_0+nX_1-sX_2=0,
\]

with moving coefficient height `o(h(X))`.

After quotienting that plane, the exact coordinate product for

\[
Y=(3^q,2^k)
\]

is

\[
|Y_1Y_2|_\infty|Y_1Y_2|_2|Y_1Y_2|_3=1,
\]

so the normalized product is exactly `H(Y)^(-2)`. No fixed power saving remains.

## Verdict

A standard moving-target application returns only the physical affine plane already known. A second independent form is necessary.

# New result 2 — exact Ridout boundary (`R-6502`)

The fixed-source approximation is

\[
\left|n+A/3^q\right|_2=2^{-k}|s|_2.
\]

At the cusp, approximant height is `3^(q+o(q))` while the error is `3^(-q+o(q))`. The exponent is only `1+o(1)`, below a Roth/Ridout exponent exceeding two.

This route does not fail because of an unoptimized constant. It fails at the exponent level.

# New result 3 — multiplicative-rank escape (`T-6507`)

Define

\[
U_j={A_j\over s_j2^{k_j}},
\qquad
V_j={n3^{q_j}\over s_j2^{k_j}}.
\]

Then

\[
U_j+V_j=1,
\qquad
U_j,V_j>0.
\]

Also

\[
V_j=P_{k_j}^{-1},
\]

where `P_k` is the exact odd-step correction product. After thinning to increasing odd counts, the `V_j` strictly decrease, so the solutions are distinct and nondegenerate.

Let `r_J` be the rank of the subgroup of `(Q^*)^2` generated by the first `J` pairs. ESS Theorem 1.1, with

```text
m=2,
(6m)^(3m)=12^6=2,985,984,
```

gives

\[
\boxed{r_J\ge {\log J\over12^6}-1.}
\]

If `S_J` is the set of primes dividing

\[
6n\prod_{j\le J}A_js_j,
\]

then

\[
\boxed{|S_J|\ge {\log J\over2\cdot12^6}-{1\over2}.}
\]

Moreover

\[
\gcd(A_j,s_j)\mid n.
\]

Outside fixed source primes, fresh numerator and endpoint prime mass are disjoint at each cusp level.

## Classes closed

- fixed finite-rank multiplicative cusp families;
- fixed finite prime-support cusp families;
- finite multiplicative-alphabet parametrizations;
- fixed-`S` source/end correction pairs.

## Gap

The affine numerators can pay all rank escape. ESS does not transfer fresh rank into a positive endpoint-height rate.

# New result 4 — bilateral zero-lift propagation (`L-6506`)

For one cusp prefix of length `K`, every fixed `0<epsilon<alpha`, and all sufficiently large cuts

\[
t_0\le t\le(\alpha-\varepsilon)K,
\]

the actual internal state is both:

```text
canonical endpoint of the prefix;
canonical source of the suffix.
```

Therefore

\[
\boxed{x_t\le\min(3^{q_t},2^{K-t}).}
\]

For

\[
B_t=\min(3^{q_t},2^{K-t}),
\]

distinct ordinary states give the exact capacity gate

\[
\boxed{
\#\{t:B_t\le Y\}
\le\lfloor Y\rfloor-n+1.}
\]

This propagates the fixed source/end condition through a linear interval of cuts. Current estimates do not force overload because the capacities remain exponentially large.

# Failed closure attempts

## 1. Scalar combination

Rejected by `R-6501`; no new contradiction was inferred from mean bank, records, or sparse low-band returns.

## 2. Fixed-target Ridout

Exponent `1+o(1)`, below threshold.

## 3. Moving-target Subspace theorem

The full power saving is exactly one known moving affine plane. Quotient gives product-formula equality.

## 4. Fixed-`S` S-unit finiteness

Successful only as a class exclusion. The actual cusp is forced to escape finite rank rather than disappear.

## 5. Bilateral counting

Produces an exact Hall-type capacity inequality, but no current bound makes it fail.

# Strongest exact new lower bound

For the first `J` cusp levels,

\[
\boxed{
\operatorname{rank}
\langle(U_1,V_1),\ldots,(U_J,V_J)\rangle
\ge {\log J\over12^6}-1.}
\]

Equivalently, the cusp products `A_js_j` involve at least

\[
{\log J\over2\cdot12^6}-O_n(1)
\]

distinct rational prime places.

This is the first unconditional quantitative Diophantine lower bound in this branch on the full cusp sequence beyond scalar surplus.

# Single missing inequality

The cleanest remaining target is a numerator-rank-to-endpoint-height transfer:

\[
\boxed{
\text{simultaneous zero canonical source/end rates}
\Longrightarrow
\operatorname{rank}\langle(U_j,V_j):j\le J\rangle=o(\log J).}
\]

This would contradict `T-6507` and close Lane A.

Equivalent alternatives are:

1. a second independent quotient form with a fixed Subspace power saving;
2. a direct lower bound `limsup log(s_j)/q_j>0`;
3. a bilateral capacity overload at one subexponential scale.

No inspected theorem supplies any of these.

# Files added

```text
research/least-counterexample-global/claims/L-6505-adelic-affine-plane-saturation.md
research/least-counterexample-global/claims/L-6506-bilateral-canonical-split.md
research/least-counterexample-global/claims/T-6507-cusp-multiplicative-rank-escape.md
research/least-counterexample-global/claims/R-6502-subspace-ridout-applicability-boundary.md
reports/gpt56-positive-02/2026-08-01-78-adelic-lane-a-attack.md
```

Metadata, literature audit, inventory, README, global blocker, PR body, and issue/PR comments are updated separately.

# Honest conclusion

Lane A is not closed.

The new work proves that its cusp must escape every finite-rank multiplicative architecture and that the obvious product-formula smallness is exactly linearly degenerate. The remaining obstruction is now one explicit transfer inequality rather than an unspecified appeal to Diophantine approximation.

No proof of Collatz, counterexample, positive cycle, or candidate is claimed.
