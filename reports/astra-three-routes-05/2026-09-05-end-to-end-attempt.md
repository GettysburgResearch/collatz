# Fifth all-route end-to-end attempt

Agent: `astra-three-routes-05` (GPT-6 Pro), 2026-09-05.
Frozen parent of PR #92: `6266bd8f73ce2f8488c02fbe29f34fd2b4f289bc`.
Main re-queried at `9704bcf1ff33cc9e2b729e0c40137a1e55b95397`.

All three requested routes were pursued. No complete Collatz proof was obtained.
All new theorem-level claims remain PROPOSED pending independent review.

## Actual advance

The shared rank has a sharp square-root sublevel count and summable powers
exactly above exponent 1/2. This provides both a natural mass scale and a
complete, smaller spatial list for lower-rank merging witnesses.

Equal-rank A edges cannot decrease the ordinary number. An explicit integer
refinement therefore orients every nonincreasing R edge, with no equal-rank
cycle loophole. Combined with the spectrum, it gives a finite clock for the
larger safe region and an all-source polynomial resolvent tail.

The global induced mass candidate R^(-p) still fails: a proved infinite family
has both source and endpoint unsafe and produces unbounded pointwise inverse
ratios. The failure is preserved at the induced, not only raw, operator.

The constructive result is an exact two-mode repayment theorem. It starts
with an inactive minimizing component, accounts for the ternary precision
lost on a different mode, and proves net common-rank decrease after an
explicit sufficient residence in the matching mode. The sharp 3->2 family
has an arbitrarily large COMMON-rank spike, then a net drop while the ordinary
number increases throughout. At t=3 the exact sequence is

    461479 -> 778747 -> 1108804,
    R: 7887684528 -> 67383853056 -> 187388721.

Its future-word hypothesis is indispensable: n=103 has the same ternary depth
and initial mode, but next mode four. No universal repayment selection is
inferred from CRT nonvacuity.

## Closing gaps, rather than substituted weaker goals

Mass closure still needs control on the actual unsafe-return population;
source closure still needs a proved total temporal meeting rule; termination
still needs a complete physically valid cover. The new data do not supply
any of these universally. The explicit failures prevent falsely claiming
that safe-state elimination or high ternary precision alone completes them.

## Verification actually performed

Generator and separately written verifier pass with byte-identical reports.
The verifier also passes under Python -O and rejects eight resealed corruptions.
Coverage and proof-versus-experiment boundaries are recorded in the new
experiment README. No large previous experiment or full-repository validator
was run. Both implementations have the same author; no independent mathematical
acceptance or formal proof is claimed.

Semantic SHA-256:

    45888e331821905d73141de70258b9cbfc03df94ec58208a94b5d66f9d687aee

## Publication result

No remote commit or PR edit was made in this session. The available GitHub
connection could read the repository but exposed no write action. Discovery
returned the existing installed plugin, not an alternative publisher. Direct
`git ls-remote` failed because github.com could not be resolved.

The complete addition-only continuation is delivered as a patch with an exact
base-head guard, file manifest, PR update text, and a publication helper.
The helper was not executed. Its patch round-trip was checked on a local
fixture; this is not falsely reported as a full private-checkout audit.
Earlier proof/experiment bodies, canonical statuses, main and other PRs are
unchanged. No route is narrowed or dropped.
