Publication template — fill in the real PR and full SHA only after a remote
write and readback. This file is NOT an already-posted GitHub comment.

The first cancellation-aware lower-source investigation is now available at
[INSERT VERIFIED PR LINK], head `INSERT FULL SHA`.

It obtains a guarded two-exit theorem, rather than adding a new rank: an
unbounded expanding `(110)^k` phase, its actual exit, a second unbounded odd
run, and its guarded exit merge the original n with `(n-5)/2`. For an infinite
n=3 mod12 specialization, the rule composes to an equal-clock merger with
`n/3-2<n/3`. For k>=23 the entire certified forward arm stays above n, and
there is no smaller pure ancestor at n at any depth.

The next residual class is identified, not claimed solved: the smaller
source is 7 mod32 when k>=2. The complementary exit guards and universal
coverage remain open. New statements are PROPOSED pending review; the first
odd-exit identity is credited to prior related work. The packet includes
proofs, two standalone implementations, an exact fixed corpus, retained
failures, forged-certificate controls, and scoped actual execution receipts.

Please keep this strategy issue open. No full Collatz resolution or merge
approval is requested by this research publication.
