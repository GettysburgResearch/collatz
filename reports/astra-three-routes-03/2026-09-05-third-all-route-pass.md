# Third continuation of PR #92: all routes retained

Agent: `astra-three-routes-03` (GPT-6 Pro), 2026-09-05.
Frozen parent: `48e043c2822dbeea4f91c801ac2d3fc531af1466`.
Existing branch: `agent/astra-three-routes-01/three-route-offense`.

The owner requested another ambitious continuation of all three routes. No
route is narrowed or dropped. All new theorem-level claims are PROPOSED
pending independent review. No full Collatz proof was obtained.

## New work and attempted closing steps

Route 1 now represents the actual survivor set by the first ordinary member
of every finite parity cylinder. Triple grouping proves an exact signed
curvature decomposition of its residue bias. Only first-survivor color 2
can contribute positively. The inverse statement quantifies the necessary
boundary concentration when bias is excessive. A universal moving-floor
inequality is proved, but the cofinal fixed-floor charge remains open. We
attempted positive-only and first-curvature majorants; the finite certificate
records where each becomes too weak while the full signed bound still passes.

Route 2 extends orbit sparsity and reciprocal control to all finite SIMPLE
paths, explicitly paying for the last q odd states. It bounds the finite
number of coefficient records in a near-source window. A supplied finite
SC bound N on that window gives a literal R(n)N descent-or-repetition clock.
This is not an unconditional SC or cycle theorem and does not disguise a
source-location bound as an unproved time bound.

Route 3 replaces separate nonlinear features by arbitrary joint functions of
finitely many POLYNOMIAL valuation and congruence observations. A rational
negative cycle center can be chosen away from every polynomial zero and
integral at every observed prime. Positive ordinary CRT shadows preserve the
whole vector while logarithmic size grows linearly in shadow duration. This
rules out the entire scalar class for fixed shortcut blocks and even for
selectors taking a bounded number of whole-run macros. The selector may
inspect all of n. Infinite feature information and unbounded repayment are
not excluded; the record-set architecture is retained as a conditional
constructive alternative.

## Actual checks

Generator and separate verifier pass. Eight resealed tamper reports are
rejected. Coverage: 16,382 cylinders, 65,528 ordinary lifts, 26 all-source
finite-time interval rows, 6,146 record subsegments, 24,101 product positions,
18 joint-feature shadows, 189 packets, eight integer certificates, and the
whole core 1..64. No finite trend is extrapolated into an all-time theorem.

Semantic digest:

    dd5c52ff9a06ae728f7f10d84db655ae3324ca2318531b130ff66eced5b7feb6

Both programs have the same author. Independent implementation is not
independent mathematical acceptance. Prior inverse-cone experiments, external
Lean/payloads, GitHub workflows, and the full-repository validator were not run.

## Scope and handoff

The pass is additive under pass3, X-ASTRA3-003, and this report, with a parent
README pointer. Existing source proofs, canonical statuses, main, other PRs,
permissions, and workflows are preserved. Publication follows on the existing
PR #92 branch; the exact final head is recorded in the PR publication comment.

The next actual theorem remains all-depth signed boundary control, universal
local SC windows plus cycle exclusion, or a total unbounded repayment/rank
mechanism. The cross-route proposal is to use record-set or full digit memory
to control the signed charge. No such coupling is assumed proved here.

## Final publication replay repair

The final post-upload generator `--check` exposed a tuple/list comparison bug:
the in-memory feature dictionaries used tuples, while the canonical JSON
necessarily used lists. Generating JSON reproduced the existing report and
semantic digest exactly, and the separate verifier and eight tamper tests
passed. The generator now normalizes its in-memory payload through canonical
JSON before comparing. Both documented commands were rerun successfully after
this repair. No mathematical result or canonical certificate data changed.
