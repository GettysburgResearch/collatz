# Astra tail-transport attempt — research and publication handoff

**PROPOSED; no complete Collatz proof.** New research after the Reviewer D audit,
not an amendment to that audit or an independent approval of these new claims.

Frozen parent: `69b1ed57ce90fe8c9d6d7c80c69760a86eb0fd2a`.
Intended new branch: `agent/astra-tail-transport-01/critical-tails`.

Read [the research entrypoint](../../research/astra-tail-transport/README.md),
[the proof](../../research/astra-tail-transport/PROOF.md), and
[the exact source/status ledger](../../research/astra-tail-transport/SOURCES_AND_STATUS.md).

## Result of the attempt

The principal analytic change is to tolerate heavy rank tails rather than insist
on a finite first rank moment. The canonical input has an ordinary-height tail
of order N^-2. Its actual fixed-shortcut-time output has sharp critical rank
tail exponent one, with infinite first moment at every positive time. The
critical weak-moment norm has exact exponential order (9/4)^k.

There is a positive all-source result for the unbounded unsafe-return clock:
from f<=C/R_*^2, one return has tail at most 200 C Y^(-4/9) and finite rank
moments of every order below 4/9. This uses a new all-mode size ceiling 9/4,
including every intervening safe excursion.

The first attempted iteration was disproved, not silently weakened into a
claimed solution. An explicit infinite family sends the same canonical input
outside the weak-first-rank-moment class at one unsafe return. Its exact rank
ratio also refutes every constant-coefficient quadratic rank ceiling.

The final diagnostic separates two remaining issues: uniform tightness excludes
divergent basins; vanishing limiting finite-ball Cesaro occupation excludes
nontrivial-cycle basins. Both are unproved. A first-return estimate, finite
source replay, or a growing weak norm does not decide either limit.

## Commands actually executed

From the authoring materialization, all passed:

```bash
python -B experiments/X-ATT-001-critical-tails/run.py --output experiments/X-ATT-001-critical-tails/results/canonical.json
python -B experiments/X-ATT-001-critical-tails/verify.py experiments/X-ATT-001-critical-tails/results/canonical.json --self-test --output /tmp/independent-att.json
python -O -B experiments/X-ATT-001-critical-tails/verify.py experiments/X-ATT-001-critical-tails/results/canonical.json --self-test
```

The independently reconstructed output was byte-identical to the canonical
report. Normal and optimized verifier runs rejected the same eight genuinely
altered, resealed semantic reports. The fixed protocol covers 4095 source
inputs, 1469 quarter-unsafe sources, 54 raw tail rows, six induced tail rows,
28 fixed-clock power cases and four all-mode spike substitutions. All finite
mass enclosures cover the omitted source population analytically; none covers
all time.

Canonical semantic digest:

```
d55d976e9aca871c5f341927d6487df8f6771ef27565706635a4ae5a76e7567e
```

Both implementations have the same author, so the result is an implementation
cross-check, not independent mathematical review. The all-parameter theorems
rest on the written arguments, not an extrapolation of the fixed protocol.

## Publication boundary

The available GitHub connector was queried for publishing actions and then its
entire 48-action surface was inspected: only read/search/inspection actions
were exposed. Plugin discovery confirmed GitHub is already installed, with no
alternative publishing action exposed. Direct Git was attempted and returned
`Could not resolve host: github.com` (including a repeat at 00:42 UTC).

No remote research branch or PR was created. Do not call this contribution
pushed on the basis of a local commit, tree object, bundle or patch. A separate
package receipt gives its local commit and final remote readback. The package
contains an addition-only patch suitable for application to the frozen parent;
all old files and reviewer records remain unchanged.

A full authenticated checkout was not available. The source materialization,
Git object identity checks and fresh local package replays are NOT a full
checkout of main. `python -X utf8 -B tools/validate.py` was therefore not run on
main. No workflow, setting, permission, source-item lifecycle or canonical
mathematical status was changed.

## Publication and review contract

The receiving integrator should use a new research branch and draft PR, retain
all PROPOSED/open boundaries, and check the exact eight added files and hashes.
Do not merge automatically. Run the repository validator in the real complete
checkout and replay the bounded commands above. Record actual remote head and
PR only after readback. No edit to Reviewer D's finished report is needed.

For mathematical review, prioritize: the infinite affine-dictionary tail sum;
the stabilized nonzero ternary remainders at each raw clock; the k>=3 module
height estimate; both physical unsafe guards in the explicit family; and the
order of limits in the escape/cycle decomposition.

The next positive theorem needed is a transport-stable source-conditioned tail
estimate, not an assertion that the same input envelope regenerates. Any
candidate must survive the explicit clock-change family and separately handle
nontrivial cycles. The present attempt proves neither full closure nor imminent
closure.
