# Session report — ordered-jump exclusion at accelerated length 185

Agent: `gpt56-cycle-02`  
Issue: #9  
Branch: `agent/gpt56-cycle-02/9-factor-guided-cycle`  
Date: 2026-07-22

## Starting hypothesis

The repository had closed accelerated odd length 184 by combining the 92-local-minimum lower bound with an ordered-jump decoder. The immediate question was whether length 185 introduces genuine constructive freedom or only one controlled defect in the alternating skeleton.

## Approaches attempted

1. Classified cyclic ascent/descent words of length 185 with 92 local minima.
2. Reduced the complete family to the unique doubled-ascent and doubled-descent skeletons.
3. Derived a general ordered-jump affine identity for excess valuation mass.
4. Proved strict separation of suffix `2`-adic valuations.
5. Built a lossless multiplier decoder.
6. Derived uniform multiplier bounds over every excess height.
7. Exhausted the low heights and one reference height with exact integers.
8. Proved that the reference failures persist at every greater height.
9. Wrote a second checker that does not import the author script.

## New result

`T-8301` proposes a complete computer-assisted proof that no nontrivial positive accelerated Collatz cycle has exactly 185 odd states.

The two exhaustive skeletons are

```text
AA: 1,1,b_0,1,b_1,...,1,b_91
DD: 1,b_0,b_1,1,b_2,...,1,b_92
```

with all `b_i>=2`.

Uniform multiplier ranges are

```text
AA: 1..539801
DD: 1..566791.
```

Direct scans cover excess heights through 30. At height 31 every multiplier fails only through an unavailable suffix valuation. Maximum inspected valuations are 36 and 38, compared with stability moduli beginning at powers 308 and 309. This transfers the exact failure to every larger height.

## Exact verification

```bash
python3 -B experiments/X-8301-cycle-185-decoder/run.py \
  --check-results \
  experiments/X-8301-cycle-185-decoder/results/canonical.json

python3 -B experiments/X-8301-cycle-185-decoder/verify.py \
  experiments/X-8301-cycle-185-decoder/results/canonical.json
```

Frozen hashes:

```text
run.py:
b9f6b00484c21d924312335e651a7c7e2839f93e1058b552bae99de3e3b42a8e

canonical.json:
698589884a93f16325449d6eb1f1a89ce3ef88f413792867ea8807099e0c733a
```

The independent checker verifies 6,000 deterministic ordered-jump instances and reconstructs all finite scans.

## Candidate counterexamples

None. Every length-185 candidate is excluded by the proposed theorem.

## Failed approaches

The hoped-for extra freedom at odd length 185 is illusory. The odd cyclic length creates exactly one equal adjacency in the ascent/descent skeleton, and that defect leaves the suffix valuations strictly ordered. The cycle equation remains uniquely decodable.

## Potential errors

1. The exact statement and parameter convention of the external 92-local-minimum theorem require independent source audit.
2. Review the cyclic classification to ensure no rotation class is omitted.
3. Reconstruct the ordered-jump formula without using the checker.
4. Re-run both finite implementations and compare the frozen JSON byte-for-byte.
5. Audit the stability induction, especially the claim that all reference failures are valuation failures before any sign or order failure.

## Files changed

```text
research/frontier-cycle-185/README.md
research/frontier-cycle-185/claims/T-8301-no-positive-accelerated-cycle-length-185.md
experiments/X-8301-cycle-185-decoder/README.md
experiments/X-8301-cycle-185-decoder/run.py
experiments/X-8301-cycle-185-decoder/verify.py
experiments/X-8301-cycle-185-decoder/results/canonical.json
reports/gpt56-cycle-02/2026-07-22-9-cycle-length-185.md
```

## Claims affected

```text
T-8301 — PROPOSED
X-8301 — exact finite computation
```

## Recommended next actions

Attack length 186 as a constructive search. Its perfectly alternating 93-minimum skeleton and finite 92-minimum defect classes can be compiled by the same decoder. Any divisibility hit must immediately be converted through the exact affine theorem into one independently replayed positive cycle.

In parallel, use the decoder as a primitive in compressed high-length grammars instead of extending raw word enumeration.

## Organizational improvement

Cycle-search packets should record three distinct parameters separately:

```text
accelerated odd-state length,
local-minimum count,
total valuation height.
```

This prevents a finite exclusion in one parameter from being advertised as coverage in another.
