# Session report — attack the negative-three chart and critical mechanical denominator together

```text
Agent: gpt56-cycle-02
Issue: #9
Branch: agent/gpt56-cycle-02/9-factor-guided-cycle
Date: 2026-07-23
```

## Starting objective

The user asked for simultaneous attacks on:

1. the exact negative-three two-branch ordinary chart; and
2. the full-denominator critical mechanical compiler of PR #45.

The positive target remained one explicit nontrivial cycle or one positive ordinary all-time chart path.

## Repository inspiration

The live sweep identified three useful interfaces:

- PR #49 reduces its refunded collision architecture to one deterministic complement counter and proves growth after all-time definedness.
- PR #51 isolates the small negative-three chart but leaves its top-boundary closure open.
- PR #34 supplies a lossless cross-prime excess-path compiler, while PR #45 supplies a trillion-letter mechanical SLP and a proper-factor repair.

The new packet combines the last three rather than running another unrelated census.

## Failed shortcut preserved

I first tried to rank successive expanding-branch states. That argument was false because the expanding chart branch may follow itself. In the shifted chart coordinate,

```text
x=61 -> 69
```

is a legal expanding-to-expanding step. Any proof that silently assumes a contracting branch between consecutive expanding branches is invalid. No extinction claim is made.

## New exact reductions

### 1. Chart-to-cycle compiler

A chart symbol `e` is exactly the accelerated valuation pair `(2-e,2)`, and

```text
2^(4-e) x' = 9x+3e.
```

A length-`m`, weight-`s` chart word therefore has

```text
k=2m,
A=4m-s,
D=2^A-3^k.
```

Its physical cycle numerator is `D+2C_chart`, so full divisibility is exactly `D|C_chart`.

For PR #45's `(k,A)`, the corresponding chart parameters are

```text
m=1,574,985,702,418,
s=1,307,356,254,663.
```

Thus the two requested lanes share one exact denominator.

### 2. Hierarchical mechanical replacements

For Farey neighbors `p/q<r/s`, lower mechanical blocks concatenate to the mediant word, and reversing them changes only one `01/10` pair. The accelerated numerator commutator is exactly

```text
-2^(r+s-1) 3^(q-1).
```

At the PR #45 scale this is

```text
-2^217976794616 3^753110839880.
```

This gives factorized replacement columns at every Euclidean recursion scale.

### 3. Quotient rather than numerator Hensel digits

If `p^d||D` and `p^d|C`, a hypothetical ordinary quotient must satisfy

```text
C/D == (C/p^d)(D/p^d)^(-1) mod p^e.
```

This is the correct next lifting coordinate. It permits direct comparison with the unique integer in a directed real interval and does not identify unrelated completions.

## Frozen paired-chart construction

The lower mechanical chart word was repaired with 41 disjoint adjacent swaps. Exact replay gives

```text
C_chart=0 mod 1,465,129,870,107,858,983.
```

Its real fixed point is

```text
1,567,441,266,425,753,353,472,608.002804312818...
```

and is not integral. The five prime-power quotient digits combine to

```text
1,051,915,013,441,653,484 mod M,
```

which disagrees with the sole real integer candidate modulo `M`. The object is rigorously rejected and receives no candidate ID.

## Unexpected order-cover result

The five multiplicative orders of two are

```text
3, 95, 70, 14,295, 3,099,962,
```

with lcm

```text
5,893,756,253,070 > A-k.
```

Thus the existing 61-bit factor set is already combinatorially lossless if the full factorwise prefix-excess paths are retained. The missing issue is not word ambiguity; it is annihilation at omitted denominator factors and correct quotient lifting.

## Candidate counterexamples

None. No complete denominator identity or all-time ordinary path was found.

## Files added

```text
research/critical-chart-mechanical/
experiments/X-8302-critical-chart-mechanical/
reports/gpt56-cycle-02/2026-07-23-9-chart-mechanical-bridge.md
```

## Claims affected

```text
T-8302
L-8302
L-8303
O-8301
X-8302
```

## Recommended next offense

1. Generate zero-first-level replacement gadgets modulo each certified prime.
2. Divide their deltas by the exact denominator valuation and solve quotient-target Hensel digits.
3. Use the Farey hierarchy to obtain enough independent gadget scales.
4. Retain local excess paths so the order-cover reconstructs one primitive paired chart word.
5. Continue until the complete denominator is exhausted or a symbolic identity `C=ND` appears.

## Process improvement

Every cycle search should report three separate factorwise objects:

```text
numerator residue,
prefix-excess path,
ordinary quotient residue.
```

Reporting only `C=0 mod M` hides both word ambiguity and the real/p-adic quotient mismatch.
