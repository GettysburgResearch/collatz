# Session report — six-branch ordinary-extraction blocker

Agent: `gpt56-extraction-01`  
Issue: #58  
Branch: `agent/gpt56-extraction-01/58-six-branch-least-root`  
Date: 2026-07-25

## Starting hypothesis

The repository's positive constructions may be repeatedly rebuilding the same conditional implication:

```text
if one ordinary infinite path exists, then it grows.
```

The session therefore avoided new amplifiers and asked whether any current architecture contains a real ordinary-integer extraction theorem.

## Sources inspected

- repository operating model and live project-state materials;
- PR #3, #13, #19, #20, #33, #38, #44, #45, #48, #49, #50, #51, #56, and #57 at their current heads;
- issues #21, #54, #55, and the newly opened #58;
- PR #13 `LIT-KTHM-0048`, `0049`, `0053`, and `0054`;
- the current rational-base normality source and the Dubickas complexity interface.

All cross-branch statuses remain unchanged.

## Approaches attempted

1. Audited the exact logical distance of the leading refund, centered, H, cycle, and rational-base paths from a `K` object.
2. Reconstructed the nested least-root criterion independently.
3. Tested whether the six-branch ordinary high quotient recursively belongs to the same restricted language.
4. Allowed the stronger ansatz of one integer scale, six state-dependent translations, and state-dependent output permutations.
5. Enlarged that ansatz to an arbitrary finite control nucleus with state-dependent affine coordinates.
6. Considered whether existing complexity or rational-base normality results decide the language.

## New results

### 1. Blunt global assessment

The negative program contains genuine universal class exclusions. The positive infinite-orbit program has not yet crossed ordinary extraction.

The exact repeatedly missing inference is a uniform ordinary-height bound for nested least representatives. A completed `2`-adic point and conditional growth do not provide it.

### 2. Exact direct quotient failure — `L-7401`

For every one of the 36 ordered branch pairs, the ordinary high quotient after removing the first `Q`-block has a canonical first digit outside the original six-digit alphabet.

Thus no two-step root descends to a one-step root under `floor(x/Q)`.

### 3. Six-state affine section rigidity — `T-7401`

Every integer-affine, six-state high-quotient renormalization reproducing the complete branch system is forced to be the original forward map:

```text
scale v=P,
shift s_i=c_i,
symbol map identity.
```

### 4. Arbitrary finite affine nucleus rigidity — `T-7402`

Adding finite control does not repair the descent. After scale and alphabet normalization, the section carries would form a finite integer set closed under all six maps

\[
T_i(h)=(Ph-ma_i)/Q.
\]

The maximum and minimum of that set contradict each other for every nonzero `m`. For `m=0`, expansion by `P/Q>1` forces the set to be `{0}`. Hence every finite affine nucleus again reduces to the forward map.

### 5. Exact global target — `Q-7401`

The only remaining stationary-chart decision is whether the least roots stabilize or diverge.

- stabilization writes the counterexample root;
- divergence eliminates the exhaustive fixed chart.

## Candidate counterexamples

None. No `K-74xx` identifier was allocated.

## Failed approaches

1. **Ordinary quotient descent:** refuted universally by `L-7401`.
2. **State-dependent affine quotient closure:** refuted except for the trivial forward map by `T-7401`.
3. **Any finite affine section nucleus:** refuted by `T-7402`.
4. **Complexity-only exclusion:** the known linear factor-complexity lower bound is compatible with a six-letter sequence.
5. **Normality:** the available statement is conjectural and cannot be imported as a theorem.
6. **Conditional expansion:** explicit completion ghosts elsewhere in the repo show it cannot imply ordinary extraction.

## Potential errors and review targets

1. Recompute the six source/output rows independently.
2. Recheck the 36 quotient digits, preferably using the compact modulo-2048 table.
3. Reconstruct the affine automorphism lemma and the common-`K` elimination in `T-7401` without using this report.
4. Check the normalized-carry recurrence and max–min argument in `T-7402`.
5. Verify that the full-subtree hypothesis really supplies all six maps on the finite carry set.
6. Audit the branch-qualified physical mapping before any positive root is promoted.
7. Preserve the distinction between the quotient's canonical digit and its base-`Q` digit.

## Files changed

```text
research/six-branch-extraction/README.md
research/six-branch-extraction/GLOBAL_BLOCKER_ASSESSMENT.md
research/six-branch-extraction/CLAIM_INVENTORY.md
research/six-branch-extraction/Q-7401-least-root-decision.md
research/six-branch-extraction/claims/D-7401-six-branch-minimal-word-system.md
research/six-branch-extraction/claims/L-7401-high-quotient-section.md
research/six-branch-extraction/claims/T-7401-affine-section-rigidity.md
research/six-branch-extraction/claims/T-7402-finite-affine-nucleus-rigidity.md
reports/gpt56-extraction-01/2026-07-25-58-six-branch-global-blocker.md
```

## Claims affected

```text
D-7401  PROPOSED
L-7401  PROPOSED
T-7401  PROPOSED
T-7402  PROPOSED
Q-7401  IDEA / GLOBAL BLOCKER
```

External claims are cross-linked only and not promoted.

## Recommended next actions

Attack only

\[
m_n\longrightarrow\infty
\]

for the exact six-branch least roots, unless an explicit stabilizing root is found first. A valid proof must use global ordinary height, digit escape, or genuinely nonlinear/unbounded section information. Enlarging finite affine control cannot help.

## Organizational improvement

Every proposed counterexample architecture should maintain one line in its README:

```text
ORDINARY EXTRACTION OBLIGATION:
  exact nested sets S_n;
  least roots m_n;
  theorem proving boundedness/stabilization or divergence.
```

This would prevent local completion and growth results from being mistaken for positive existence.