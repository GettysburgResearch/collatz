# Session report — centered forced-tail PDR and PR #33 verification

**Agent:** `gpt56-pdr-01`  
**Model:** OpenAI `GPT-5.6 Pro`  
**Issue:** #40  
**Branch:** `agent/gpt56-pdr-01/40-centered-forced-tail-pdr`  
**Date:** 2026-07-22

## Starting hypothesis

A fixed-modulus PDR abstraction might either isolate a small recurrent kernel capable of yielding an ordinary lasso, or expose the exact extra state needed to distinguish finite integers from `2`-adic ghosts. Independently, PR #33's Evertse closure might fail at the signed co-cap, primitive normalization, or projective distinctness interface.

## Approaches attempted

1. Reconstructed PR #33 from the frozen PR #3 tower, connector, and stage-compression interfaces.
2. Inspected Evertse's original 1984 Corollary and matched every native hypothesis.
3. Compiled the centered eventual-zero equations into a partial integer map and a bounded-carry base-64 transducer.
4. Built full existential modular graphs through precision three.
5. Computed their greatest infinite-path kernels independently by SCC/reverse-basin and greatest-fixed-point deletion.
6. Identified the exact kernel symbolically and proved it at every precision.
7. Searched one million positive nearest-integer seeds and several simple renormalization-lasso patterns.
8. Developed the next proof-carrying top-boundary specification.

## New proved-looking results

- PR #33's `T-9705` chain passes independent reconstruction within the frozen corrected-stage class.
- The centered forced-tail map has invariant `B+4e mod17`, bounded carry at most 17, and strictly increases every positive ordinary state.
- At precision `64^d`, the greatest existential PDR kernel is exactly the `2^(d+1)`-state binary de Bruijn graph of length-`d+1` cylinder words.
- Every fixed-precision lasso is a periodic `2`-adic ghost; nonconstant periodic controls are not signed ordinary integers.

## Computational observations

- Full graph sizes:
  ```text
  d=1: ambient 4,     kernel 4
  d=2: ambient 256,   kernel 8
  d=3: ambient 16384, kernel 16
  ```
- Cylinder injectivity was checked through `d=15`.
- No seed `B<=10^6` survived more than four forced steps. The unique first record at depth four is `B=64^3,e=0`.
- Simple exact prefix-extension and scale-lasso searches over bounded seeds found no constructive closure. These are bounded diagnostics only.

## Candidate counterexamples

None. No `K-####` identifier is justified.

## Failed approaches

- Fixed dyadic precision does not shrink to an ordinary candidate. It retains the entire de Bruijn cylinder graph.
- Modular cycles are periodic `2`-adic completions, not finite ordinary integers.
- Residue-only refinement, even at high precision, cannot express canonical top termination.
- A bounded seed scan provides no infinite conclusion.

## Potential errors found

No mathematical error was found in the frozen PR #33 theorem chain.

One integration issue remains outside this packet: current PR #16 reserves `R-9304` for its corrected `9/8` refutation, while review PR #37 independently used `R-9304` for the `T-9318` constant-word refutation. Those review changes require claim-ID reconciliation before integration. The mathematical counterexample to the unrestricted `T-9318` remains valid.

## Files changed

- `research/centered-pdr/`
- `experiments/X-8701-centered-pdr/`
- `experiments/X-8702-pr33-evertse-audit/`
- `reports/gpt56-pdr-01/`

## Claims affected

New isolated claims `D-8701`, `L-8701`, `T-8701`, `R-8701`, `Q-8701`, `O-8701`, `X-8701`, and `X-8702`.

PR #33 review recommends independent verification of `L-9701`--`L-9706`, `T-9704`, and `T-9705` within their exact frozen scope.

## Recommended next actions

1. Build a one-counter regular-language synthesis lane for canonical finite base-64 words under the bounded-carry transducer.
2. Require every proposed invariant to discharge the most-significant-digit flush and finite-support obligations.
3. Couple reachable-state transducer synchronization to the verified recurrence/complexity lower bounds from PR #37.
4. Continue issue #39's cross-cycle constructive lane independently; PR #33 excludes only its own frozen class.
5. Reconcile the `R-9304` ID collision before merging PR #37 with the newer PR #16 head.

## Organizational improvement

Every modular/PDR certificate should declare its concretization level explicitly:

```text
finite integer state,
finite residue with existential high block,
compatible inverse-limit point,
periodic p-adic ghost,
ordinary initialized trajectory.
```

A certificate may be promoted only after its claimed concretization level is independently replayed.
