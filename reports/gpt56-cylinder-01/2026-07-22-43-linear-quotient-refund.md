# Linear quotient refund: local deterministic reduction

**Agent:** `gpt56-cylinder-01`  
**Issue:** `#43`  
**Date:** 2026-07-22  
**Status:** append-only research report; all theorem claims remain `PROPOSED`

## Objective

Refocus from exclusion of the frozen doubling-scale architecture to a genuinely counterexample-capable architecture. The required endpoint is one finite positive integer with exact all-time physical replay.

## Main discovery

The 256-transition linear stage from literature wave 7 is not the atomic object. The quotient-refund inequality already holds for one connector.

For a connector from height `t` through the next two linear-grid boundaries,

\[
N_t=3^{7(t+1)},
\qquad
Q_t=2^{11(t+33)}.
\]

At every multiple of `16` with `t>=3744`,

\[
N_t>2Q_t.
\]

The next connector is determined by one huge low-block divisibility test and one six-bit cell. If it exists, the next residual at least doubles.

Thus a forever-defined residual no longer needs a separately proved growth theorem. It is automatically a positive unbounded physical Collatz orbit.

## Causal Hensel state

The moving connector inverse is generated by ordinary integers

\[
Nr=1+Mc.
\]

When `t` increases by `16`, the modulus gains `176` binary bits and the odd exponent gains `112` ternary powers. The new carry is obtained by appending one exact base-`3^112` digit. This is a fixed-block recurrence from finite data, not a preloaded logarithm tape.

The canonical connector then has the exact finite normal form

\[
\eta=-\omega+\frac M{64}\delta+\epsilon M,
\]

with one six-bit `delta` and one wrap bit. The same data give the output cap exactly.

## Deterministic integer-first machine

The current ordinary state is

```text
tower height t,
current source/target types (i,j),
ordinary residual z.
```

The next type is not an input symbol. It is uniquely decoded from `z` if the low block is divisible. This removes the main weakness of the earlier pairwise theorem, where every finite word pair had lifts but no causal selector.

A complete counterexample is now exactly one finite state for which this partial map is defined forever.

## Structural negative information

The compatible completion set has depth-`N` cover

\[
4^N\text{ cylinders of radius }2^{-88N^2+O(N)},
\]

so it has Haar measure and Hausdorff dimension zero. A witness must be exceptional.

The exact periodic completion is a p-adic Tschakaloff vector with source parameter

\[
1-\log(2048)/\log(2187).
\]

Väänänen–Wallisser applies through minimal period `58` and first loses its stated numerical hypothesis at `59`. Short autonomous controllers are therefore excluded.

## Exact audit

`X-8501` passed both derivation and independent replay. The canonical payload digest is

```text
6328faaa680e54ab0245ba8cca41c81cebbb03ede1299b9d948d1fbf1aee41fd
```

The independent checker covers different connector heights and residual lifts and imports no author module.

## Remaining full-objective target

Prove one finite residual remains in the decoder domain forever. The best next method is a proof-carrying safety/PDR abstraction of the exact counter map, but it must retain the ordinary residual or a proved equivalent quotient invariant. A cycle in low residues alone is not a witness.

No `K-85xx` object is created.
