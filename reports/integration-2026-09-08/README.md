# Tail and rank integration candidate

**Prepared for independent integration review; not a merge or public-release clearance.** This candidate extends the cumulative scientific account without changing the earlier canonical mathematical statuses. No Collatz proof, complete selector, cofinal clearance, or vanishing exceptional-mass result is accepted here. The publishing PR identifies the final candidate head/tree.

## Exact scope and preservation

Base main is `ab7a62cbbb83860ea9436c5d3cf6cf7ce501066a`, including the owner-selected MIT license, contributor-first README, simplified access instructions and PR115's invitation to propose new problem repositories. The two research heads are unchanged from the [component review](../post-integration-2026-09-08/README.md):

| Source | Frozen head | Imported units |
|---|---|---|
| PR105 | `912aec235fb2c5dbeac69a82454dd9c3d3a1e9fe` | Critical tails ATT-001–006; clearance ATT-101–105; expanding-word rank ATT-201–206. |
| PR106 | `7b7471ea0e7b359a2ac46fb1e6aff269331509c3` | Linear frontier ALF-001–007; actual-prefix rank APR-001–009. |
| Review PR114 | `65235530e9cf4dda1c5850421b16b7189a491f36` | All eight review files, unchanged. |

All **46 source paths** are present. Both research trees, all author/publication receipts, all five canonical artifacts and all five generators are preserved byte-for-byte. The only two source changes are the verifier repairs below; their originals are also preserved [locally](../../archive/research-2026-09-08/README.md). The [preservation record](source-preservation.json) identifies the exact subtrees, exceptions, artifacts and reading-surface hashes. No older proof packet, correction history, canonical registry, workflow, license or contributor policy is rewritten. No source PR is closed.

The earlier review's narrow verdicts govern the named claims; author-stage PROPOSED headers remain as source history. The new prose in README, scientific status, cumulative catalog, proof index, conventions and existing mass/rank guides is an editorial synopsis, not acceptance of a new joint argument. The current catalog fingerprint is maintained in the D follow-up record with separate provenance; D's findings, review scope, original archive identities and all other active fingerprints are unchanged.

## Scientific reading path

Start at the [README](../../README.md) and [scientific status](../../STATE.md). The [mass guide](../../research/integrated/exceptional-mass/README.md#source-tails-and-clearance) explains sharp fixed-clock tails and the clearance forest's **positive** all-future residual. The [rank guide](../../research/integrated/rank-merging/README.md#linear-inverse-precision-and-completed-section-clocks) distinguishes linear inverse precision, rank-specific completed-section costs, formal-word rho and actual-prefix Gamma. Every earlier catalog family and its later restrictions remain visible.

The important interfaces are deliberately not combined: P, R_*, rho and Gamma are different ranks; raw shortcut, completed section, maximal-module and unsafe-return clocks differ. Gamma evaluates raw T after a visit to 1 and omits zero displacements; killing belongs to the separate mass operator. Its all-period sources vary with the requested horizon. The forest's `12/2^48` is an absolute residual for `1_U/R_*^2`, not natural density, a percentage solved or a contraction factor to multiply. Its norm-one obstruction, rho's illegal minimizers, Gamma's compulsory spikes and all cofinal/selector gaps remain beside the positive results.

## F01 implemented, not just proposed

The critical-tail and expanding-word verifiers now compare canonical typed JSON, rejecting integer/boolean/integral-float aliases even after the submitted digest is recomputed. Each also retains its original mutation tests and adds four typed substitutions; the no-op guard checks canonical encodings. The resulting self-test totals are **12 and 16**. AST comparison confirms that only `validate` and the respective self-test function changed; the mathematical reconstruction functions are unchanged.

| Experiment | Original verifier blob | Current verifier blob |
|---|---|---|
| X-ATT-001 | `38588f83e8dd1a234e753bcd8fb8bd3f7d6e6902` | `41ba1085e1be5d77e29dde454a873f55a0ad3bbc` |
| X-ATT-003 | `760b5ade36c9927dfeea96cbbaafac08a99b07de` | `de49bf089d19e036fa277e4ba19b21f0dba32f42` |

The other three verifiers are unchanged: their complete-envelope/encoding comparisons do not share F01. The preserved experiment READMEs describe the original mutation counts; this record documents the four additional tests. The old `prepare_checker_fixes.py --apply` deliberately refuses already-patched source blobs and must **not** be reapplied to this candidate.

## Checks actually completed in this preparation

Both modified native verifiers reconstructed their **entire declared canonical payloads**. The generated canonical bytes exactly matched the original artifact Git blobs, not merely a digest supplied by the artifact. Both final command-line runs passed normally and under optimized Python with site packages disabled, including every original and typed mutation. Their outputs are identical across modes. [Exact commands, outputs and identities](validation.json).

For X-ATT-003 this preserves its two evidence tiers: all 60 designated large-family positions receive exhaustive rank evaluation; other large cases check written-theorem premises plus physical trajectories. A full protocol replay is not a finite proof of its universal all-word theorem. Likewise no finite run settles the open selector or cofinal-clearance obligations.

The edited original reading pages were authenticated before amendment. Existing headings and link occurrences were retained, added links into edited pages have matching anchors, and the timed-bootstrap block was preserved exactly. The catalog's previous content remains in order; inherited qualifications are not replaced by new headlines. These are scoped editorial checks, not a browser usability test or an exhaustive external-link crawl.

**Not executed here:** the complete-checkout `tools/validate.py` bundle; the five native generators; the unchanged ATT-002, ALF-001 and APR native verifiers; or remote CI. The independent numerical reconstructions in PR114 and source publishers' Windows receipts remain attributed to their own exact heads, not claimed again as this preparation's runs. Direct Git access failed; a partial materialization and API tree checks are not a complete checkout. No preservation rule is disabled to hide that limit.

## Next agent: independent candidate review

Freeze the candidate PR's actual head and recheck main, PR105 and PR106. Review the candidate diff against its recorded base and the [claim matrix](../post-integration-2026-09-08/CLAIM_MATRIX.md), concentrating on source preservation, the typed fixes, and faithful summaries. Independently scrutinize any mathematical claims needed for approval, especially all-competing-prefix selection, finite-versus-cofinal clearance, and raw-versus-induced operator scopes; the earlier review is evidence, not a substitute for judgment.

Verify every source path against `source-preservation.json`; only the two documented verifier files may differ from their source versions. Confirm that all original artifacts, author receipts and review files match. Run all five native generators with `--check` and all five verifiers normally and under `-O` with `--self-test`, following their actual experiment READMEs. Then, on a clean complete checkout of the exact candidate, run:

```bash
python -X utf8 -B tools/validate.py --regressions --receipt ../collatz-candidate-validation.json
```

Record actual commands, exit codes, head/tree and any exclusions. Do not substitute successful scoped replay for full-tree execution, silently reset a source cutoff, or relax a frozen validator. Test the newcomer path from README to one current statement, proof, evidence and useful next contribution. Publish a separate review with exact findings and a merge recommendation; leave this candidate unmerged until the owner/integrator accepts that review. Source lifecycle and any public-launch decisions remain separate.
