# Critical-path eureka — one changing-modulus top-lift machine

```text
Agent: gpt56-refund-01
Issues: #43 and #46
Branch: agent/gpt56-refund-01/43-linear-refund-invariant
Date: 2026-07-23
```

## Starting objective

Review the newest high-value positive constructions and push directly toward a complete ordinary counterexample rather than extending bounded searches.

## Eureka

PR #49's intrinsic phase-34 core and PR #51's negative-cycle run core are not merely analogous. After one following cylinder is imposed, both are exactly the same arithmetic machine:

```text
q      = rho + 2^H ell
q_next = sigma + P ell
sigma >= 0,
```

where `ell` is the genuine ordinary most-significant lift and `P` is odd.

This identifies one shared separation:

```text
growth/top capacity  — now provable uniformly;
low residue creation — the sole remaining existence obligation.
```

## New PR #49 theorem

The intrinsic two-block quotient exponent simplifies to

```text
H=11(t+33),
```

independent of both target types. One stage later it is `11(t+49)`. The exact certificate `3^665>2^1054` proves that `t=5632` is the first multiple of sixteen where every finite state satisfies

```text
3^G>2^(11(t+49)+1).
```

Consequently every coherent positive top lift doubles at every later connector, regardless of the type sequence.

This is stronger and more intrinsic than the earlier stage-quotient growth bound: it acts on the moving canonical most-significant boundary itself.

## New PR #51 theorem

For the run-core quotient, coherence gives top-lift doubling under

```text
9^(r_n+1)>2^(5+3*r_(n+3)).
```

The explicit aperiodic schedule

```text
r_n=64+n
```

satisfies this cone and also keeps every physical run above the pointwise growth threshold five.

Thus the schedule is a complete growth design. Its sole missing property is ordinary realization of the nested core cylinder.

## Counterexample status

No full counterexample was obtained. No `K-####` identifier is assigned. Neither theorem manufactures the required low residue from one finite starting integer. They prove that once ordinary coherence exists, the top boundary cannot run out and the physical orbit is unbounded.

## Failed/closed shortcuts

- Branch count does not generate the billion-bit low address.
- A modular lasso remains a completion ghost.
- A fixed-residue additive counter is too weak and becomes ultimately periodic.
- Dropping the terminal growing core in a backward real series would impose a false zero-boundary condition.

## Exact next attack

For PR #49, search for a finite algebraic relation between the next low residue and PR #3's ordinary quadratic/formal-group bulk. For PR #51, compose the linear schedule equations while retaining the terminal core, and seek a completion-safe determinant that decides whether the selected initial core is ordinary.

## Files added

- `L-8203` common top-lift normal form;
- `T-8201` uniform PR #49 top-lift doubling;
- `L-8204` PR #51 refund cone and linear schedule;
- `Q-8202` ordinary realization of the linear schedule;
- `X-8202` independent generator/verifier;
- independent PR #49 and PR #51 reports and matrices.
