# Constructive analysis of the linear-height quotient-refund architecture

**Agent:** `gpt56-refund-01`  
**Issue:** #43  
**Independent parallel attempt:** yes; the earlier `gpt56-cylinder-01` owner retains the `85xx` namespace  
**Local namespace:** `82xx`  
**Date:** 2026-07-22

## Objective

The target was not another finite pair transition. It was one explicit ordinary positive initialization with a causal all-time rule and unbounded physical replay.

No such initialization was obtained. The session nevertheless isolates the positive problem more sharply: **growth is now proved automatic; existence is the only missing theorem.**

## 1. Independent reconstruction

From PR #3's scaled-tail recurrence on the linear grid `t_j=B+16j`, one stage has

```text
A(B)=1792B+3657472,
E(B)=2816B+5792512.
```

The next-cylinder equation has one quotient class because the stage odd multiplier is invertible modulo every dyadic radix. The finite pair theorem in `LIT-KTHM-0050` reconstructs exactly.

## 2. New two-stage theorem

At `B>=955024`, the current odd multiplier exceeds **twice** the complete radix two stages ahead:

```text
3^A(B) > 2^(E(B+8192)+1).
```

For a coherent path, write the current pair class as

```text
Y_n=y_n+Q_n k_n
```

and the next pair class similarly. Canonical caps are nonnegative, so exact division gives

```text
k_(n+1)>=floor(3^A(B_n) k_n / 2^E(B_n+8192)).
```

Thus `k_n>=1` implies `k_(n+1)>=2k_n`. Every actual positive lift path after the threshold is unbounded.

This eliminates a major constructive obligation: a future counterexample certificate need not prove drift separately.

## 3. Why finite transitions do not assemble automatically

For fixed current word, the 256 next tower types provide at most `4^256=2^512` input residue classes. At the first refund threshold the next modulus has

```text
1,361,752,832 bits.
```

The selector is therefore not a generic cover. It must generate one extraordinarily sparse arithmetic residue from the current ordinary state.

For a fixed infinite directive, the quotient equations remain an odd-affine chain over powers of two. Every finite prefix selects one initial residue; the infinite directive selects one `Z_2` point; an ordinary initial quotient exists exactly when the least initial representatives stabilize. The refund inequality does not affect this algebra.

## 4. Most promising crack

The stage word itself contributes only 512 control bits. The missing billion-bit address must be generated by arithmetic, not selected by branching. PR #3's ordinary quadratic/formal-group bulk generator is the strongest current candidate source of fresh low bits.

The next load-bearing theorem should be an exact coupling

```text
word-pair cylinder residue
 = finite affine/polynomial functional of the ordinary bulk state
```

with a simultaneous most-significant carry invariant. A bit-count comparison is not enough.

A successful certificate would contain:

```text
one explicit B_0>=955024,
one explicit positive quotient Y_0,
a finite controller,
one ordinary unbounded arithmetic state,
exact local connector replay,
canonical top-boundary flush.
```

Then `L-8201` supplies unboundedness.

## 5. Why the cycle lane remains complementary

A positive cycle remains the shortest finite proof object. PR #45 supplies a logarithmic-depth compiler at the correct continued-fraction scale, while PR #34 reduces false shortcuts:

- proper powers are duplicate cycles;
- Smith normal form restates `D|C`;
- a selected primitive prime power may divide both numerator and denominator;
- at least five non-neutral valuations are necessary.

The best finite route is an exact full-denominator circuit with genuinely varying prime-order cancellation, not a larger proper-modulus hit.

## 6. Counterexample status

No `K-82xx`, cycle, divergent seed, or all-time sanctuary is created. The new theorem is conditional only on coherence, and the coherence/ordinary-intersection theorem remains open.
