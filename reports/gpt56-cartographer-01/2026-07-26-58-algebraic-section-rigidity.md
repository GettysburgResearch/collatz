# Session report — finite algebraic extraction nuclei collapse

**Agent:** `gpt56-cartographer-01` (`GPT-5.6 Pro`)  
**Date:** 2026-07-26  
**Issues:** #36, #55, #58  
**Cartography branch:** `agent/gpt56-cartographer-01/36-global-counterexample-map`  
**Frozen offense source:** PR #64 at `88884c3e590b08aeb2018872987e71e14de1fe7b`  
**Candidate status:** no positive seed, cycle, or `K-####` object

## Objective

Continue only at the global ordinary-extraction blocker. The session rejected:

- another finite-prefix minimum census;
- another prescribed schedule;
- another conditional growth or refund theorem;
- another coordinate for the same inverse-limit point.

The target was either a direct decision of the six-branch least roots or an all-depth theorem eliminating an exhaustive extraction mechanism.

## Current exact decision

For the fixed six-branch rational-base chart, let `S_n` be the positive ordinary roots legal through depth `n` and let `m_n=min S_n`. The branch changes full-objective status only if one proves

```text
sup_n m_n < infinity
```

or

```text
m_n -> infinity.
```

The first case stabilizes at one written root and produces the physical shortcut-Collatz seed `6m-5`; the second eliminates the complete six-branch architecture.

## Concurrent work audited

PR #64 already developed, after the global-blocker handoff:

- direct high-quotient language exit;
- finite affine nucleus rigidity;
- finite rational-function nucleus rigidity;
- absence of semilinear value sanctuaries.

The rational-function proof is structurally sound within its submitted scope:

1. eventual integer values make a rational function polynomial by a Bezout-resultant argument;
2. exact child identities transport polynomial degree and leading coefficient;
3. a cycle in finite control forces degree one;
4. the finite affine rigidity theorem leaves only the original forward map.

The present contribution does not duplicate that chain.

## New theorem — `ACL-N092`

The rational hypothesis can be removed.

### Integer-valued algebraic branches are polynomial

A real algebraic branch analytic on a positive ray has Puiseux growth

\[
f^{(m)}(x)=O(x^{\alpha-m}).
\]

For `m>alpha`, its `m`th derivative tends to zero. The exact repeated-integral identity makes the integer forward differences `Delta^m f(n)` tend to zero, hence become identically zero. Newton interpolation gives a polynomial agreeing on every sufficiently large integer. The algebraic relation then forces the branch itself to equal that polynomial.

### General expanding-chart rigidity

Consider any stationary chart

\[
QF(r_i+Qk)=P(r_i+Qk)+a_i,
\qquad P>Q,
\]

whose digit alphabet has trivial affine stabilizer modulo `Q`.

For a finite-control family of eventually integer-valued algebraic sections carrying every child of the complete subtree:

1. the preceding lemma makes every section polynomial;
2. finite-control cycles force degree one;
3. rigid alphabet symmetry forces the common slope to satisfy `v=P mod Q` and fixes the labels;
4. normalized finite carries obey

   \[
   Qh'=Ph-ma_i;
   \]

5. minimum/maximum inequalities exclude `m>0` and `m<0`;
6. for `m=0`, expansion by `P/Q>1` forces the finite carry set to be `{0}`.

Therefore every section is

\[
f_i(k)=Pk+c_i,
\]

the original expanding forward boundary value.

PR #64 proves the rigid-alphabet hypothesis for the six physical digits, so the theorem applies directly.

## Why this is a genuine reduction

The result is uniform over every depth and every finite-control algebraic formula satisfying the exact full-tail hypotheses. It rules out an entire method class rather than reporting failed samples:

```text
finite affine extraction nuclei       closed;
finite rational extraction nuclei     closed;
finite algebraic extraction nuclei    closed.
```

The negative theorem is genuinely weaker than Collatz because it excludes only one proof mechanism inside a strict prescribed subsystem.

## What remains open

The theorem does not decide `m_n`. It does not exclude:

- one isolated survivor in a proper infinite sublanguage;
- genuinely unbounded arithmetic state;
- transcendental or pushdown sections;
- a direct height or digit-escape theorem.

Thus it is not an unconditional counterexample and not a complete six-branch exclusion.

## Blunt conclusion

The finite recursive-section program is exhausted much further than before. Enlarging an affine or rational section to an algebraic formula cannot cross ordinary extraction. A future positive construction must genuinely use unbounded state or solve the original least-root problem directly.

The exact remaining offense is unchanged but sharper:

```text
write the stabilizing six-branch root,
```

or

```text
prove the six-branch least roots tend to infinity.
```

## Files changed

- `cartography/FINITE_ALGEBRAIC_NUCLEUS_RIGIDITY.md`
- `ATOMIC_COUNTEREXAMPLE_LEMMAS.md`
- this report

No existing theorem status is promoted.
