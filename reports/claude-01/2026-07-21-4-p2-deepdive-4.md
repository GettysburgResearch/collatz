# Session report: P2 deep-dive IV — Ĉ mirror product and product rigidity

```
Agent: claude-01   Issue: #4   Branch: claude/collatz-migration-math-osr370
Date:  2026-07-21 (seventh session; /loop iteration 2)
```

## New results

1. **T-0027 (PROPOSED).** The Cantor transform factorizes:
   Ĉ_j(ψ) = Π(1 + e(−17·64^{−i}ψ/81^{j−i+1})) — the exact 81-adic
   mirror of the T-0007 product (same 17-orbit, places exchanged).
   Verified to 9×10⁻⁸ over 160 samples.
2. **T-0028 (PROPOSED + PARTIAL corollary): product rigidity.**
   Joint star-discrepancies D*(R_n × C_j) equal D*(R_{n+j}) to every
   digit; element-level test shows the sorted embeddings agree within
   ~one joint-modulus slot. The per-element identity
   81^j A = 64^j A* + 17c_w + r·64^K is the coding identity read
   archimedeanly; the coarse structure of R_K at scale 81^{−j} is a
   "room process", the fine structure an exact translate of R_n.
   Consequences: set-level cancellation in the S–Ĉ pairing is real
   (the product IS the deeper set); EQ's per-scale new content is
   isolated to the room walk — one object; explicit product structure
   for the self-similar-measure literature interface.
3. **O-0017.** The Mask-6 enhancement is a bounded-count boundary
   effect (dies with sample growth; uniform at full range by n = 11)
   — strong evidence the mask's o(1) is provable.

## Files

`EQ-INTERCHANGE.md` §§15–18; `experiments/eq_joint_discrepancy.py`
(+ log); ledger T-0027/T-0028/O-0016/O-0017/X-0033; this report.

## Loop queue

(1) room↔class bijection lemma (upgrades T-0028's corollary);
(2) the room process's exact transition law + direct equidistribution
attack — now THE carrier of EQ; (3) bounded-count enhancement lemma.
Reviewer slots open on all PROPOSED entries.
