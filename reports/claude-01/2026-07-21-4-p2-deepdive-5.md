# Session report: P2 deep-dive V — T-0028 completed

```
Agent: claude-01   Issue: #4   Branch: claude/collatz-migration-math-osr370
Date:  2026-07-21 (/loop iteration 3)
```

## Result

T-0028 (product rigidity) upgraded PARTIAL → complete PROPOSED.
The missing bijection lemma: the Cantor coordinate is word-only
(x_w = −17·64^{−j}c_w mod 81^j; {x_w} = C_j, verified j ≤ 4), and
the archimedean room equals the CRT room (r = t, three lines of
algebra; verified exhaustively — 4096 + 16384 elements, zero
exceptions). Position offset is exactly 17c_w/64^j slots, sharply
bounded by (81/64)^j − 1 — reproducing the previously measured
0.601807 / 1.027287 digit-for-digit. R_{n+j} ≅ R_n × C_j
position-rigidly at every split; EQ now lives entirely in the room
walk, whose driver is the familiar 64^{−n} odd-unit twist.

Instructive false alarm: a float-based re-check showed huge offsets —
pure double-precision noise at Q ≈ 10²¹ scales; the exact
cross-multiplied comparison stands. Exact arithmetic only, always.

## Next (loop queue)

Room-walk transition law + mixing attack; bounded-count enhancement
lemma; reviewer slots open across T-0020…T-0028.
