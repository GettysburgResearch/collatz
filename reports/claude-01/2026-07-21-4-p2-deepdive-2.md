# Session report: P2 deep-dive II — the room recursion and Mask 6

```
Agent:  claude-01   Issue: #4   Branch: claude/collatz-migration-math-osr370
Date:   2026-07-21 (fifth session)
```

## New results

1. **T-0024 (PROPOSED): the program's first unconditional
   near-window bound.** The pullback of the coding recursion in its
   one-room regime is an exact bijection carrying a FREE congruence
   `A′ ≡ ε (mod 81)`; discarding the congruences and iterating gives
   `#{A ∈ R_K : 0 < A ≤ 64^{(1−ε)K}} ≤ 2^{K(1−0.9455ε+o(1))}`,
   unconditionally — previously nothing below the trivial 2^K existed.
   Identity machine-verified in 144/144 cells (including the general
   multi-room form); bound checked on a (K, ε) grid.
2. **Mask 6 (O-0014).** Keeping the congruences recovers the exact
   law `2^{K(1−6ε)}` **iff** interval-restricted R_n equidistributes
   mod 81 (classes {0,1} → 2/81). EQ's core now has a purely
   combinatorial, Fourier-free equivalent form — the most elementary
   mask yet. Measured: 3×10⁻³ accuracy at full range, but a genuine
   2–3× enhancement of classes {0,1} at small Z (smallest survivors
   cluster near the trivial fixed points): the o(1) is real work.
3. **L-0017 (PROPOSED): (Λ) = twisted pair separation.** The L² range
   hypothesis reduces to `Σ_{pairs} 64^K/dist ≲ 4^K`, and pair
   differences stratify exactly as 64^{t₀}·(81^{−t₀} unit)·(copy):
   self-similar up to the odd-unit twist — (Λ) is wall-adjacent, so
   Mask 6 is the recommended attack surface, not (Λ).

## Failed approach recorded

First statement of the pullback identity omitted the room-wrap and
failed at boundary Y (7 mismatches at Y ≈ 64^{K−1}) — caught by the
script, fixed by the regime condition + general multi-room form.
Instructive: the identity is exact only where the archimedean window
respects the 81-room geometry.

## Files

Added: `experiments/eq_recursion_count.py` (+ log), this report;
`EQ-INTERCHANGE.md` §§7–10 appended; `CLAIMS.md` (T-0024, L-0017,
O-0014, X-0031).

## Next

1. Mask 6 at intermediate Z: quantify the enhancement's decay
   (mechanism: fixed-point clusters thin as (2/81)^depth?) — a
   provable-looking sub-target.
2. Recover part of the congruence information in T-0024
   unconditionally (even one bit per O(1) levels moves 0.9455ε
   toward 6ε).
3. Reviewer slots: T-0023/T-0024/L-0016/L-0017 open (M-0003).
