# Session report: T-0005 review + P2 deep-dive I

```
Agent:    claude-01
Issue:    #4
Branch:   claude/collatz-migration-math-osr370
Date:     2026-07-21 (fourth session)
```

## Part 1 — Adversarial review of PR3/T-0005 (reading-level, per owner guidance)

Reviewed PR #3's T-0005 (exponentially unbounded supercritical fibers)
and its dependency L-0005 (inverse signatures) by independent
re-derivation against this program's affine machinery. Findings:

* Their `B(w)` is exactly this program's `c_w` (RIGIDITY §0) —
  identical object, independent derivation; their bound (2), residue
  characterization (3), inverse criterion (6)–(7), and odd-tail lemma
  all check.
* T-0005's proof verifies step by step: signature pigeonhole (9);
  CRT tail synchronization (10) with hypothesis (1) used exactly
  where needed (positivity of `r_w` via `y > 3^a`); distinctness via
  Terras uniqueness at length L+k; supercriticality via (2); the
  `L = 3m, a = m` family arithmetic (k_m ≥ 2m+1; expansion in
  (1, 3/2] by minimality×3/2) and the Stirling asymptotic (8) are
  correct.
* Instance evidence: the 339-branch L = 44 chart was already
  independently machine-verified (X-0029).
* Minor note (cosmetic): the PR body's summary line can be misread as
  placing the fiber at depth 3m; the claim file correctly places it
  at depth `3m + k_m ≈ 5.42m` (asymptotic width slope 0.2159).

**Verdict: review passed — no gap found.** Under README §7 this
review supports promotion of PR3/T-0005 and PR3/L-0005 from PROPOSED
to PROVED on their branch (their ledger's call; recorded here and in
the PR comment). Consequently C-0003's unboundedness half rests on a
reviewed theorem once they record the promotion.

## Part 2 — P2 deep-dive I (`EQ-INTERCHANGE.md`)

* **L-0016 (union-bound obstruction, PROPOSED).** T-0012's
  exceptional densities summed over frequencies diverge like
  `(81κ₂)^m` (factor ≈ 60.6/scale): no marginal-theorem union bound
  can close the interchange. Joint (θ,K) structure is mandatory.
* **T-0023 (L² block-mean decay, PROPOSED).** T-0011's machinery with
  cos²: block means of `(|S_K|/2^K)²` ≤ `(1/2 + 1/81)^{m+1}`,
  position-free; corollary L² mass profile `≤ CX^{0.848}`.
* **The copy barrier (O-0013).** Worked the Fejér-majorant counting
  template through with the exact self-similar decomposition
  `Σ_{h≤H}|S_K(h)| = Σ_j 2^j Σ' |S_{K−j}(θ′)|`: resolving intervals
  below the fair window `32^K` forces the frequency window beyond
  `2^K`, where it contains the 2-adic copies; at measured profiles
  the template's error exceeds its main term for **every** ε > 1/6
  (deficit exponents 0.59ε / 0.46ε for the L¹ / L² variants). The
  obstruction is not lack of decay — it is that the beyond-range mass
  *is* the self-similarity `R_K = J₀R_{K−1} ⊔ J₁R_{K−1}`. Rigorous
  form needs anti-concentration (open; skeleton in place).
* **O-0012 (measurements, K ≤ 16).** In-range L² mass is O(1)
  ([0.88, 1.11] — hypothesis (Λ), the new isolated analytic target);
  in-range L¹ ≈ X^{1/3}; T-0011/T-0023 loose by ~10³ on small-θ
  blocks (truth far below the provable — encouraging for (Λ));
  |S_K(1)| non-monotone with near-exceptional depths (1e−1 at
  K = 10 vs 4e−7 at K = 15), geometric rate 0.585/level.
* **Revised P2 program** (§6): (i) prove (Λ) by a two-variable cos²
  Markov argument; (ii) recursion-direct counting of `R_K ∩ [0,Y]`
  (positivity lives there, unlike in majorants); (iii)
  anti-concentration for the barrier; (iv) the dial across ladder
  rungs.

## Corrections to my own prior text

`EQ.md`/T-0011's "essentially sharp" reading applies to truncated
top-level products, not to full `|S_K|` block means (measured 6.2e−4
vs bound 0.649 at K = 16). Recorded in O-0012; no migrated document
edited.

## Files changed

Added: `EQ-INTERCHANGE.md`, `experiments/eq_interchange_l1l2.py`
(+ log), this report. Updated: `CLAIMS.md` (T-0023, L-0016, O-0012,
O-0013, X-0030), `PACKETS.md` (P2 progress).

## Recommended next actions

1. Prove (Λ) — the in-range L² O(1) bound (P2's new isolated target).
2. Recursion-direct counting prototype (exact, small K) to test
   whether positivity beats the majorant barrier.
3. Independent re-derivation of the §4 exponent bookkeeping (M-0003
   slot).
4. gpt56-pro-01: record the T-0005/L-0005 review outcome in the PR #3
   ledger; a reciprocal review of CLAUDE/T-0020's Fatou step would
   complete the exchange.

## Organizational note

Review conducted at reading level without new computation, per owner
guidance on token economy — the 339-chart machine check from session 3
already covered the computational side. This division (verify
instances cheaply early, review proofs when they become load-bearing)
worked well and is proposed as standing practice.
