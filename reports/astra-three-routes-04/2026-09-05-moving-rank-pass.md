# Fourth continuation of PR #92: constructive moving-rank connection

Agent: `astra-three-routes-04` (GPT-6 Pro), 2026-09-05.
Frozen parent: `7d0397c92879545609aa4097f89f5497cd53026d`.
Existing branch: `agent/astra-three-routes-01/three-route-offense`.

The owner requested continuing all three routes and pursuing a genuine closing
insight. All three were pursued. No full Collatz proof was obtained; all new
theorem-level claims remain PROPOSED pending independent review.

## What changed in the attempt

The earlier positive-growth shadows were used constructively. A repeated word
1^a0 has an exact maximal residence, a block-adapted integer rank, and an
integrated all-source occupation formula. Instead of another finite feature
dictionary, take the minimum over ALL block-adapted ranks. An elementary
valuation argument reduces that infinite minimum to three fixed entries and
one input-dependent entry indexed by v3(2n+1).

This common rank decreases on nonvacuous ordinary CRT families with unbounded
mode, repetition count, and ternary precision, even while the number grows.
The complete inverse acceleration then collapses to an even ray and finitely
many odd branches, with arbitrary repetition confined to the single moving
index. This connects mass transport, source separation, and a constructive
rank within one exact ordinary model.

The first tempting full proof was false: the envelope increases at 7->13 and
9->7. Those controls are retained. Mode switches need additional credit; the
all-parameter aligned family itself exits to a changed active mode and cannot
be iterated for free. The one-edge two-sided cover also fails, with explicit
residuals such as 3 and 9. The next proposed target is NOT to prove that known
residuals never occur; it is to supply additional valid diagrams or control the
unsafe return distribution.

## Three substantive outcomes

Route 1: exact zeta(2)/(64-lambda) whole-source/all-initial-residence identity;
a summable bound over the infinite dictionary; and an all-source geometric
tail for the quarter-drop region, killed on unsafe states as well as 1.

Route 2: complete inverse source fan and positivity/maximality checks; one deep
odd column; short exact rank-fiber lists; complete finite one-edge lower-rank
normalization; and an all-height numerically-larger backward source family.

Route 3: total maximal-corridor acceleration; a proper integer rank computed
from at most four entries despite genuinely infinite mode support; and
all-parameter uphill common-rank quarter-drops. This is outside the previous
fixed finite polynomial-observation scope, not a refutation of that theorem.

## Checks actually performed

The generator and separately written verifier both pass on the frozen report,
including eight resealed corruption cases. The verifier uses literal stepping,
reverse words, and extended Euclid, with no generator/repository imports.
The exact coverage is recorded in the experiment README and pass4 entrypoint.
Python compilation, Markdown local links within the new packet, integer
example readback, and the exact parent README blob reconstruction were checked.

Semantic digest:

    7ac43379cc79e1cd550a77a72d8587b0e7d9b5cd45ed2bbf10895e16667b9710

Both programs have the same author: implementation independence is not
independent mathematical review. Universal statements have written elementary
proofs; finite samples are not extrapolated. No previous large inverse-cone
experiment, external payload or Lean build, full-repository validator, or
GitHub workflow was run.

## Publication boundary and next handoff

The pass is separate under research/astra-three-routes/pass4 and
experiments/X-ASTRA3-004-moving-ghost, plus this report and an append-only
parent README pointer. No existing proof or experiment body is changed.
The branch is to be advanced without rewriting history, main, another PR,
canonical mathematical status, settings, permissions, or workflows.

The mathematical handoff is to attack unsafe-mode transfers or a recursively
closed lower-rank merging cover using the exact source fan. The adapted
occupation seed is not assumed invariant after transfer. Neither the original
signed fixed-floor inequality nor SC* and cycle exclusion is declared solved.
