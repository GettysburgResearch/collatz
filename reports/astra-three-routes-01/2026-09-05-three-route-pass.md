# Three-route research pass — 2026-09-05

Agent: `astra-three-routes-01` (GPT-6 Pro).
Requested scope: pursue all three proposed full-resolution routes and publish a
single PR for collaborators to review and extend.

## Scientific output

The [packet](../../research/astra-three-routes/README.md) contains readable proofs,
precise scope, source pins, and finite evidence for all three routes:

1. Native aggregate Mellin transport and an explicit survivor-conditioned ternary
   bias target that would prove Collatz; source-cutoff-free finite-time mass
   certificates; a refutation of the stricter one-third target; and a critical
   exceptional-mass residue-bias identity.
2. An ordinary return-echo descent lemma, exact common-displacement thresholds by
   v_2(d), and a direct least-counterexample reduction that allows descent after
   the first coefficient crossing rather than demanding it exactly there.
3. An all-class obstruction to finite affine-valuation logarithmic ranks for
   every fixed shortcut block. A separate extension excludes that same class
   for the adaptive whole-run macro. The carry core is normalized by an exact
   terminating, unique-normal-form procedure; no global semantic rank is found.

Every new theorem-level claim remains PROPOSED pending independent mathematical
review. Neither the all-time Mellin ceiling, SC*, nor the complete echo-safe
exclusion is proved. No Collatz resolution is claimed.

## Frozen source state

Intended canonical base:
`9704bcf1ff33cc9e2b729e0c40137a1e55b95397`.

Current #88 was re-queried at
`c28922fb6d1c070bf86a76192f40bc9ea3edd67c`.
Current #90 was discovered and frozen at
`aeb69ce631370be36fd1f80b472ab448e7df2a73`.
Earlier source snapshots and external trust boundaries are in
[SOURCES.md](../../research/astra-three-routes/SOURCES.md).

## Checks actually run

The standard-library generator regenerated its exact canonical JSON. A separate
verifier imported no generator or repository module and reconstructed the
certificate. Both passed. Three resealed tamper cases were rejected: all-time
scope inflation, a missing mass horizon, and a nonphysical rank endpoint.

Coverage:

* all positive sources at H=1 through time 40, and at H=64 through time 36;
* complete inverse cones of 267,024 and 2,510,783 vertices at the final horizons;
* exact square-root and Peano-tail enclosures, including critical-bias radical
  brackets and the explicit 0.993 contraction constant;
* 32,768 ordinary return-echo tests;
* 12,449 first-crossing words through length 21, 9,779 formal rational positive-
  displacement pairs, and 6,431 echo rejections (zero ordinary sources among
  those formal positive-displacement pairs);
* 19,531 mixed-radix words, 64,746 carry swaps, and 16,384 odd macro sources;
* 18 exact ordinary fixed-block rank violations, and 6 adaptive-macro violations.

Final certificate semantic SHA-256:

```
f0b839ae1f53a2d3de138ab245e6961d8a0f608cdb5641781b10d51e8755565b
```

The full repository structural validator, external Lean builds, large external
payload replays, and GitHub workflows were not run. This was a local packet
materialization, not a filesystem-authenticated full repository checkout.

## Publication boundary

At the end of this authoring session, the connected GitHub action inventory
exposed read/search operations but no commit, push, branch-create, issue-create,
or PR-create action. Plugin discovery found the installed GitHub integration but
no additional available publishing action. A direct git remote access attempt
failed because github.com could not be resolved in the execution environment.

Therefore no remote branch, issue, commit, or PR was created in this session.
The complete result is packaged as one addition-only patch with a ready-to-use
PR description and an optional publication script for an authenticated checkout.
The local packaging commit is not represented as a commit on canonical main.
This access limitation does not change any mathematical verdict.

No existing repository file, source PR, setting, visibility, permission, or
workflow was changed.

## Handoff

Mathematical review should start with coefficient transport in both rank
obstructions, the killed inverse boundary and tail bounds, then the shifted
echo-displacement algebra. The next offensive pass should focus on the aggregate
survivor-bias estimate and joint echo/denominator compatibility, while retaining
the nonlinear/infinite-feature rank route as a clearly separated research lane.
