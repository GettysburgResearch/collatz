# First major integration report

## Snapshot

- cutoff: `2026-08-01T21:16:40Z` / `2026-08-02T00:16:40+03:00`;
- frozen main: `0ae0c67bb058f9a7c56cc7744fe5bf2650a7cb84`;
- open PRs: 45;
- exact-SHA reviewed: 42;
- unreviewed: #67, #68, #69;
- branch: `agent/gpt56-integrator-01/first-major-integration-pass`.

No source PR was merged into `main`, no repository visibility or settings were changed, and no public-release declaration was made.

## What entered the integrated state

1. A stable README that says Collatz is unsolved and separates finite compatibility, 2-adic completion, and one ordinary all-time trajectory.
2. A timestamped state and exact open-PR/head snapshot.
3. A structured claim registry with eight canonical records and two open roadmap obligations.
4. Branch-qualified aliases and an explicit collision/repair ledger.
5. A complete exact-SHA review coverage matrix for the 42 reviewed PRs.
6. A reusable integration practice, lightweight contributor contract, consistency checker, and next-integrator handoff.

The integration mode is **claim-level canonicalization by frozen reference**. Proof-bearing files remain on their source commits; this pass does not duplicate or silently edit contributor proofs. A future pass may import a proof packet after reconciling its dependencies and current-head delta.

## Coherent packets extracted

- **Ordinary extraction:** PR #57 `L-7601/T-7601` is canonical; overlapping PR #3/#56/#60 formulations are aliases and independent corroboration.
- **Completion firewall:** PR #57 `T-7602` is integrated separately from any physical Collatz consequence.
- **Periodic/full denominator:** PR #61/#62/#63 are consolidated into a corrected canonical statement with the all-zero endpoint explicit.
- **SC interface:** PR #77 `T-6709/T-6710/L-6711` is integrated with the review's terminology correction; the universal SC* obligation remains open.
- **Finite safety:** PR #14's isolated theorem packet is integrated.
- **Six-branch rigidity:** PR #64's finite affine/rational/semilinear packet is canonicalized under a collision-free ID; the physical replay and PR #65 extension are excluded.
- **Refutation and repair:** PR #37 `T-9318` remains refuted; `T-9319` is a separate verified theorem.

## Editorial and structural fixes

- Bare colliding IDs are replaced at the integrated layer by canonical IDs while preserving `PR<number>:<claim-id>` aliases.
- Repairs and refutations are recorded as relations, not status overwrites.
- Mathematical status, integration status, and artifact/replay status are separated.
- Volatile PR inventory is moved out of the README into a timestamped snapshot.
- `SC*` terminology is kept distinct from the stronger literature equality `t(n)=tau_c(n)`.
- The periodic statement handles the all-zero endpoint explicitly.
- Current-head deltas are classified instead of inheriting frozen verdicts automatically.

## PR #74 decision

PR #74 is **not** adopted as the repository's final organization. Useful principles are extracted:

- exact-SHA review;
- public status `UNSOLVED`;
- artifact integration is not claim promotion;
- private pre-integration operation;
- contributor freedom;
- bounded CI and external proof-carrying computation;
- source/data/provenance discipline.

The following are deferred:

- public-launch checklist and announcement;
- visibility changes;
- licensing conclusions for legacy content;
- security reporting channel;
- team/protection/settings/2FA/App/Actions/runner checks;
- public issue/fork/direct-branch tests;
- backup/tag and privacy/copyright audits.

The replacement structure is lighter: a stable README, immutable timestamped state, a small canonical registry, and no submission bureaucracy for exploratory work.

## Deferred packets and reasons

- **#11:** reject monolithic integration. Prepare a clean salvage PR for the narrow verified lemmas and retain the false tensor-freeze and fixed-modulus claims as refutations.
- **#19:** integrate its exact native H core only after a dedicated extraction; leave `C-9501`, source-blocked `T-9509`, finite-certificate `T-9515`, and source-qualified `T-9516` separate.
- **#34:** split the reviewed cycle/method-boundary claims from the blocked all-order period-four ladder.
- **#42:** retain verified reductions, but defer missing/unreplayed census headlines.
- **#47:** retain verified subchains, but resolve duplicate IDs, the 1024/2048 mismatch, and the unreplayed billion-pair dependency.
- **#49/#50/#51/#53:** retain as scoped H/cycle-family reference packets; apply the explicit repairs and dependency pins before proof import.
- **#65:** defer the algebraic/semialgebraic extension until the constant branch, finite-difference, semialgebraic, citation, and namespace repairs land at a reviewed SHA.
- **#67/#68/#69:** unreviewed; no classification.
- **post-review theorem deltas:** #13 proposed repair, #57 `L-7610`, #61 `O-7401`, #65 `L-7502`, #77 `M-6712`, and #83 `L-6916` remain outside older verdicts.

## Dependency and merge-order concerns

A safe proof-import order is approximately:

```text
canonical extraction base
  -> periodic/full-denominator firewall
  -> coefficient-stopping fixed-source interface
  -> repaired active dependencies from #34 / closed #82
  -> #81 support-corrected first-crossing layer
  -> #83 complete-factor layer
  -> #80 least-counterexample synthesis
```

The six-branch stack should be imported separately:

```text
#64 collision-free base
  -> repaired #65 and #66
```

The H/cycle-family stack requires exact parent pins:

```text
#3 phase interface -> #49
#47 pulse cocycle -> #51 -> #53 -> #70
```

## Checks performed

The integration changes are documentation, JSON, and a standard-library consistency checker. No proof search or large computation was rerun. The checker validates:

- JSON parseability and schema fields;
- 45 unique open PRs;
- exactly 42 reviewed PRs and unreviewed set `(67, 68, 69)`;
- full 40-hex SHAs;
- unique canonical IDs and allowed statuses;
- canonical Markdown coverage;
- alias targets;
- stable unsolved wording and integration pointers.
