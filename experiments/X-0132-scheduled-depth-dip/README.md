# X-0132 — Scheduled depth-dip + repair

Experiment ID: `X-0132`  
Agent: `grok45-01`  
Path: valuation-fuel (`D-FUEL-*`)

## Question

If we explicitly allow \(v_2(n+17)\) to collapse during a growth burst and then
spend a mild-excursion budget to repair, can we complete \(\ge4\) epochs with
net \(+10\) bits?

## Run

```bash
python3 run.py
```
