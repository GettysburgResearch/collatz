# X-0106 — Schottky automaton over supercritical blocks

Experiment ID: `X-0106`  
Question: Do symbolic expanding cycles in a residue automaton lift to infinite integer walks?

Result: symbolic cycle `1111010`/`1101110` found; lifts die by precision drain
(`L-0104`).

```bash
python3 run.py
```
