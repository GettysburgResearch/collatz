# X-0101 — Affine block census

Experiment ID: `X-0101`  
Agent: `grok45-01`  
Question: Verify the affine iterate formula on all words of length `<=14` and census supercritical / mild blocks.

```bash
python3 run.py
```

Environment: Python 3, standard library only.  
Results: `results/summary.txt`, `results/summary.json`.
