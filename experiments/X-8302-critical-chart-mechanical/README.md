# X-8302 — Paired chart, Farey commutator, and quotient-cylinder audit

Experiment ID: `X-8302`  
Agent: `gpt56-cycle-02`  
Issue: #9  
Classification: exact finite/compressed computation; no counterexample

## Research question

Can the exact negative-three two-branch chart be placed at PR #45's critical mechanical scale, repaired through the same certified denominator component, and coupled to factorwise ordinary quotient digits rather than numerator divisibility alone?

## Frozen work

The experiment:

1. verifies 720 small Farey-neighbor mechanical commutators;
2. certifies the exact PR #45 Farey pair and monomial commutator;
3. proves that the five known denominator factors form an order-cover for the complete excess window;
4. reconstructs p-adic quotient cylinders for the PR #45 word;
5. compiles the paired chart word with the same `(k,A,D)`;
6. verifies a frozen 41-swap solution modulo the five-factor product;
7. evaluates it in a 145-digit directed real interval;
8. reconstructs its factorwise quotient residues; and
9. rejects both frozen words by comparing the sole real integer candidate with the CRT quotient cylinder.

## Replay

```bash
python3 -B run.py --check-results results/canonical.json
python3 -B verify.py results/canonical.json
```

`verify.py` imports no author module and rebuilds the modular and real evaluations independently.

## Frozen digest

```text
canonical JSON SHA-256:
51bf08f45f38ebfcd6fb71dc31a84211bce54d43a769ed526726cd6138c5975b
```

## Interpretation boundary

The experiment proves exact identities for two finite compressed words and a reusable arithmetic interface. It does not factor the full denominator, construct a cycle, prove an infinite chart path, or resolve Collatz.
