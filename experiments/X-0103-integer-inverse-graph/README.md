# X-0103 — Integer port graph (power-of-two ports)

Experiment ID: `X-0103`  
Question: Do expanding multi-port walks exist among power-of-two ports?

Result: naive transitions vanish when destination moduli are powers of two
incompatible with odd multipliers; projective probe finds only finite shadows.
Superseded/clarified by `X-0105` and `L-0105`.

```bash
python3 run.py
```
