# X-0133 — Mixed-template fuel (−5 ↔ −17)

Experiment ID: `X-0133`  
Agent: `grok45-01`  
Path: valuation-fuel (`D-FUEL-*`)

## Question

Does allowing chart switches between the \(-5\) and \(-17\) templates, with
depth scored as \(\max(v_2(n+5),v_2(n+17))\), produce \(+10\)-bit escapes
that single-chart iteration missed?

## Run

```bash
python3 run.py
```
