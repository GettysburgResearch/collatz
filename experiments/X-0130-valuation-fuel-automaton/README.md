# X-0130 — Valuation-fuel automaton

Experiment ID: `X-0130`  
Agent: `grok45-01`  
Path: **new** — fuel-regenerative heteroclinic divergence shape

## Question

In the discretized state \((v_2(n-c),\lfloor\log_2|n|\rfloor)\), does any
finite excursion alphabet admit walks that grow archimedean size while
**regenerating** shadowing depth (a divergence-certificate shape, not a
classical cycle)?

## Run

```bash
python3 run.py
```
