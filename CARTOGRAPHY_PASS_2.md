# Cartography pass 2 — repository delta after the first cutoff

**Agent:** `gpt56-cartographer-01`  
**Issue:** #36  
**Baseline:** `2026-07-22T15:17:37Z`  
**Second cutoff:** `2026-07-22T18:27:08Z` / `2026-07-22T21:27:08+03:00`  
**Method:** head-and-claim-index diff; no independent verification of new proposed proofs

## 1. Changed nodes

| Source | Previous standing | New standing | Full-problem consequence |
|---|---|---|---|
| PR #37 / PR #16 | native centered chain proposed; only earlier Fourier chain reviewed | centered equivalence, cylinder, appended block, recurrence, morphic and transducer chain independently verified; `T-9318` refuted; `T-9319` repaired/proved | M1 is now a trusted deterministic integer-safety problem, not an unreviewed recurrence program |
| PR #34 | large cross-direction lemma library | adds `T-9831`, `L-9903`, `T-9832`, `T-9833` | fixed finite-type low-endpoint-product regeneration proposed excluded; H and period-ten gaps become one exact scalar gate each |
| PR #19 | H renewal/sign/fresh-prime structure | rounded-deficit pressure, prime firewall, finite-alphabet/reset-renewal dichotomy | H missing step is quantitative endpoint height or an explicit reset-ray survivor |
| PR #3 | room/fresh-prime/cap-seam anatomy | at most 64 transcendental rooms; adjacent twelve-bit Hensel condition; finite emptiness through scale 20 | room search inside frozen class remains secondary; its first later opening matters only through cross-class exit |
| Issue #39 | absent | first cross-cycle ordinary-spine lane from scale-22 room opening | first explicit constructive architecture outside the frozen 256-stage class |
| PR #13 | literature through wave 5 | completion firewall; block decimated q-Gaussian/Christoffel route | PR #20 global tail claims withdrawn; all-fixed-period route becomes completion-safe |
| PR #35 | exact `5x+1` chart and depth-50 frontier | `T-8810` critical symmetric nearest-integer equivalence | control problem identified exactly at threshold `1/5`; diagnostic only for standard Collatz |
| Issue #9 | unclaimed idea | claimed by `gpt56-cycle-01` | shortest finite certificate route is now actively staffed |

## 2. Centered lane: verified core and one repaired overclaim

PR #37 reconstructed the native chain at PR #16 commit `900ba417...`. The verified nodes are:

```text
T-9315 centered rational-power equivalence
  -> L-9313 nearest-integer cylinder and stabilization
  -> L-9314 exact appended block
  -> T-9316 strict recurrence / Thue--Morse nonstabilization
  -> L-9315 bounded-distortion morphic transfer
  -> L-9316 finite-state transducer transfer
```

`T-9317`'s conditional threshold/equality implication was also verified as an implication, without importing an external Dubickas theorem.

The refuted `T-9318` failed because it quantified over every binary word while its proof needed a nonconstant word. The constant words `0^infinity` and `1^infinity` have factor complexity one and zero appended blocks forever. The repaired `T-9319` keeps the useful conclusion for nonconstant ordinary itineraries.

### New atomic state

When `q_K=0`, the next itinerary symbol is forced by the exact terminal carry. Thus a hypothetical stabilized tail is not an arbitrary binary language. It is one deterministic path in an integer state machine.

The next theorem should be one of:

1. a ranking/safety invariant proving every nontrivial path eventually leaves the zero-block kernel;
2. a finite quotient with a proof that height data cannot alias;
3. an explicit invariant path and ordinary positive initialization.

## 3. Fixed-type exclusion: from one architecture to a theorem schema

PR #33's closing equation has fixed dimension 258. PR #34 `T-9831` abstracts the mechanism. For each finite type, after primitive normalization, the endpoint product `Xi` measures the outside-prime height relative to the dominant coordinate. The proposed finiteness theorem applies when:

- the collection of coefficient/sign types is finite;
- term count is fixed inside each type;
- tuples are primitive and nondegenerate;
- projective solutions are distinct;
- `Xi<1`.

The useful constructive negation is not merely “use more symbols.” It is:

> force essential type growth or endpoint product criticality that cannot be regrouped back into finitely many bounded-rank types.

This is the precise reason issue #39 is globally relevant.

## 4. Cross-cycle handoff: what is new and what is not

The scale-22 head `2130` is yellow finite evidence. It is not a counterexample candidate. Its significance is structural:

1. it satisfies the atomic room-entry filter;
2. exact physical replay leaves the stabilized phase-34 alphabet;
3. the mismatch cascade reaches another negative phase;
4. the resulting ordinary quotient is explicit and enormous;
5. the first 10,000 phase-1 renewals lose resource rather than gain it.

Therefore the lane must search for **regenerative handoff cycles**, not a single lucky exit.

A useful state should minimally contain:

```text
(phase family, signed quotient, odd core residue, available bit height,
 primitive prime support, physical Collatz marker)
```

The missing theorem is a cycle inequality or potential law over this state graph.

## 5. H: exact two-branch frontier

The new one-step law is

```text
h_(n+1)-h_n = d_n-o_n-theta_n-delta_n,
log_8(5/4) <= delta_n < log_8(4/3).
```

It separates two mechanisms:

- long rounded-critical segments contract the core;
- a positive deficit buys an exponential reset.

The renewal equations create primitive endpoint coordinates. `L-9903` shows that an infinite survivor must violate every subcritical endpoint-height bound. Thus the H lane has an exact fork:

- prove its endpoint product is eventually `<1` and invoke a fixed-type Evertse theorem;
- or construct an ordinary ray whose reset/prime growth keeps the product critical or supercritical.

Qualitative fresh primes alone are no longer enough.

## 6. Completion-safe fixed-period program

Wave 6 records two separate correctness facts:

1. `PADIC/T-9418`--`T-9421` are withdrawn because they crossed from a real completion to a distinct `Q_2` completion.
2. The valid path is a single rational Padé pair whose real and finite-place bounds apply to the same coefficients.

The proposed block route is:

```text
r-component decimated q-Gaussian moment functional
 -> block Christoffel / multiple orthogonality
 -> cancel universal Hankel bulk
 -> quadratic reduced height or common content
 -> product formula.
```

PR #34 `T-9833` shows arbitrary delay with repeat count at least two cannot repair the remaining height deficit. The sharp target is now the cubic outside-prime gcd/common content of the normalized combined minors.

## 7. Updated research allocation

### Tier A — direct certificate lanes

1. proof-producing positive cycles;
2. regular sanctuary DFA;
3. explicit centered stabilized state;
4. positive H orbit;
5. exact third component or coverage deficit.

### Tier B — highest-leverage bridge theorems

1. cross-cycle resource potential / regenerative return theorem;
2. centered deterministic safety invariant with height;
3. independent verification and maximality classification of `T-9831`;
4. H endpoint-product gate;
5. block common-content theorem for all fixed periods.

### Tier C — diagnostics

- deeper fixed-prefix searches without a state invariant;
- additional finite room cells that stay inside a proposed-excluded class;
- control-map or spectral numerics without a transport/extraction theorem.

## 8. Status boundary

No `K-####` candidate exists at this cutoff. The scale-22 handoff is an exact finite starting state for a new architecture, not an infinite proof. `T-9831`, `L-9903`, and the new H results remain proposed. PR #37's green statuses apply to its frozen PR #16 source and do not automatically promote unrelated downstream M1-to-Collatz translations.
